import LoginForm from "./LoginForm";

/**
 * Public login — Server Component kabuk.
 * Form HTML ilk response'ta gelir; oturum doğrulaması LoginForm içinde arka planda.
 */
export default function LoginPage() {
  return <LoginForm />;
}
