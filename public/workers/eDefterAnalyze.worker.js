/* annvero eDefterAnalyze classic worker — generated by scripts/bundle-edefter-analyze-worker.mjs; do not edit */
"use strict";
(() => {
  // src/config/eDefterKontrolDefaults.js
  var E_DEFTER_KAYNAK = {
    MUAVIN: "muavin",
    YEVMIYE: "yevmiye",
    MIZAN: "mizan",
    EDEFTER_LISTE: "edefter_liste",
    YEVMIYE_XML: "yevmiye_xml",
    KEBIR_XML: "kebir_xml",
    BERAT: "berat",
    TEKNIK: "teknik",
    VERGISEL: "vergisel",
    CAPRAZ: "capraz"
  };
  var E_DEFTER_FINDING_STATUS = {
    YENI: "Yeni",
    COZULDU: "\xC7\xF6z\xFCld\xFC",
    COZULMEDI: "\xC7\xF6z\xFClmedi"
  };
  var E_DEFTER_HATA_TURU = {
    TEKNIK: "Teknik",
    MUHASEBESEL: "Muhasebesel",
    VERGISEL: "Vergisel"
  };
  var E_DEFTER_SONUC_SEVIYE = {
    UYGUN: "Uygun",
    BILGI: "Bilgi",
    UYARI: "Uyar\u0131",
    KRITIK: "Kritik hata"
  };
  var E_DEFTER_RISK_LEVEL = {
    DUSUK: "D\xFC\u015F\xFCk",
    ORTA: "Orta",
    YUKSEK: "Y\xFCksek",
    KRITIK: "Kritik",
    /** Yeni 4 seviye ile uyum (alias) */
    UYGUN: E_DEFTER_SONUC_SEVIYE.UYGUN,
    BILGI: E_DEFTER_SONUC_SEVIYE.BILGI,
    UYARI: E_DEFTER_SONUC_SEVIYE.UYARI,
    KRITIK_HATA: E_DEFTER_SONUC_SEVIYE.KRITIK
  };
  var E_DEFTER_KONTROL_GRUP = {
    HATASIZ: "Hatas\u0131z kay\u0131tlar",
    KRITIK: "Kritik hatalar",
    MUKERRER: "M\xFCkerrer riskliler",
    TERS_BAKIYE: "Ters bakiye verenler",
    EKSIK_BILGI: "Eksik bilgi olanlar",
    DONEM_SONU: "D\xF6nem sonu kay\u0131t uyar\u0131lar\u0131",
    KDV_KONTROL: "KDV kontrol uyar\u0131lar\u0131",
    TEKNIK: "Teknik hatalar",
    VERGISEL: "Vergisel riskler",
    CAPRAZ: "\xC7apraz mutabakat",
    INCELEME_GEREKLI: "\u0130nceleme gerekli"
  };
  var E_DEFTER_KONTROL_DURUM = {
    HATASIZ: "Hatas\u0131z",
    KRITIK: "Kritik hata",
    MUKERRER: "M\xFCkerrer risk",
    TERS_BAKIYE: "Ters bakiye",
    EKSIK_BILGI: "Eksik bilgi",
    DONEM_SONU: "D\xF6nem sonu uyar\u0131",
    KDV_KONTROL: "KDV kontrol uyar\u0131",
    FIS_DENGESIZ: "Fi\u015F dengesiz",
    INCELEME_GEREKLI: "\u0130nceleme gerekli"
  };
  var E_DEFTER_ISSUE_SEVERITY = {
    BILGI: "BILGI",
    UYARI: "UYARI",
    KRITIK: "KRITIK"
  };
  var E_DEFTER_ISSUE_CODE = {
    ACCOUNT_NOT_IN_PLAN: "ACCOUNT_NOT_IN_PLAN",
    DATE_OUT_OF_PERIOD: "DATE_OUT_OF_PERIOD",
    NEGATIVE_AMOUNT: "NEGATIVE_AMOUNT",
    DEBIT_CREDIT_MISMATCH: "DEBIT_CREDIT_MISMATCH",
    DUPLICATE_ENTRY: "DUPLICATE_ENTRY",
    MISSING_DESCRIPTION: "MISSING_DESCRIPTION",
    MISSING_DOCUMENT_INFO: "MISSING_DOCUMENT_INFO",
    JOURNAL_SEQUENCE_GAP: "JOURNAL_SEQUENCE_GAP",
    ZERO_AMOUNT: "ZERO_AMOUNT",
    SUSPICIOUS_ROUNDING: "SUSPICIOUS_ROUNDING",
    DOCUMENT_DATE_GAP: "DOCUMENT_DATE_GAP",
    MULTI_COUNTERPART: "MULTI_COUNTERPART",
    MISSING_COUNTERPART: "MISSING_COUNTERPART",
    COUNTERPART_SELF: "COUNTERPART_SELF",
    COUNTERPART_SAME_SIDE: "COUNTERPART_SAME_SIDE",
    COUNTERPART_REVIEW: "COUNTERPART_REVIEW",
    COUNTERPART_CONFLICT: "COUNTERPART_CONFLICT",
    COUNTERPART_VERIFIED: "COUNTERPART_VERIFIED",
    PLAN_EVIDENCE_MISSING: "PLAN_EVIDENCE_MISSING",
    MIZAN_MUAVIN_MISMATCH: "MIZAN_MUAVIN_MISMATCH",
    MIZAN_MUAVIN_EVIDENCE_MISSING: "MIZAN_MUAVIN_EVIDENCE_MISSING",
    MUAVIN_YEVMIYE_MISMATCH: "MUAVIN_YEVMIYE_MISMATCH",
    MUAVIN_YEVMIYE_RECONCILE_FAILED: "MUAVIN_YEVMIYE_RECONCILE_FAILED",
    UNKNOWN_ISSUE: "UNKNOWN_ISSUE"
  };
  var E_DEFTER_FINDING_CODE = {
    COMPANY_MISMATCH: "COMPANY_MISMATCH",
    MIXED_COMPANY_OR_PERIOD: "MIXED_COMPANY_OR_PERIOD",
    JOURNAL_LEDGER_MISMATCH: "JOURNAL_LEDGER_MISMATCH",
    EXTERNAL_VERIFICATION_REQUIRED: "EXTERNAL_VERIFICATION_REQUIRED",
    HESAP_PLANINDA_YOK: "HESAP_PLANINDA_YOK",
    DONEM_DISI: "DONEM_DISI",
    NEGATIF_SIFIR: "NEGATIF_SIFIR",
    TOPLAM_BORC_ALACAK: "TOPLAM_BORC_ALACAK",
    ...E_DEFTER_ISSUE_CODE
  };
  var E_DEFTER_REPORT_DISCLAIMER = "Bu rapor ANNVERO E-Defter Kontrol Merkezi taraf\u0131ndan \xFCretilmi\u015Ftir. G\u0130B do\u011Frulamas\u0131 veya mali m\xFCh\xFCr kriptografik teyidi i\xE7ermez; kesin vergi h\xFCkm\xFC de\u011Fildir.";
  var RISK_BAND = {
    DUSUK: "D\xFC\u015F\xFCk",
    KONTROL: "Kontrol edilmeli",
    YUKSEK: "Y\xFCksek"
  };
  var KASA_BAKIYE_ESIK = 5e4;
  var NEAR_DATE_DAYS = 3;
  var BORC_ALACAK_TOLERANCE = 0.05;
  var BELGE_TARIH_FARK_GUN = 7;
  function riskBandFromScore(score) {
    if (score >= 70) return RISK_BAND.YUKSEK;
    if (score >= 31) return RISK_BAND.KONTROL;
    return RISK_BAND.DUSUK;
  }
  function riskLevelFromScore(score) {
    if (score >= 71) return E_DEFTER_RISK_LEVEL.KRITIK;
    if (score >= 51) return E_DEFTER_RISK_LEVEL.YUKSEK;
    if (score >= 31) return E_DEFTER_RISK_LEVEL.ORTA;
    return E_DEFTER_RISK_LEVEL.DUSUK;
  }
  function sonucSeviyeFromScore(score) {
    if (score >= 71) return E_DEFTER_SONUC_SEVIYE.KRITIK;
    if (score >= 40) return E_DEFTER_SONUC_SEVIYE.UYARI;
    if (score >= 15) return E_DEFTER_SONUC_SEVIYE.BILGI;
    return E_DEFTER_SONUC_SEVIYE.UYGUN;
  }
  function mapLegacyLevelToSonuc(level = "") {
    const t = String(level || "");
    if (t === E_DEFTER_SONUC_SEVIYE.KRITIK || t === E_DEFTER_RISK_LEVEL.KRITIK || t === "Kritik" || t === "Kritik hata") {
      return E_DEFTER_SONUC_SEVIYE.KRITIK;
    }
    if (t === E_DEFTER_RISK_LEVEL.YUKSEK || t === "Y\xFCksek" || t === E_DEFTER_SONUC_SEVIYE.UYARI) {
      return E_DEFTER_SONUC_SEVIYE.UYARI;
    }
    if (t === E_DEFTER_RISK_LEVEL.ORTA || t === "Orta" || t === E_DEFTER_SONUC_SEVIYE.BILGI || t === "Bilgi") {
      return E_DEFTER_SONUC_SEVIYE.BILGI;
    }
    return E_DEFTER_SONUC_SEVIYE.UYGUN;
  }

  // src/utils/cloudStorage/types.js
  var CLOUD_CONNECTION_STATUS = {
    DISCONNECTED: "disconnected",
    CONNECTED: "connected",
    ERROR: "error",
    PENDING: "pending"
  };
  var CLOUD_SYNC_STATUS = {
    IDLE: "idle",
    SYNCING: "syncing",
    OK: "ok",
    ERROR: "error"
  };
  var TAHAKKUK_EXCLUDED_OBLIGATIONS = Object.freeze(["MTV", "EMLAK"]);
  var emptyCloudStorageBinding = () => ({
    provider: null,
    connectionStatus: CLOUD_CONNECTION_STATUS.DISCONNECTED,
    accountEmail: "",
    rootFolderId: "",
    rootFolderName: "",
    folderStructureVersion: "",
    lastSyncAt: null,
    syncStatus: CLOUD_SYNC_STATUS.IDLE,
    lastError: "",
    indexedDocumentCount: 0
  });

  // src/utils/companyNormalize.js
  var emptyCompany = {
    id: "",
    companyName: "",
    taxNumber: "",
    taxOffice: "",
    address: "",
    notes: "",
    contactPerson: "",
    contactPhone: "",
    whatsappPhone: "",
    contactEmail: "",
    contacts: [],
    accountingSoftware: "LUCA",
    hotelSoftware: "YOK",
    hasForeignCurrency: false,
    isActive: true,
    enabledModules: {
      lucaExport: true,
      bankaParser: true,
      elektraweb: false,
      fifoFunds: false,
      payroll: false,
      taxControl: true,
      audit: true,
      officeManagement: true,
      policyExpense: true
    },
    bankAccounts: [],
    creditCards: [],
    cashAccounts: [],
    posMerchantAccounts: [],
    checkAccountMappings: {
      receivedChecksAccount: "",
      givenChecksAccount: "",
      useMonthlyGivenChecks: true,
      bankGivenChecks: []
    },
    taxSgkAccountMappings: {
      sgkMainAccount: "",
      sgdpAccount: "",
      unemploymentAccount: "",
      sgkIncentiveIncomeAccount: "",
      extraMappings: []
    },
    accountMappingResults: [],
    accountMappingSummary: null,
    documentSeriesRules: [],
    vehicles: [],
    employees: [],
    accountingRules: {
      mtvExpenseAccount: "",
      posAccountCode: "",
      salaryAccountCode: "",
      advanceAccountCode: "",
      businessAdvanceAccountCode: "",
      policyExpenseMethod: "AYLIK",
      exchangeDifferenceMethod: "DONEM_SONU",
      transferToleranceDays: 1,
      /** Firma Muhasebe Politikası — senaryo katmanı */
      useGivenChecksAccount: true,
      useReceivedChecksAccount: true,
      usePos108Accounts: true,
      useCash100Account: true,
      useFxSeparate102Accounts: true,
      /** Son kapalı/beratlı e-Defter dönemi (YYYY/AA) — düzeltme fişi varsayılan tarihi için */
      lastClosedEdefterPeriod: ""
    },
    /** Bulut Depolama — yalnız non-secret bağ (token yok) */
    cloudStorage: emptyCloudStorageBinding()
  };

  // src/utils/companyCenter.js
  var LUCA_TRANSFER_TTL_MS = 24 * 60 * 60 * 1e3;

  // src/utils/textNormalize.js
  var NORMALIZE_MEMO_MAX = 2e4;
  var activeNormalizeMemo = null;
  var ACCOUNT_CODE_INVISIBLE_RE = /[\u200B-\u200D\uFEFF\u00AD\u2060]/g;
  var ACCOUNT_CODE_SEPARATOR_RE = /[./]/g;
  function normalizeAccountCodeForComparison(value = "") {
    return String(value ?? "").replace(ACCOUNT_CODE_INVISIBLE_RE, "").normalize("NFC").trim().toLocaleUpperCase("tr-TR").replace(/\s+/g, "").replace(ACCOUNT_CODE_SEPARATOR_RE, "");
  }
  function computeNormalizeParserText(value) {
    return String(value || "").replaceAll("\u0131", "i").toUpperCase().replaceAll("\u0130", "I").replaceAll("\u011E", "G").replaceAll("\xDC", "U").replaceAll("\u015E", "S").replaceAll("\xD6", "O").replaceAll("\xC7", "C").replace(new RegExp("[.,/()\\-_*:;]", "g"), " ").replace(/\s+/g, " ").trim();
  }
  function normalizeParserText(value) {
    const profile = globalThis.__ANNVERO_ANALYSIS_PROFILE__;
    const started = profile?.enabled ? performance.now() : 0;
    const key = String(value ?? "");
    if (activeNormalizeMemo?.has(key)) {
      if (profile?.enabled) {
        profile.normalizeCallCount += 1;
        profile.normalizeMemoHitCount = (profile.normalizeMemoHitCount || 0) + 1;
        profile.normalizeTotalMs += performance.now() - started;
      }
      return activeNormalizeMemo.get(key);
    }
    const result = computeNormalizeParserText(value);
    if (activeNormalizeMemo && activeNormalizeMemo.size < NORMALIZE_MEMO_MAX) {
      activeNormalizeMemo.set(key, result);
    }
    if (profile?.enabled) {
      const elapsed = performance.now() - started;
      profile.normalizeCallCount += 1;
      profile.normalizeTotalMs += elapsed;
    }
    return result;
  }

  // src/utils/muhasebeDescriptionStandards.js
  var STANDARD_MASRAF_DESCRIPTION = "HAVALE/EFT MASRAFI";
  var STANDARD_POS_TAHSILATI = "POS TAHS\u0130LATI";
  var STANDARD_POS_BATCH_TAHSILATI = "POS BATCH TAHS\u0130LATI";
  var STANDARD_POS_KOMISYONU = "POS KOM\u0130SYONU";
  var STANDARD_DOVIZ = "D\xD6V\u0130Z ALI\u015E / SATI\u015E \u0130\u015ELEM\u0130";
  var STANDARD_SGK = "SGK \xD6DEMES\u0130";
  var STANDARD_VERGI = "VERG\u0130 \xD6DEMES\u0130";
  var STANDARD_TRAFIK = "TRAF\u0130K CEZASI \xD6DEMES\u0130";
  var STANDARD_CEK = "\xC7EK \xD6DEMES\u0130";

  // src/utils/transactionMemoryEngine.js
  var ISSUE_TYPE = {
    MISSING_CARI: "missing_cari",
    MISSING_ACCOUNT: "missing_account",
    UNCLEAR_DOCUMENT: "unclear_document",
    FIRST_SEEN: "first_seen"
  };
  var ISSUE_TYPE_META = {
    [ISSUE_TYPE.MISSING_CARI]: {
      id: ISSUE_TYPE.MISSING_CARI,
      label: "Cari bulunamad\u0131",
      className: "bg-amber-500/15 text-amber-200 ring-1 ring-amber-500/40"
    },
    [ISSUE_TYPE.MISSING_ACCOUNT]: {
      id: ISSUE_TYPE.MISSING_ACCOUNT,
      label: "Hesap bulunamad\u0131",
      className: "bg-sky-500/15 text-sky-200 ring-1 ring-sky-500/40"
    },
    [ISSUE_TYPE.UNCLEAR_DOCUMENT]: {
      id: ISSUE_TYPE.UNCLEAR_DOCUMENT,
      label: "Belge tipi belirsiz",
      className: "bg-emerald-500/15 text-emerald-200 ring-1 ring-emerald-500/40"
    },
    [ISSUE_TYPE.FIRST_SEEN]: {
      id: ISSUE_TYPE.FIRST_SEEN,
      label: "\u0130lk kez g\xF6r\xFClen",
      className: "bg-red-500/15 text-red-200 ring-1 ring-red-500/40"
    }
  };

  // src/utils/bankSmartSuggestions.js
  var SMART_SUGGESTION_CONFIDENCE = {
    HIGH: "y\xFCksek",
    MEDIUM: "orta",
    LOW: "d\xFC\u015F\xFCk"
  };
  var SAFE_SYSTEM_FAMILIES = [
    {
      id: "pos-batch",
      label: "POS batch tahsilat\u0131",
      positive: [
        "POS BATCH",
        "POS GUN SONU",
        "POS TOPLU",
        "BATCH POS",
        "GUNSONU POS",
        "UYE ISYERI BATCH"
      ],
      negative: ["KOMISYON", "KOM.", "MASRAF", "IADE", "BLOKE", "VIRMAN", "COZUM"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: ["KREDI", "KART"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_BATCH_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 96
    },
    {
      id: "pos-komisyon",
      label: "POS komisyonu",
      positive: [
        "POS KOMISYON",
        "POS KOM.",
        "SANAL POS KOMISYON",
        "BKM KOMISYON",
        "UYE ISYERI KOMISYON",
        "POS HIZMET BEDEL"
      ],
      negative: ["TAHSILAT", "BATCH", "SATIS ISLEM", "BLOKE COZUM", "HAVALE", "EFT"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["POS", "KOMISYON"] },
        { code: "760", nameKeywords: ["POS"] },
        { code: "780", nameKeywords: ["KOMISYON"] },
        { code: "770", nameKeywords: ["BANKA", "MASRAF"] },
        { code: "770", nameKeywords: ["KOMISYON"] }
      ],
      descriptionStandard: STANDARD_POS_KOMISYONU,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 95
    },
    {
      id: "pos-cozum",
      label: "POS \xE7\xF6z\xFCm / virman",
      positive: [
        "POS BLOKE COZUM",
        "POS BLOKE COZUMU",
        "BLOKE COZUM",
        "POS COZUM",
        "POS VIRMAN"
      ],
      negative: ["KOMISYON", "HAVALE", "EFT"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: ["BLOKE"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 94
    },
    {
      id: "pos-iade",
      label: "POS iadesi",
      positive: ["POS IADE", "POS IPTAL", "POS REFUND", "UYE ISYERI IADE"],
      negative: ["KOMISYON", "HAVALE"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 93
    },
    {
      id: "pos-bloke",
      label: "Blokeli POS",
      positive: ["BLOKE POS", "POS BLOKE", "POS BLOKEDE"],
      negative: ["COZUM", "KOMISYON", "HAVALE"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: ["BLOKE"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 93
    },
    {
      id: "pos-ertesi-gun",
      label: "Ertesi g\xFCn POS tahsilat\u0131",
      positive: ["ERTESI GUN POS", "POS ERTESI", "NEXT DAY POS", "T+1 POS"],
      negative: ["KOMISYON", "HAVALE"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_BATCH_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 93
    },
    {
      id: "pos-sanal",
      label: "Sanal POS",
      positive: ["SANAL POS", "VIRTUAL POS", "E-POS", "EPOS"],
      negative: ["KOMISYON", "HAVALE", "EFT"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: ["SANAL"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 93
    },
    {
      id: "pos-tahsilat",
      label: "POS tahsilat\u0131",
      positive: [
        "POS TAHSILAT",
        "POS SATIS",
        "POS PROVIZYON",
        "UYE ISYERI",
        "POS"
      ],
      negative: [
        "KOMISYON",
        "KOM.",
        "MASRAF",
        "BATCH",
        "HIZMET BEDEL",
        "HAVALE",
        "EFT",
        "FAST",
        "GLN",
        "GOND",
        "IADE",
        "BLOKE",
        "SANAL"
      ],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "108", nameKeywords: ["POS"] },
        { code: "108", nameKeywords: ["KREDI", "KART"] },
        { code: "108", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_POS_TAHSILATI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 90
    },
    {
      id: "havale-masraf",
      label: "EFT/havale masraf\u0131",
      positive: [
        "HAVALE MASRAF",
        "EFT MASRAF",
        "FAST UCRET",
        "FAST MASRAF",
        "BSMV",
        "HAVALE UCRET",
        "EFT UCRET",
        "BKM UCR",
        "HAVALE/EFT MASRAF",
        "HAVALE EFT MASRAF",
        "ISLEM UCRETI",
        "EFT ISLEM UCRET"
      ],
      negative: ["POS"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "780", nameKeywords: ["FINANSMAN"] },
        { code: "770", nameKeywords: ["BANKA", "MASRAF"] },
        { code: "770", nameKeywords: ["KOMISYON"] },
        { code: "770", nameKeywords: ["MASRAF"] }
      ],
      descriptionStandard: STANDARD_MASRAF_DESCRIPTION,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 95
    },
    {
      id: "diger-masraf",
      label: "Di\u011Fer banka masraf\u0131",
      positive: [
        "HESAP ISLETIM",
        "HESAP ISLETME",
        "PAKET UCRET",
        "KART YILLIK",
        "DIGITAL BANKACILIK UCRET",
        "BANKA MASRAF",
        "DEKONT UCRET"
      ],
      negative: ["POS", "HAVALE MASRAF", "EFT MASRAF", "FAIZ"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["BANKA", "MASRAF"] },
        { code: "770", nameKeywords: ["MASRAF"] },
        { code: "780", nameKeywords: ["FINANSMAN"] }
      ],
      descriptionStandard: STANDARD_MASRAF_DESCRIPTION,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 86
    },
    {
      id: "kredi-karti",
      label: "Kredi kart\u0131 \xF6demesi",
      positive: [
        "KREDI KART",
        "EKSTRE BORC",
        "KART EKSTRE",
        "KK ODEME",
        "KREDI KARTI ODEME",
        "KART BORC ODEME"
      ],
      negative: ["POS", "UYE ISYERI"],
      directions: ["CIKIS"],
      documentType: "KR",
      accountCandidates: [
        { code: "309", nameKeywords: ["KREDI", "KART"] },
        { code: "309", nameKeywords: ["KART"] },
        { code: "300", nameKeywords: ["KREDI", "KART"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 91
    },
    {
      id: "maas",
      label: "Maa\u015F \xF6demesi",
      positive: ["MAAS", "BORDRO", "PERSONEL UCRET", "UCRET ODEME", "MAAS ODEME"],
      negative: ["AVANS"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "335", nameKeywords: ["PERSONEL"] },
        { code: "335", nameKeywords: ["UCRET"] },
        { code: "335", nameKeywords: [] }
      ],
      descriptionStandard: null,
      islemTipi: "MAAS",
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 90
    },
    {
      id: "maas-avans",
      label: "Maa\u015F avans\u0131",
      positive: [
        "MAAS AVANS",
        "PERSONEL AVANS",
        "ON AVANS",
        "AVANS ODEME",
        "PERSONELE AVANS"
      ],
      negative: ["IS AVANS", "TEDARIK", "CARI"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "196", nameKeywords: ["AVANS"] },
        { code: "196", nameKeywords: ["PERSONEL"] },
        { code: "196", nameKeywords: [] }
      ],
      descriptionStandard: null,
      islemTipi: "MAAS AVANS",
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 89
    },
    {
      id: "is-avans",
      label: "\u0130\u015F avans\u0131",
      positive: ["IS AVANS", "IS AVANSI", "TEDARIKCI AVANS"],
      negative: ["MAAS", "PERSONEL", "BORDRO"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "195", nameKeywords: ["AVANS"] },
        { code: "195", nameKeywords: ["IS"] },
        { code: "195", nameKeywords: [] }
      ],
      descriptionStandard: null,
      islemTipi: "IS AVANS",
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 82
    },
    {
      id: "cek",
      label: "\xC7ek \xF6demesi",
      positive: [
        "CEK ODEME",
        "CEK TAHSIL",
        "KESTIGIMIZ CEK",
        "ALINAN CEK",
        "CEK BORCU",
        "CEK KARSILIGI"
      ],
      negative: ["HAVALE", "EFT", "POS", "FAST", "CEKIM", "ATM"],
      directions: ["CIKIS", "GIRIS"],
      documentType: "CK",
      accountCandidates: [
        { code: "101", nameKeywords: ["CEK"] },
        { code: "103", nameKeywords: ["CEK"] },
        { code: "121", nameKeywords: ["CEK"] }
      ],
      descriptionStandard: STANDARD_CEK,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 88
    },
    {
      id: "senet",
      label: "Senet \xF6demesi",
      positive: ["SENET", "BONO"],
      negative: ["POS", "HAVALE", "EFT"],
      directions: ["CIKIS", "GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "121", nameKeywords: ["SENET"] },
        { code: "321", nameKeywords: ["SENET"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 80
    },
    {
      id: "sgk",
      label: "SGK \xF6demesi",
      positive: ["SGK", "MUHSGK", "SOSYAL GUVENLIK", "BAGKUR", "PRIM ODEME"],
      negative: ["TRAFIK", "HAVALE", "EFT", "POS"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "361", nameKeywords: ["SGK"] },
        { code: "361", nameKeywords: ["SOSYAL"] },
        { code: "361", nameKeywords: ["PRIM"] },
        { code: "361", nameKeywords: ["ISSIZLIK"] }
      ],
      descriptionStandard: STANDARD_SGK,
      // Ham 360/361 kör atama yok — alt hesap plan hit gerekir
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 92
    },
    {
      id: "vergi",
      label: "Vergi \xF6demesi",
      positive: [
        "VERGI",
        "GIB",
        "IVD",
        "KDV2",
        "KDV ",
        "MUHTASAR",
        "STOPAJ",
        "GECICI VERGI"
      ],
      negative: [
        "TRAFIK",
        "CEZA",
        "EMLAK",
        "MTV",
        "AIDAT",
        "POS",
        "HAVALE",
        "FAIZ STOPAJ",
        "MEVDUAT STOPAJ",
        "FAIZ GELIR",
        "MEVDUAT FAIZ"
      ],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "360", nameKeywords: ["KDV"] },
        { code: "360", nameKeywords: ["MUHTASAR"] },
        { code: "360", nameKeywords: ["ODENECEK"] },
        { code: "360", nameKeywords: ["VERGI"] }
      ],
      descriptionStandard: STANDARD_VERGI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 91
    },
    {
      id: "gecikme-zammi",
      label: "Gecikme zamm\u0131",
      positive: ["GECIKME ZAMMI", "GECIKME FAIZ", "VERGI GECIKME"],
      negative: ["TRAFIK", "POS"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["GECIKME"] },
        { code: "770", nameKeywords: ["ZAMMI"] },
        { code: "689", nameKeywords: ["GECIKME"] }
      ],
      descriptionStandard: STANDARD_VERGI,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 90
    },
    {
      id: "damga-vergisi",
      label: "Damga vergisi",
      positive: ["DAMGA VERGISI", "DAMGA VERGI"],
      negative: ["TRAFIK", "POS", "HAVALE"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "360", nameKeywords: ["DAMGA"] },
        { code: "360", nameKeywords: ["VERGI"] }
      ],
      descriptionStandard: STANDARD_VERGI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 90
    },
    {
      id: "konaklama-vergisi",
      label: "Konaklama vergisi",
      positive: ["KONAKLAMA VERGISI", "KONAKLAMA VERGI"],
      negative: ["POS", "HAVALE", "REZERVASYON"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "360", nameKeywords: ["KONAKLAMA"] },
        { code: "360", nameKeywords: ["VERGI"] }
      ],
      descriptionStandard: STANDARD_VERGI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 90
    },
    {
      id: "turizm-vergisi",
      label: "Turizm / konaklama pay\u0131",
      positive: ["TURIZM PAYI", "TURIZM PAY", "TURIZM VERGISI"],
      negative: ["POS", "HAVALE"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "360", nameKeywords: ["TURIZM"] },
        { code: "360", nameKeywords: ["PAY"] }
      ],
      descriptionStandard: STANDARD_VERGI,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 89
    },
    {
      id: "vergi-cezasi",
      label: "Vergi cezas\u0131",
      positive: ["VERGI CEZASI", "VERGI CEZA", "USULSUZLUK CEZA"],
      negative: ["TRAFIK", "POS"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["CEZA"] },
        { code: "689", nameKeywords: ["CEZA"] },
        { code: "770", nameKeywords: ["VERGI"] }
      ],
      descriptionStandard: STANDARD_VERGI,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 89
    },
    {
      id: "emlak-vergisi",
      label: "Emlak vergisi",
      positive: ["EMLAK VERGISI", "EMLAK VERGI"],
      negative: ["TRAFIK", "HAVALE", "POS"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["EMLAK"] },
        { code: "770", nameKeywords: ["VERGI"] },
        { code: "360", nameKeywords: ["EMLAK"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 85
    },
    {
      id: "oda-aidati",
      label: "Oda aidat\u0131",
      positive: ["ODA AIDAT", "MESLEK ODASI", "ODA AIDATI"],
      negative: ["TRAFIK", "HAVALE", "EFT", "POS", "SITE AIDAT"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["AIDAT"] },
        { code: "770", nameKeywords: ["ODA"] },
        { code: "770", nameKeywords: ["MESLEK"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 84
    },
    {
      id: "mtv-emlak-aidat",
      label: "MTV",
      positive: ["MTV", "BELEDIYE MTV", "MOTORLU TASIT"],
      negative: [
        "TRAFIK CEZA",
        "KDV",
        "MUHTASAR",
        "HAVALE",
        "EFT",
        "FAST",
        "GLN",
        "GOND",
        "POS"
      ],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["MTV"] },
        { code: "770", nameKeywords: ["VERGI"] },
        { code: "360", nameKeywords: ["VERGI"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 83
    },
    {
      id: "trafik",
      label: "Trafik cezas\u0131",
      positive: [
        "TRAFIK CEZA",
        "TRAFIK CEZASI",
        "HTS CEZA",
        "EGM CEZA",
        "TRAFIK PARA CEZASI"
      ],
      negative: ["VERGI", "SGK", "KDV"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "770", nameKeywords: ["CEZA"] },
        { code: "770", nameKeywords: ["TRAFIK"] },
        { code: "689", nameKeywords: ["CEZA"] }
      ],
      descriptionStandard: STANDARD_TRAFIK,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 90
    },
    {
      id: "faiz-gelir",
      label: "Banka faiz geliri",
      positive: [
        "FAIZ GELIR",
        "VADE FAIZ",
        "MEVDUAT FAIZ",
        "FAIZ TAHSIL",
        "FAIZ GELIRI"
      ],
      negative: ["FAIZ GIDER", "KREDI FAIZ", "STOPAJ"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "642", nameKeywords: ["FAIZ"] },
        { code: "640", nameKeywords: ["FAIZ"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 88
    },
    {
      id: "faiz-stopaj",
      label: "Faiz stopaj\u0131 (193)",
      positive: [
        "FAIZ STOPAJ",
        "STOPAJ FAIZ",
        "MEVDUAT STOPAJ",
        "VADELI STOPAJ",
        "STOPAJ KESINT",
        "FAIZDEN STOPAJ"
      ],
      negative: [
        "MUHSGK",
        "MUHTASAR",
        "SGK",
        "KDV",
        "GECICI VERGI",
        "KURUMLAR",
        "DAMGA",
        "KONAKLAMA",
        "TURIZM PAY",
        "MTV",
        "EMLAK"
      ],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "193", nameKeywords: ["PESIN"] },
        { code: "193", nameKeywords: ["VERGI"] },
        { code: "193", nameKeywords: ["FON"] },
        { code: "193.01.001", nameKeywords: [] }
      ],
      descriptionStandard: null,
      // Kullanıcı belge kararı uygular — kör auto-fill yok
      autoFillIfPlanHit: false,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 93
    },
    {
      id: "faiz-gider",
      label: "Banka faiz gideri",
      positive: [
        "FAIZ GIDER",
        "KREDI FAIZ",
        "FINANSMAN FAIZ",
        "FAIZ ODEME",
        "FAIZ GIDER\u0130"
      ],
      negative: ["FAIZ GELIR", "STOPAJ"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "780", nameKeywords: ["FAIZ"] },
        { code: "660", nameKeywords: ["FAIZ"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 88
    },
    {
      id: "doviz",
      label: "D\xF6viz al\u0131\u015F/sat\u0131\u015F",
      positive: ["DOVIZ ALIS", "DOVIZ SATIS", "FX ALIS", "FX SATIS", "DOVIZ"],
      negative: ["POS", "HAVALE", "EFT", "KUR FARK"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "102", nameKeywords: ["USD"] },
        { code: "102", nameKeywords: ["EUR"] },
        { code: "102", nameKeywords: ["DOVIZ"] },
        { code: "646", nameKeywords: ["KUR"] },
        { code: "656", nameKeywords: ["KUR"] }
      ],
      descriptionStandard: STANDARD_DOVIZ,
      autoFillIfPlanHit: false,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 80
    },
    {
      id: "kur-farki",
      label: "Kur fark\u0131",
      positive: ["KUR FARK", "KUR FARKI", "FX DIFFERENCE"],
      negative: ["POS", "HAVALE"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "646", nameKeywords: ["KUR"] },
        { code: "656", nameKeywords: ["KUR"] },
        { code: "646", nameKeywords: [] },
        { code: "656", nameKeywords: [] }
      ],
      descriptionStandard: STANDARD_DOVIZ,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 82
    },
    {
      id: "virman",
      label: "Virman / hesaplar aras\u0131",
      positive: ["VIRMAN", "HESAPLAR ARASI", "ACCOUNT TRANSFER", "IC VIRMAN"],
      negative: ["POS", "HAVALE MASRAF", "EFT MASRAF"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "102", nameKeywords: ["BANKA"] },
        { code: "102", nameKeywords: [] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: false,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 84
    },
    {
      id: "fon-alis",
      label: "Fon al\u0131\u015F",
      positive: ["FON ALIS", "YATIRIM FONU ALIS", "FON ALIM"],
      negative: ["POS", "HAVALE"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "118", nameKeywords: ["FON"] },
        { code: "11", nameKeywords: ["FON"] },
        { code: "118", nameKeywords: ["YATIRIM"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 83
    },
    {
      id: "fon-satis",
      label: "Fon sat\u0131\u015F",
      positive: ["FON SATIS", "YATIRIM FONU SATIS", "FON SATIM"],
      negative: ["POS", "HAVALE"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "118", nameKeywords: ["FON"] },
        { code: "11", nameKeywords: ["FON"] },
        { code: "118", nameKeywords: ["YATIRIM"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 83
    },
    {
      id: "ters-repo",
      label: "Ters repo",
      positive: ["TERS REPO"],
      negative: ["POS"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "118", nameKeywords: ["REPO"] },
        { code: "108", nameKeywords: ["REPO"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 84
    },
    {
      id: "repo",
      label: "Repo",
      positive: ["REPO"],
      negative: ["TERS", "POS", "HAVALE"],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "118", nameKeywords: ["REPO"] },
        { code: "300", nameKeywords: ["REPO"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 83
    },
    {
      id: "kredi-kullanim",
      label: "Kredi kullan\u0131m\u0131",
      positive: ["KREDI KULLANIM", "KREDI KULLANDIRIM", "KREDI ODEME ALINDI"],
      negative: ["KART", "POS", "FAIZ", "ANAPARA"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "300", nameKeywords: ["KREDI"] },
        { code: "400", nameKeywords: ["KREDI"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 85
    },
    {
      id: "kredi-anapara",
      label: "Kredi anapara \xF6demesi",
      positive: ["KREDI ANAPARA", "ANAPARA ODEME", "KREDI TAKSIT ANAPARA"],
      negative: ["KART", "POS", "FAIZ", "KOMISYON"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "300", nameKeywords: ["KREDI"] },
        { code: "400", nameKeywords: ["KREDI"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 85
    },
    {
      id: "kredi-faiz",
      label: "Kredi faiz \xF6demesi",
      positive: ["KREDI FAIZ", "KREDI FAIZ ODEME"],
      negative: ["KART", "POS", "KMH", "EK HESAP"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "780", nameKeywords: ["FAIZ"] },
        { code: "660", nameKeywords: ["FAIZ"] },
        { code: "780", nameKeywords: ["KREDI"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 88
    },
    {
      id: "kmh-faiz",
      label: "Ek hesap / KMH faiz tahakkuku",
      positive: [
        "KMH FAIZ",
        "EK HESAP FAIZ",
        "KMH TAHAKKUK",
        "EK HESAP TAHAKKUK",
        "EKS\u0130 BAK\u0130YE FAIZ"
      ],
      negative: ["POS", "KREDI KART"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "780", nameKeywords: ["FAIZ"] },
        { code: "780", nameKeywords: ["KMH"] },
        { code: "660", nameKeywords: ["FAIZ"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 88
    },
    {
      id: "ek-hesap-kapama",
      label: "Ek hesap kapama",
      positive: ["EK HESAP KAPAMA", "KMH KAPAMA", "EKS\u0130 BAK\u0130YE KAPAMA"],
      negative: ["FAIZ", "POS"],
      directions: ["CIKIS", "GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "300", nameKeywords: ["KMH"] },
        { code: "300", nameKeywords: ["EK HESAP"] },
        { code: "309", nameKeywords: ["KMH"] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      requireSubAccount: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 82
    },
    {
      id: "nakit-cekim",
      label: "Nakit \xE7ekim",
      positive: [
        "NAKIT CEKIM",
        "ATM CEKIM",
        "VEZNEDEN CEKIM",
        "CASH WITHDRAWAL",
        "PARA CEKME"
      ],
      negative: ["YATIRMA", "DEPOSIT"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "100", nameKeywords: ["KASA"] },
        { code: "100", nameKeywords: ["NAKIT"] },
        { code: "100", nameKeywords: [] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 87
    },
    {
      id: "nakit-yatirma",
      label: "Nakit yat\u0131rma",
      positive: [
        "NAKIT YATIRMA",
        "VEZNE YATIRMA",
        "ATM YATIRMA",
        "CASH DEPOSIT",
        "PARA YATIRMA"
      ],
      negative: ["CEKIM"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "100", nameKeywords: ["KASA"] },
        { code: "100", nameKeywords: ["NAKIT"] },
        { code: "100", nameKeywords: [] }
      ],
      descriptionStandard: null,
      autoFillIfPlanHit: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.HIGH,
      score: 87
    },
    {
      id: "iade",
      label: "\u0130ade / ters kay\u0131t",
      positive: ["IADE", "TERS KAYIT", "IPTAL", "REFUND", "REVERSAL"],
      negative: [],
      directions: ["GIRIS", "CIKIS"],
      documentType: "DK",
      accountCandidates: [],
      descriptionStandard: null,
      autoFillIfPlanHit: false,
      confidence: SMART_SUGGESTION_CONFIDENCE.LOW,
      score: 60
    },
    {
      id: "gelen-havale",
      label: "Gelen havale",
      positive: [
        "GELEN HAVALE",
        "GLN HVL",
        "GLN. HVL",
        "GELEN EFT",
        "FAST GELEN",
        "HAVALE GELEN",
        "EFT GELEN",
        "ALINAN HAVALE",
        "GELEN FAST",
        "HAVALE/EFT GELEN",
        "EFT",
        "HAVALE",
        "FAST"
      ],
      negative: ["MASRAF", "UCRET", "KOMISYON", "POS", "CEK", "GIDEN", "GONDER"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "120", nameKeywords: [] },
        { code: "320", nameKeywords: [] }
      ],
      descriptionStandard: null,
      islemTipi: "GELEN",
      autoFillIfPlanHit: false,
      needsEntity: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 72
    },
    {
      id: "giden-havale",
      label: "Giden havale",
      positive: [
        "GIDEN HAVALE",
        "GOND HVL",
        "GONDERILEN HAVALE",
        "GONDERILEN EFT",
        "FAST GONDER",
        "EFT GONDER",
        "HAVALE GONDER",
        "GONDERILEN FAST",
        "HAVALE/EFT GIDEN",
        "GONDERME",
        "EFT",
        "HAVALE",
        "FAST"
      ],
      negative: [
        "MASRAF",
        "UCRET",
        "KOMISYON",
        "POS",
        "MAAS",
        "AVANS",
        "CEK",
        "SENET",
        "GELEN"
      ],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [
        { code: "320", nameKeywords: [] },
        { code: "120", nameKeywords: [] }
      ],
      descriptionStandard: null,
      islemTipi: "GIDEN",
      autoFillIfPlanHit: false,
      needsEntity: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 72
    },
    {
      id: "cari-tahsilat",
      label: "Cari tahsilat",
      positive: ["TAHSILAT", "CARI TAHSILAT", "FATURA TAHSILAT"],
      negative: ["POS", "MASRAF", "KOMISYON", "VERGI", "SGK"],
      directions: ["GIRIS"],
      documentType: "DK",
      accountCandidates: [{ code: "120", nameKeywords: [] }],
      descriptionStandard: null,
      islemTipi: "GELEN",
      autoFillIfPlanHit: false,
      needsEntity: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 68
    },
    {
      id: "cari-odeme",
      label: "Cari \xF6deme",
      positive: ["CARI ODEME", "FATURA ODEME", "TEDARIKCI ODEME"],
      negative: ["POS", "MASRAF", "MAAS", "VERGI", "SGK", "CEK"],
      directions: ["CIKIS"],
      documentType: "DK",
      accountCandidates: [{ code: "320", nameKeywords: [] }],
      descriptionStandard: null,
      islemTipi: "GIDEN",
      autoFillIfPlanHit: false,
      needsEntity: true,
      confidence: SMART_SUGGESTION_CONFIDENCE.MEDIUM,
      score: 68
    }
  ];
  var SMART_RULES = SAFE_SYSTEM_FAMILIES.filter(
    (f) => ["pos-tahsilat", "havale-masraf", "sgk", "maas", "is-avans"].includes(f.id)
  );

  // src/utils/bankTransactionType.js
  var BANK_TRANSACTION_TYPE = {
    // POS ailesi
    POS_TAHSILAT: "POS_TAHSILAT",
    POS_BATCH_TAHSILAT: "POS_BATCH_TAHSILAT",
    POS_KOMISYON: "POS_KOMISYON",
    POS_IADE: "POS_IADE",
    POS_BLOKE: "POS_BLOKE",
    POS_SANAL: "POS_SANAL",
    POS_COZUM: "POS_COZUM",
    POS_ERTESI_GUN: "POS_ERTESI_GUN",
    // Transfer / cari
    GELEN_HAVALE: "GELEN_HAVALE",
    GIDEN_HAVALE: "GIDEN_HAVALE",
    MUSTERI_TAHSILAT: "MUSTERI_TAHSILAT",
    TEDARIKCI_ODEME: "TEDARIKCI_ODEME",
    DIGER_CARI_HAREKET: "DIGER_CARI_HAREKET",
    /** @deprecated alias → MUSTERI_TAHSILAT / DIGER_CARI_HAREKET */
    CARI_TAHSILAT: "CARI_TAHSILAT",
    /** @deprecated alias → TEDARIKCI_ODEME / DIGER_CARI_HAREKET */
    CARI_ODEME: "CARI_ODEME",
    CEK_ODEMESI: "CEK_ODEMESI",
    CEK_TAHSILATI: "CEK_TAHSILATI",
    /** @deprecated alias → CEK_ODEMESI / CEK_TAHSILATI */
    CEK: "CEK",
    SENET: "SENET",
    // Banka / personel
    BANKA_MASRAFI: "BANKA_MASRAFI",
    BSMV: "BSMV",
    KREDI_KARTI_ODEMESI: "KREDI_KARTI_ODEMESI",
    MAAS: "MAAS",
    MAAS_AVANSI: "MAAS_AVANSI",
    IS_AVANSI: "IS_AVANSI",
    // Vergi / SGK
    VERGI: "VERGI",
    SGK: "SGK",
    MUHSGK: "MUHSGK",
    KDV: "KDV",
    KDV2: "KDV2",
    /** V1 ödeme alias’ları — VERGI_SGK ailesi (mapper multi-leg Paket C) */
    MUHSGK_ODEME: "MUHSGK_ODEME",
    SGK_ODEME: "SGK_ODEME",
    KDV1_ODEME: "KDV1_ODEME",
    KDV2_ODEME: "KDV2_ODEME",
    GECICI_VERGI_ODEME: "GECICI_VERGI_ODEME",
    KURUMLAR_VERGISI_ODEME: "KURUMLAR_VERGISI_ODEME",
    DAMGA_VERGISI_ODEME: "DAMGA_VERGISI_ODEME",
    KONAKLAMA_VERGISI_ODEME: "KONAKLAMA_VERGISI_ODEME",
    TURIZM_PAYI_ODEME: "TURIZM_PAYI_ODEME",
    MTV: "MTV",
    EMLAK_VERGISI: "EMLAK_VERGISI",
    ODA_AIDATI: "ODA_AIDATI",
    KONAKLAMA_VERGISI: "KONAKLAMA_VERGISI",
    TURIZM_VERGISI: "TURIZM_VERGISI",
    TURIZM_PAYI: "TURIZM_PAYI",
    DAMGA_VERGISI: "DAMGA_VERGISI",
    VERGI_CEZASI: "VERGI_CEZASI",
    GECIKME_ZAMMI: "GECIKME_ZAMMI",
    CEZA: "CEZA",
    TRAFIK_CEZASI: "TRAFIK_CEZASI",
    // Finans / kasa / virman
    BANKA_ICI_VIRMAN: "BANKA_ICI_VIRMAN",
    BANKALAR_ARASI_VIRMAN: "BANKALAR_ARASI_VIRMAN",
    /** @deprecated alias → BANKA_ICI_VIRMAN / BANKALAR_ARASI_VIRMAN */
    VIRMAN: "VIRMAN",
    /** Ürün sınıflandırma aliası → BANKA_ICI_VIRMAN */
    BANK_INTERNAL_TRANSFER: "BANK_INTERNAL_TRANSFER",
    KASA_BANKAYA_YATAN: "KASA_BANKAYA_YATAN",
    BANKADAN_KASAYA_CEKILEN: "BANKADAN_KASAYA_CEKILEN",
    FON_ALIS: "FON_ALIS",
    FON_SATIS: "FON_SATIS",
    REPO: "REPO",
    TERS_REPO: "TERS_REPO",
    KREDI_KULLANIM: "KREDI_KULLANIM",
    KREDI_ANAPARA: "KREDI_ANAPARA",
    KREDI_FAIZ: "KREDI_FAIZ",
    KMH_FAIZ: "KMH_FAIZ",
    EK_HESAP_KAPAMA: "EK_HESAP_KAPAMA",
    DOVIZ_ALIS: "DOVIZ_ALIS",
    DOVIZ_SATIS: "DOVIZ_SATIS",
    KUR_FARKI: "KUR_FARKI",
    FAIZ_GELIRI: "FAIZ_GELIRI",
    FAIZ_GIDERI: "FAIZ_GIDERI",
    /** Mevduat faizinden kesilen stopaj → 193 Peşin Ödenen Vergiler (Vergi/SGK tahakkuk kapısı değil) */
    FAIZ_STOPAJI: "FAIZ_STOPAJI",
    /** Vadeli mevduat açılışı — karşı hesap aynı banka VADESIZ 102 */
    VADELI_ACILIS: "VADELI_ACILIS",
    /** Vadeli mevduat kapanışı — karşı hesap aynı banka VADESIZ 102 */
    VADELI_KAPANIS: "VADELI_KAPANIS",
    NAKIT_CEKME: "NAKIT_CEKME",
    /** @deprecated alias → NAKIT_CEKME */
    NAKIT_CEKIM: "NAKIT_CEKIM",
    NAKIT_YATIRMA: "NAKIT_YATIRMA",
    BILINMEYEN: "BILINMEYEN"
  };
  var POS_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.POS_TAHSILAT,
    BANK_TRANSACTION_TYPE.POS_BATCH_TAHSILAT,
    BANK_TRANSACTION_TYPE.POS_KOMISYON,
    BANK_TRANSACTION_TYPE.POS_IADE,
    BANK_TRANSACTION_TYPE.POS_BLOKE,
    BANK_TRANSACTION_TYPE.POS_SANAL,
    BANK_TRANSACTION_TYPE.POS_COZUM,
    BANK_TRANSACTION_TYPE.POS_ERTESI_GUN
  ]);
  var VERGI_SGK_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.VERGI,
    BANK_TRANSACTION_TYPE.SGK,
    BANK_TRANSACTION_TYPE.MUHSGK,
    BANK_TRANSACTION_TYPE.KDV,
    BANK_TRANSACTION_TYPE.KDV2,
    BANK_TRANSACTION_TYPE.MUHSGK_ODEME,
    BANK_TRANSACTION_TYPE.SGK_ODEME,
    BANK_TRANSACTION_TYPE.KDV1_ODEME,
    BANK_TRANSACTION_TYPE.KDV2_ODEME,
    BANK_TRANSACTION_TYPE.GECICI_VERGI_ODEME,
    BANK_TRANSACTION_TYPE.KURUMLAR_VERGISI_ODEME,
    BANK_TRANSACTION_TYPE.DAMGA_VERGISI_ODEME,
    BANK_TRANSACTION_TYPE.KONAKLAMA_VERGISI_ODEME,
    BANK_TRANSACTION_TYPE.TURIZM_PAYI_ODEME,
    BANK_TRANSACTION_TYPE.MTV,
    BANK_TRANSACTION_TYPE.EMLAK_VERGISI,
    BANK_TRANSACTION_TYPE.ODA_AIDATI,
    BANK_TRANSACTION_TYPE.KONAKLAMA_VERGISI,
    BANK_TRANSACTION_TYPE.TURIZM_VERGISI,
    BANK_TRANSACTION_TYPE.TURIZM_PAYI,
    BANK_TRANSACTION_TYPE.DAMGA_VERGISI,
    BANK_TRANSACTION_TYPE.VERGI_CEZASI,
    BANK_TRANSACTION_TYPE.GECIKME_ZAMMI,
    BANK_TRANSACTION_TYPE.CEZA,
    BANK_TRANSACTION_TYPE.TRAFIK_CEZASI
  ]);
  var CEK_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.CEK_ODEMESI,
    BANK_TRANSACTION_TYPE.CEK_TAHSILATI,
    BANK_TRANSACTION_TYPE.CEK
  ]);
  var KASA_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.KASA_BANKAYA_YATAN,
    BANK_TRANSACTION_TYPE.BANKADAN_KASAYA_CEKILEN
  ]);
  var VIRMAN_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.BANKA_ICI_VIRMAN,
    BANK_TRANSACTION_TYPE.BANKALAR_ARASI_VIRMAN,
    BANK_TRANSACTION_TYPE.VIRMAN,
    BANK_TRANSACTION_TYPE.BANK_INTERNAL_TRANSFER
  ]);
  var FINANCE_TYPES = /* @__PURE__ */ new Set([
    ...VIRMAN_TYPES,
    ...KASA_TYPES,
    BANK_TRANSACTION_TYPE.FON_ALIS,
    BANK_TRANSACTION_TYPE.FON_SATIS,
    BANK_TRANSACTION_TYPE.REPO,
    BANK_TRANSACTION_TYPE.TERS_REPO,
    BANK_TRANSACTION_TYPE.KREDI_KULLANIM,
    BANK_TRANSACTION_TYPE.KREDI_ANAPARA,
    BANK_TRANSACTION_TYPE.KREDI_FAIZ,
    BANK_TRANSACTION_TYPE.KMH_FAIZ,
    BANK_TRANSACTION_TYPE.EK_HESAP_KAPAMA,
    BANK_TRANSACTION_TYPE.DOVIZ_ALIS,
    BANK_TRANSACTION_TYPE.DOVIZ_SATIS,
    BANK_TRANSACTION_TYPE.KUR_FARKI,
    BANK_TRANSACTION_TYPE.FAIZ_GELIRI,
    BANK_TRANSACTION_TYPE.FAIZ_GIDERI,
    BANK_TRANSACTION_TYPE.FAIZ_STOPAJI,
    BANK_TRANSACTION_TYPE.VADELI_ACILIS,
    BANK_TRANSACTION_TYPE.VADELI_KAPANIS,
    BANK_TRANSACTION_TYPE.NAKIT_CEKME,
    BANK_TRANSACTION_TYPE.NAKIT_CEKIM,
    BANK_TRANSACTION_TYPE.NAKIT_YATIRMA
  ]);
  var CARI_NOT_REQUIRED_TYPES = /* @__PURE__ */ new Set([
    ...POS_TYPES,
    ...CEK_TYPES,
    ...KASA_TYPES,
    ...VIRMAN_TYPES,
    BANK_TRANSACTION_TYPE.BANKA_MASRAFI,
    BANK_TRANSACTION_TYPE.BSMV,
    BANK_TRANSACTION_TYPE.KREDI_KARTI_ODEMESI,
    ...VERGI_SGK_TYPES,
    ...FINANCE_TYPES
  ]);
  var CARI_REQUIRED_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.GELEN_HAVALE,
    BANK_TRANSACTION_TYPE.GIDEN_HAVALE,
    BANK_TRANSACTION_TYPE.MUSTERI_TAHSILAT,
    BANK_TRANSACTION_TYPE.TEDARIKCI_ODEME,
    BANK_TRANSACTION_TYPE.DIGER_CARI_HAREKET,
    BANK_TRANSACTION_TYPE.CARI_TAHSILAT,
    BANK_TRANSACTION_TYPE.CARI_ODEME,
    BANK_TRANSACTION_TYPE.IS_AVANSI,
    BANK_TRANSACTION_TYPE.SENET
  ]);
  var PERSONEL_REQUIRED_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.MAAS,
    BANK_TRANSACTION_TYPE.MAAS_AVANSI
  ]);
  var DIRECT_EXPENSE_ALLOWED_TYPES = /* @__PURE__ */ new Set([
    BANK_TRANSACTION_TYPE.BANKA_MASRAFI,
    BANK_TRANSACTION_TYPE.POS_KOMISYON,
    BANK_TRANSACTION_TYPE.FAIZ_GELIRI,
    BANK_TRANSACTION_TYPE.FAIZ_GIDERI,
    BANK_TRANSACTION_TYPE.FAIZ_STOPAJI,
    BANK_TRANSACTION_TYPE.KREDI_FAIZ,
    BANK_TRANSACTION_TYPE.KMH_FAIZ,
    BANK_TRANSACTION_TYPE.GECIKME_ZAMMI,
    BANK_TRANSACTION_TYPE.MTV,
    BANK_TRANSACTION_TYPE.EMLAK_VERGISI,
    BANK_TRANSACTION_TYPE.ODA_AIDATI,
    BANK_TRANSACTION_TYPE.CEZA,
    BANK_TRANSACTION_TYPE.VERGI_CEZASI
  ]);
  var FAMILY_ID_TO_TYPE = {
    "pos-batch": BANK_TRANSACTION_TYPE.POS_BATCH_TAHSILAT,
    "pos-tahsilat": BANK_TRANSACTION_TYPE.POS_TAHSILAT,
    "pos-komisyon": BANK_TRANSACTION_TYPE.POS_KOMISYON,
    "pos-iade": BANK_TRANSACTION_TYPE.POS_IADE,
    "pos-bloke": BANK_TRANSACTION_TYPE.POS_BLOKE,
    "pos-sanal": BANK_TRANSACTION_TYPE.POS_SANAL,
    "pos-cozum": BANK_TRANSACTION_TYPE.POS_COZUM,
    "pos-ertesi-gun": BANK_TRANSACTION_TYPE.POS_ERTESI_GUN,
    "havale-masraf": BANK_TRANSACTION_TYPE.BANKA_MASRAFI,
    "diger-masraf": BANK_TRANSACTION_TYPE.BANKA_MASRAFI,
    "gelen-havale": BANK_TRANSACTION_TYPE.GELEN_HAVALE,
    "giden-havale": BANK_TRANSACTION_TYPE.GIDEN_HAVALE,
    "kredi-karti": BANK_TRANSACTION_TYPE.KREDI_KARTI_ODEMESI,
    maas: BANK_TRANSACTION_TYPE.MAAS,
    "maas-avans": BANK_TRANSACTION_TYPE.MAAS_AVANSI,
    "is-avans": BANK_TRANSACTION_TYPE.IS_AVANSI,
    sgk: BANK_TRANSACTION_TYPE.SGK,
    vergi: BANK_TRANSACTION_TYPE.VERGI,
    "mtv-emlak-aidat": BANK_TRANSACTION_TYPE.MTV,
    "emlak-vergisi": BANK_TRANSACTION_TYPE.EMLAK_VERGISI,
    "oda-aidati": BANK_TRANSACTION_TYPE.ODA_AIDATI,
    "konaklama-vergisi": BANK_TRANSACTION_TYPE.KONAKLAMA_VERGISI,
    "turizm-vergisi": BANK_TRANSACTION_TYPE.TURIZM_VERGISI,
    "damga-vergisi": BANK_TRANSACTION_TYPE.DAMGA_VERGISI,
    "vergi-cezasi": BANK_TRANSACTION_TYPE.VERGI_CEZASI,
    "gecikme-zammi": BANK_TRANSACTION_TYPE.GECIKME_ZAMMI,
    trafik: BANK_TRANSACTION_TYPE.CEZA,
    "faiz-gelir": BANK_TRANSACTION_TYPE.FAIZ_GELIRI,
    "faiz-gider": BANK_TRANSACTION_TYPE.FAIZ_GIDERI,
    "faiz-stopaj": BANK_TRANSACTION_TYPE.FAIZ_STOPAJI,
    doviz: null,
    "kur-farki": BANK_TRANSACTION_TYPE.KUR_FARKI,
    "nakit-cekim": BANK_TRANSACTION_TYPE.NAKIT_CEKME,
    "nakit-yatirma": BANK_TRANSACTION_TYPE.NAKIT_YATIRMA,
    virman: BANK_TRANSACTION_TYPE.VIRMAN,
    "fon-alis": BANK_TRANSACTION_TYPE.FON_ALIS,
    "fon-satis": BANK_TRANSACTION_TYPE.FON_SATIS,
    repo: BANK_TRANSACTION_TYPE.REPO,
    "ters-repo": BANK_TRANSACTION_TYPE.TERS_REPO,
    "kredi-kullanim": BANK_TRANSACTION_TYPE.KREDI_KULLANIM,
    "kredi-anapara": BANK_TRANSACTION_TYPE.KREDI_ANAPARA,
    "kredi-faiz": BANK_TRANSACTION_TYPE.KREDI_FAIZ,
    "kmh-faiz": BANK_TRANSACTION_TYPE.KMH_FAIZ,
    "ek-hesap-kapama": BANK_TRANSACTION_TYPE.EK_HESAP_KAPAMA,
    cek: BANK_TRANSACTION_TYPE.CEK,
    senet: BANK_TRANSACTION_TYPE.SENET,
    "cari-tahsilat": BANK_TRANSACTION_TYPE.MUSTERI_TAHSILAT,
    "cari-odeme": BANK_TRANSACTION_TYPE.TEDARIKCI_ODEME,
    iade: BANK_TRANSACTION_TYPE.BILINMEYEN
  };

  // src/utils/formatDateTR.js
  function normalizeYear(yearPart) {
    const year = Number(yearPart);
    if (Number.isNaN(year)) {
      return null;
    }
    if (year < 100) {
      return 2e3 + year;
    }
    return year;
  }
  function excelSerialToDate(serial) {
    const utcDays = Math.floor(Number(serial) - 25569);
    const date = new Date(utcDays * 86400 * 1e3);
    return Number.isNaN(date.getTime()) ? null : date;
  }
  function isUtcMidnight(date) {
    return date.getUTCHours() === 0 && date.getUTCMinutes() === 0 && date.getUTCSeconds() === 0 && date.getUTCMilliseconds() === 0;
  }
  function calendarPartsFromExcelDate(value) {
    if (!(value instanceof Date) || Number.isNaN(value.getTime())) {
      return null;
    }
    if (isUtcMidnight(value)) {
      return {
        year: value.getFullYear(),
        month: value.getMonth() + 1,
        day: value.getDate()
      };
    }
    if (value.getUTCHours() === 21 && value.getUTCMinutes() === 0) {
      const shifted = new Date(value.getTime() + 3 * 3600 * 1e3);
      return {
        year: shifted.getUTCFullYear(),
        month: shifted.getUTCMonth() + 1,
        day: shifted.getUTCDate()
      };
    }
    const noonUtc = new Date(
      Date.UTC(value.getUTCFullYear(), value.getUTCMonth(), value.getUTCDate(), 12, 0, 0)
    );
    return {
      year: noonUtc.getUTCFullYear(),
      month: noonUtc.getUTCMonth() + 1,
      day: noonUtc.getUTCDate()
    };
  }
  function dateFromCalendarParts(parts) {
    return new Date(parts.year, parts.month - 1, parts.day);
  }
  function parseSlashDate(parts) {
    if (parts.length < 3) {
      return null;
    }
    const first = Number(parts[0]);
    const second = Number(parts[1]);
    const year = normalizeYear(parts[2]);
    if (Number.isNaN(first) || Number.isNaN(second) || year === null) {
      return null;
    }
    let day;
    let month;
    if (first > 12) {
      day = first;
      month = second;
    } else if (second > 12) {
      month = first;
      day = second;
    } else {
      month = first;
      day = second;
    }
    const date = new Date(year, month - 1, day);
    return Number.isNaN(date.getTime()) ? null : date;
  }
  function parseDateTR(value) {
    if (value === null || value === void 0 || value === "") {
      return null;
    }
    if (value instanceof Date && !Number.isNaN(value.getTime())) {
      const parts = calendarPartsFromExcelDate(value);
      return parts ? dateFromCalendarParts(parts) : value;
    }
    if (typeof value === "number") {
      return excelSerialToDate(value);
    }
    const text = String(value).trim();
    if (!text) {
      return null;
    }
    if (/^\d{1,2}\.\d{1,2}\.\d{2,4}$/.test(text)) {
      const [dayPart, monthPart, yearPart] = text.split(".");
      const year = normalizeYear(yearPart);
      const day = Number(dayPart);
      const month = Number(monthPart);
      if (year === null || Number.isNaN(day) || Number.isNaN(month)) {
        return null;
      }
      const date = new Date(year, month - 1, day);
      return Number.isNaN(date.getTime()) ? null : date;
    }
    if (text.includes("/")) {
      return parseSlashDate(text.split("/").map((part) => part.trim()));
    }
    if (/^\d{4}-\d{2}-\d{2}/.test(text)) {
      const date = new Date(text);
      return Number.isNaN(date.getTime()) ? null : date;
    }
    if (text.includes("-")) {
      const date = new Date(text);
      return Number.isNaN(date.getTime()) ? null : date;
    }
    return null;
  }
  function formatDateTR(value) {
    if (value === null || value === void 0 || value === "") {
      return "";
    }
    if (typeof value === "number") {
      const serialDate = excelSerialToDate(value);
      if (serialDate) {
        const day2 = String(serialDate.getUTCDate()).padStart(2, "0");
        const month2 = String(serialDate.getUTCMonth() + 1).padStart(2, "0");
        const year2 = serialDate.getUTCFullYear();
        return `${day2}.${month2}.${year2}`;
      }
    }
    if (typeof value === "string") {
      const trimmed = value.trim();
      if (/^\d{2}\.\d{2}\.\d{4}$/.test(trimmed)) {
        return trimmed;
      }
    }
    if (value instanceof Date && !Number.isNaN(value.getTime())) {
      const parts = calendarPartsFromExcelDate(value);
      if (parts) {
        const day2 = String(parts.day).padStart(2, "0");
        const month2 = String(parts.month).padStart(2, "0");
        return `${day2}.${month2}.${parts.year}`;
      }
    }
    const date = parseDateTR(value);
    if (!date || Number.isNaN(date.getTime())) {
      return String(value).trim();
    }
    const day = String(date.getDate()).padStart(2, "0");
    const month = String(date.getMonth() + 1).padStart(2, "0");
    const year = date.getFullYear();
    return `${day}.${month}.${year}`;
  }

  // src/utils/parseMoneyTR.js
  function parseMoneyTR(value) {
    if (value === null || value === void 0 || value === "") {
      return 0;
    }
    if (typeof value === "number") {
      return Number.isFinite(value) ? Math.abs(value) : 0;
    }
    let text = String(value).trim();
    if (!text) {
      return 0;
    }
    text = text.replace(/\s+/g, "").replace(/TL/gi, "").replace(/₺/g, "");
    const lastComma = text.lastIndexOf(",");
    const lastDot = text.lastIndexOf(".");
    if (lastComma !== -1 && lastDot !== -1) {
      if (lastComma > lastDot) {
        text = text.replace(/\./g, "").replace(",", ".");
      } else {
        text = text.replace(/,/g, "");
      }
    } else if (lastComma !== -1) {
      text = text.replace(",", ".");
    } else if (lastDot !== -1) {
      const dotCount = (text.match(/\./g) || []).length;
      if (dotCount > 1) {
        text = text.replace(/\./g, "");
      } else {
        const [, fraction = ""] = text.split(".");
        if (fraction.length === 3) {
          text = text.replace(".", "");
        }
      }
    }
    text = text.replace(/[^\d.]/g, "");
    const number = Number(text);
    if (Number.isNaN(number)) {
      return 0;
    }
    return Math.abs(number);
  }

  // src/utils/accountMemoryPolicy.js
  var MEMORY_DECISION_CODE = Object.freeze({
    AUTO_APPLIED: "MEMORY_AUTO_APPLIED",
    SUGGEST: "MEMORY_SUGGEST",
    REVIEW: "MEMORY_REVIEW",
    CONFLICT: "MEMORY_CONFLICT",
    CORE_OVERRIDE: "MEMORY_CORE_BLOCKED",
    IDEMPOTENT: "MEMORY_IDEMPOTENT",
    TENANT_DENIED: "MEMORY_TENANT_DENIED"
  });
  var MEMORY_PRIORITY = Object.freeze({
    CORE_MEVZUAT: 100,
    FIRM_USER_RULE: 80,
    FIRM_LEARNED: 60,
    SYSTEM_HINT: 40,
    REVIEW: 0
  });

  // src/utils/taxObligation/types.js
  var MATCH_STATUS = {
    FULL_MATCH: "FULL_MATCH",
    ACCRUAL_PENDING: "ACCRUAL_PENDING",
    MULTIPLE_CANDIDATES: "MULTIPLE_CANDIDATES",
    LATE_INCENTIVE_SUSPECT: "LATE_INCENTIVE_SUSPECT",
    SUPPORT_VERIFY_PENDING: "SUPPORT_VERIFY_PENDING",
    PARTIAL_PAYMENT: "PARTIAL_PAYMENT",
    OVERPAYMENT: "OVERPAYMENT",
    UNRESOLVED_DIFFERENCE: "UNRESOLVED_DIFFERENCE",
    MANUAL_REVIEW: "MANUAL_REVIEW"
  };
  var MATCH_STATUS_LABEL = {
    [MATCH_STATUS.FULL_MATCH]: "Tam e\u015Fle\u015Fti",
    [MATCH_STATUS.ACCRUAL_PENDING]: "Tahakkuk bekleniyor",
    [MATCH_STATUS.MULTIPLE_CANDIDATES]: "Birden fazla tahakkuk aday\u0131",
    [MATCH_STATUS.LATE_INCENTIVE_SUSPECT]: "Ge\xE7 \xF6deme / te\u015Fvik iptali \u015F\xFCphesi",
    [MATCH_STATUS.SUPPORT_VERIFY_PENDING]: "Destek fark\u0131 do\u011Frulama bekliyor",
    [MATCH_STATUS.PARTIAL_PAYMENT]: "K\u0131smi \xF6deme",
    [MATCH_STATUS.OVERPAYMENT]: "Fazla \xF6deme",
    [MATCH_STATUS.UNRESOLVED_DIFFERENCE]: "\xC7\xF6z\xFClmemi\u015F tutar fark\u0131",
    [MATCH_STATUS.MANUAL_REVIEW]: "Manuel inceleme"
  };

  // src/utils/vadeliMevduatLifecycle.js
  var VADELI_LIFECYCLE_ROLE = Object.freeze({
    ACILIS: "VADELI_ACILIS",
    KAPANIS: "VADELI_KAPANIS",
    FAIZ: "FAIZ_GELIRI",
    STOPAJ: "FAIZ_STOPAJI"
  });

  // src/utils/beyannameTahakkukEngine.js
  var BEYANNAME_TAHAKKUK_STORAGE_KEY = "annvero_beyanname_tahakkuk_v1";
  function safeParseJson(value, fallback) {
    try {
      return JSON.parse(value);
    } catch {
      return fallback;
    }
  }
  function loadDeclarationAccrualRecords() {
    if (typeof window === "undefined") return [];
    return safeParseJson(
      window.localStorage.getItem(BEYANNAME_TAHAKKUK_STORAGE_KEY) || "[]",
      []
    );
  }

  // src/utils/mizanAccountStructure.js
  var MIZAN_ACCOUNT_ROLE = {
    LEAF: "LEAF",
    PARENT: "PARENT"
  };
  function compactText(value = "") {
    return normalizeParserText(value).replace(/\s+/g, "");
  }
  function splitAccountCodeSegments(code = "") {
    return String(code ?? "").replace(/[\u200B-\u200D\uFEFF\u00AD\u2060]/g, "").normalize("NFC").trim().split(/[./]/).filter(Boolean);
  }
  function isHierarchicalAccountPrefix(parent = "", child = "") {
    if (!parent || !child || parent === child) return false;
    const parentSegs = splitAccountCodeSegments(parent);
    const childSegs = splitAccountCodeSegments(child);
    if (parentSegs.length < childSegs.length) {
      let segmentMatch = true;
      for (let i = 0; i < parentSegs.length; i += 1) {
        if (parentSegs[i] !== childSegs[i]) {
          segmentMatch = false;
          break;
        }
      }
      if (segmentMatch) return true;
    }
    const parentCompact = normalizeAccountCodeForComparison(parent);
    const childCompact = normalizeAccountCodeForComparison(child);
    return parentCompact !== childCompact && childCompact.startsWith(parentCompact);
  }
  function isMizanTotalsRow(row = {}) {
    const code = String(row.hesapKodu || "").trim();
    const name = String(row.hesapAdi || row.aciklama || "").trim();
    const combined = compactText(`${code} ${name}`);
    if (combined.includes("GENELTOPLAM")) return true;
    if (/^TOPLAM:?$/i.test(code.trim())) return true;
    return false;
  }
  function isMizanNonAccountRow(row = {}) {
    if (isMizanTotalsRow(row)) return true;
    const code = String(row.hesapKodu || "").trim();
    const name = String(row.hesapAdi || row.aciklama || "").trim();
    const combined = `${code} ${name}`.toLocaleUpperCase("tr-TR");
    if (!code && !name && !row.borc && !row.alacak) return true;
    if (/^HESAP\s*(KODU|KOD|ADI)?$/.test(combined.replace(/\s+/g, " ").trim())) {
      return true;
    }
    const compactCode = normalizeAccountCodeForComparison(code);
    if (!compactCode && /TOPLAM/.test(combined)) return true;
    return false;
  }
  function classifyMizanAccountCodes(codes = []) {
    const uniqueCodes = [...new Set(codes.filter(Boolean))];
    const parentCodes = /* @__PURE__ */ new Set();
    for (const code of uniqueCodes) {
      for (const other of uniqueCodes) {
        if (isHierarchicalAccountPrefix(code, other)) {
          parentCodes.add(code);
          break;
        }
      }
    }
    const leafCodes = new Set(uniqueCodes.filter((code) => !parentCodes.has(code)));
    return { parentCodes, leafCodes, accountCount: uniqueCodes.length };
  }
  function roundMoney(value) {
    return Number(Number(value || 0).toFixed(2));
  }
  function structureMizanParseResult(rawRows = []) {
    const skipRows = [];
    const accountRows = [];
    let mizanTotals = null;
    for (const row of rawRows) {
      if (isMizanTotalsRow(row)) {
        mizanTotals = {
          label: String(row.hesapKodu || row.hesapAdi || "GENEL TOPLAM").trim(),
          borc: roundMoney(row.borc),
          alacak: roundMoney(row.alacak),
          source: "GENEL_TOPLAM_ROW"
        };
        skipRows.push(row);
        continue;
      }
      if (isMizanNonAccountRow(row)) {
        skipRows.push(row);
        continue;
      }
      accountRows.push(row);
    }
    const codes = accountRows.map((row) => row.hesapKodu);
    const { parentCodes, leafCodes, accountCount } = classifyMizanAccountCodes(codes);
    const rows = accountRows.map((row) => ({
      ...row,
      mizanAccountRole: parentCodes.has(row.hesapKodu) ? MIZAN_ACCOUNT_ROLE.PARENT : MIZAN_ACCOUNT_ROLE.LEAF
    }));
    return {
      rows,
      mizanTotals,
      skipRows,
      stats: {
        parsedRowCount: rawRows.length,
        accountRowCount: accountRows.length,
        skipRowCount: skipRows.length,
        parentCount: parentCodes.size,
        leafCount: leafCodes.size,
        accountCount,
        totalsRowCount: mizanTotals ? 1 : 0
      }
    };
  }
  function filterLeafMizanRows(rows = []) {
    return (rows || []).filter((row) => row.mizanAccountRole !== MIZAN_ACCOUNT_ROLE.PARENT);
  }
  function verifyMizanMuavinGrandTotals({
    muavinRows = [],
    mizanTotals = null,
    leafMizanRows = [],
    tolerance = BORC_ALACAK_TOLERANCE
  } = {}) {
    const muavinBorc = roundMoney(
      (muavinRows || []).reduce((sum, row) => sum + roundMoney(row.borc), 0)
    );
    const muavinAlacak = roundMoney(
      (muavinRows || []).reduce((sum, row) => sum + roundMoney(row.alacak), 0)
    );
    let mizanBorc = roundMoney(mizanTotals?.borc);
    let mizanAlacak = roundMoney(mizanTotals?.alacak);
    let source = mizanTotals ? "GENEL_TOPLAM_ROW" : "LEAF_SUM";
    if (!mizanTotals) {
      mizanBorc = roundMoney(
        (leafMizanRows || []).reduce((sum, row) => sum + roundMoney(row.borc), 0)
      );
      mizanAlacak = roundMoney(
        (leafMizanRows || []).reduce((sum, row) => sum + roundMoney(row.alacak), 0)
      );
    }
    const borcDelta = roundMoney(muavinBorc - mizanBorc);
    const alacakDelta = roundMoney(muavinAlacak - mizanAlacak);
    const borcMatched = Math.abs(borcDelta) <= tolerance;
    const alacakMatched = Math.abs(alacakDelta) <= tolerance;
    return {
      matched: borcMatched && alacakMatched,
      source,
      muavin: { borc: muavinBorc, alacak: muavinAlacak },
      mizan: { borc: mizanBorc, alacak: mizanAlacak },
      delta: { borc: borcDelta, alacak: alacakDelta },
      tolerance
    };
  }

  // src/utils/openingVoucherClassify.js
  function compactText2(value = "") {
    return String(value ?? "").trim();
  }
  function normalizeOpeningLabelText(value = "") {
    return compactText2(value).toLocaleUpperCase("tr-TR").replace(/İ/g, "I").replace(/İ/g, "I").normalize("NFD").replace(/[\u0300-\u036f]/g, "");
  }
  var OPENING_LABEL_RE = /\b(ACILIS(\s+FIS[I]?)?|DEVIR(\s+FIS[I]?)?|OPENING(\s+VOUCHER)?|OPENING\s+ENTRY)\b/;
  function hasOpeningVoucherLabel(row = {}) {
    const blob = normalizeOpeningLabelText(
      [
        row.fisTuru,
        row.belgeTuru,
        row.documentType,
        row.fisTipi,
        row.voucherType,
        row.aciklama,
        row.belgeAciklama
      ].map((part) => compactText2(part)).filter(Boolean).join(" ")
    );
    if (!blob) return false;
    return OPENING_LABEL_RE.test(blob);
  }
  function normalizeNumericFisNo(value = "") {
    const trimmed = compactText2(value);
    if (!trimmed || !/^\d+$/.test(trimmed)) return null;
    const stripped = trimmed.replace(/^0+/, "");
    return stripped === "" ? "0" : stripped;
  }
  function isFiscalYearFirstDay(tarih = "") {
    const raw = compactText2(tarih);
    if (!raw) return false;
    const iso = raw.match(/^(\d{4})[./-](\d{1,2})[./-](\d{1,2})$/);
    if (iso) {
      const month = Number(iso[2]);
      const day = Number(iso[3]);
      return month === 1 && day === 1;
    }
    const dmy = raw.match(/^(\d{1,2})[./-](\d{1,2})[./-](\d{2,4})$/);
    if (dmy) {
      const day = Number(dmy[1]);
      const month = Number(dmy[2]);
      return month === 1 && day === 1;
    }
    return false;
  }
  function isOpeningVoucherGroup(rows = []) {
    const list = Array.isArray(rows) ? rows.filter(Boolean) : [];
    if (!list.length) return false;
    if (list.some((row) => hasOpeningVoucherLabel(row))) return true;
    const fisValues = [
      ...new Set(list.map((row) => normalizeNumericFisNo(row.fisNo)).filter(Boolean))
    ];
    if (fisValues.length !== 1 || fisValues[0] !== "1") return false;
    return list.some((row) => isFiscalYearFirstDay(row.tarih));
  }
  var OPENING_VOUCHER_CLASS = "OPENING_VOUCHER";

  // src/utils/companyIdentity.js
  var COMPANY_TAX_IDENTITY_KEYS = Object.freeze([
    "taxNumber",
    "vkn",
    "vergiNo",
    "tax_number",
    "taxId",
    "tax_id",
    "tckn"
  ]);
  function digitsOnly(value) {
    return String(value || "").replace(/\D/g, "");
  }
  function isValidVkn(vkn) {
    const d = digitsOnly(vkn);
    return d.length === 10;
  }

  // src/utils/eDefterCompanyIdentityGate.js
  var EDEFTER_IDENTITY_STATUS = Object.freeze({
    MATCHED: "MATCHED",
    MISMATCH: "MISMATCH",
    COMPANY_IDENTITY_MISSING: "COMPANY_IDENTITY_MISSING",
    DOCUMENT_IDENTITY_MISSING: "DOCUMENT_IDENTITY_MISSING",
    DOCUMENT_IDENTITY_MISSING_REVIEW: "DOCUMENT_IDENTITY_MISSING_REVIEW",
    IDENTITY_INVALID: "IDENTITY_INVALID",
    IDENTITY_AMBIGUOUS: "IDENTITY_AMBIGUOUS",
    IDENTITY_TYPE_CONFLICT: "IDENTITY_TYPE_CONFLICT",
    NOT_APPLICABLE: "NOT_APPLICABLE"
  });
  var EDEFTER_IDENTITY_TYPE = Object.freeze({
    VKN: "VKN",
    TCKN: "TCKN",
    UNKNOWN: "UNKNOWN",
    EMPTY: "EMPTY"
  });
  var IDENTITY_CONFIRMATION = Object.freeze({
    NONE: "",
    AUTO_MATCHED: "AUTO_MATCHED",
    USER_CONFIRMED: "USER_CONFIRMED",
    UNVERIFIED: "UNVERIFIED",
    BLOCKED: "BLOCKED"
  });
  var IDENTITY_CONFIRMATION_ALLOWLIST = Object.freeze([
    IDENTITY_CONFIRMATION.NONE,
    IDENTITY_CONFIRMATION.AUTO_MATCHED,
    IDENTITY_CONFIRMATION.USER_CONFIRMED,
    IDENTITY_CONFIRMATION.UNVERIFIED,
    IDENTITY_CONFIRMATION.BLOCKED
  ]);
  var BLOCKING_IDENTITY_STATUSES = Object.freeze([
    EDEFTER_IDENTITY_STATUS.MISMATCH,
    EDEFTER_IDENTITY_STATUS.COMPANY_IDENTITY_MISSING,
    EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING,
    EDEFTER_IDENTITY_STATUS.IDENTITY_INVALID,
    EDEFTER_IDENTITY_STATUS.IDENTITY_AMBIGUOUS,
    EDEFTER_IDENTITY_STATUS.IDENTITY_TYPE_CONFLICT
  ]);
  var EXCEL_REVIEW_IDENTITY_STATUSES = Object.freeze([
    EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING_REVIEW,
    EDEFTER_IDENTITY_STATUS.NOT_APPLICABLE
  ]);
  var SAFE_MESSAGES = Object.freeze({
    [EDEFTER_IDENTITY_STATUS.MATCHED]: "Firma ve belge kimli\u011Fi e\u015Fle\u015Fiyor.",
    [EDEFTER_IDENTITY_STATUS.MISMATCH]: "Belge kimli\u011Fi se\xE7ili firma ile uyu\u015Fmuyor. Analiz ve kay\u0131t durduruldu.",
    [EDEFTER_IDENTITY_STATUS.COMPANY_IDENTITY_MISSING]: "Se\xE7ili firman\u0131n vergi kimli\u011Fi eksik. Firma kimli\u011Fi tan\u0131mlanmadan devam edilemez.",
    [EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING]: "Belgede vergi kimli\u011Fi bulunamad\u0131. Kimlik do\u011Frulanmadan sonu\xE7 onayl\u0131 say\u0131lamaz.",
    [EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING_REVIEW]: "Excel girdide belge kimli\u011Fi yok; analiz devam eder, kay\u0131t i\xE7in kullan\u0131c\u0131 onay\u0131 gerekir.",
    [EDEFTER_IDENTITY_STATUS.IDENTITY_INVALID]: "Vergi kimli\u011Fi ge\xE7ersiz veya eksik uzunlukta. Analiz durduruldu.",
    [EDEFTER_IDENTITY_STATUS.IDENTITY_AMBIGUOUS]: "Pakette birden fazla farkl\u0131 firma kimli\u011Fi tespit edildi. Analiz durduruldu.",
    [EDEFTER_IDENTITY_STATUS.IDENTITY_TYPE_CONFLICT]: "Firma ve belge kimlik t\xFCrleri uyu\u015Fmuyor (VKN/TCKN). Analiz durduruldu.",
    [EDEFTER_IDENTITY_STATUS.NOT_APPLICABLE]: "Bu girdi i\xE7in belge kimli\u011Fi beklenmiyor; kimlik do\u011Frulanm\u0131\u015F say\u0131lmaz."
  });
  var EDEFTER_TEST_ONLY_IDENTITIES = Object.freeze({
    VKN_A: "1111111111",
    VKN_B: "2222222222",
    VKN_INVALID_SHORT: "12345",
    TCKN_A: "10000000146",
    TCKN_B: "10000000154"
  });
  function normalizeIdentityDigits(value = "") {
    return String(value || "").replace(/\D/g, "");
  }
  function classifyTaxIdentityType(value = "") {
    const d = normalizeIdentityDigits(value);
    if (!d) return EDEFTER_IDENTITY_TYPE.EMPTY;
    if (d.length === 10) return EDEFTER_IDENTITY_TYPE.VKN;
    if (d.length === 11) return EDEFTER_IDENTITY_TYPE.TCKN;
    return EDEFTER_IDENTITY_TYPE.UNKNOWN;
  }
  function isValidTaxIdentityDigits(value = "") {
    const d = normalizeIdentityDigits(value);
    if (d.length === 10) return isValidVkn(d);
    if (d.length === 11) return /^\d{11}$/.test(d);
    return false;
  }
  function maskTaxIdSafe(value = "") {
    const d = normalizeIdentityDigits(value);
    if (!d) return "";
    if (d.length <= 4) return `****${d.slice(-2)}`;
    return `${"*".repeat(Math.max(0, d.length - 4))}${d.slice(-4)}`;
  }
  function fingerprintTaxIdentity(value = "", companyId = "") {
    const d = normalizeIdentityDigits(value);
    if (!d) return "";
    const salt = String(companyId || "annvero-edefter").slice(0, 80);
    const type = classifyTaxIdentityType(d);
    let h = 2166136261;
    const material = `${salt}|${type}|len:${d.length}|tail:${d.slice(-2)}`;
    for (let i = 0; i < material.length; i += 1) {
      h ^= material.charCodeAt(i);
      h = Math.imul(h, 16777619);
    }
    let h2 = 2166136261;
    for (let i = d.length - 1; i >= 0; i -= 1) {
      h2 ^= d.charCodeAt(i) + salt.charCodeAt(i % Math.max(salt.length, 1));
      h2 = Math.imul(h2, 16777619);
    }
    return `eid_${type.toLowerCase()}_${(h >>> 0).toString(16)}${(h2 >>> 0).toString(16)}`;
  }
  function decision({
    status,
    matched = false,
    verified = false,
    userConfirmed = false,
    blocking = false,
    reviewRequired = false,
    allowAnalyze = true,
    allowPersist = false,
    allowExport = false,
    companyType = EDEFTER_IDENTITY_TYPE.EMPTY,
    documentType = EDEFTER_IDENTITY_TYPE.EMPTY,
    companyId = "",
    companyTaxId = "",
    documentTaxId = "",
    sourceKind = "xml",
    confirmation = IDENTITY_CONFIRMATION.NONE
  }) {
    const verifiedOk = Boolean(verified);
    const userOk = Boolean(userConfirmed);
    const identityOk = verifiedOk || userOk;
    let resolvedConfirmation = confirmation || IDENTITY_CONFIRMATION.NONE;
    if (userOk) {
      resolvedConfirmation = IDENTITY_CONFIRMATION.USER_CONFIRMED;
    } else if (verifiedOk) {
      resolvedConfirmation = IDENTITY_CONFIRMATION.AUTO_MATCHED;
    } else if (blocking) {
      resolvedConfirmation = IDENTITY_CONFIRMATION.BLOCKED;
    } else if (reviewRequired) {
      resolvedConfirmation = IDENTITY_CONFIRMATION.UNVERIFIED;
    }
    return {
      code: status,
      status,
      matched: Boolean(matched),
      /** Yalnız otomatik, geçerli belge↔firma eşleşmesi */
      verified: verifiedOk,
      identityVerified: verifiedOk,
      /** Yalnız kimlik taşımayan Excel için kullanıcı onayı */
      userConfirmed: userOk,
      identityUserConfirmed: userOk,
      blocking: Boolean(blocking),
      reviewRequired: Boolean(reviewRequired),
      allowAnalyze: Boolean(allowAnalyze),
      allowPersist: Boolean(allowPersist) && identityOk && !blocking,
      allowExport: Boolean(allowExport) && identityOk && !blocking,
      confirmation: resolvedConfirmation,
      confirmedScope: null,
      safeMessage: SAFE_MESSAGES[status] || "Kimlik do\u011Frulamas\u0131 tamamlanamad\u0131.",
      safeFingerprint: fingerprintTaxIdentity(documentTaxId || companyTaxId || "", companyId),
      companyIdentityType: companyType,
      documentIdentityType: documentType,
      companyMask: maskTaxIdSafe(companyTaxId),
      documentMask: maskTaxIdSafe(documentTaxId),
      sourceKind,
      skipped: false
    };
  }
  function evaluateEDefterCompanyIdentity(input = {}) {
    const companyId = String(input.companyId || "").slice(0, 80);
    const sourceKind = String(input.sourceKind || "xml").toLowerCase();
    const expectsDocumentIdentity = sourceKind === "xml" || sourceKind === "zip";
    const companyRaw = normalizeIdentityDigits(input.companyTaxId);
    const multi = Array.isArray(input.documentTaxIds) ? [...new Set(input.documentTaxIds.map((x) => normalizeIdentityDigits(x)).filter(Boolean))] : [];
    const documentRaw = multi.length === 1 ? multi[0] : multi.length > 1 ? "" : normalizeIdentityDigits(input.documentTaxId);
    const companyType = classifyTaxIdentityType(companyRaw);
    const documentType = classifyTaxIdentityType(documentRaw);
    if (multi.length > 1) {
      return decision({
        status: EDEFTER_IDENTITY_STATUS.IDENTITY_AMBIGUOUS,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType: EDEFTER_IDENTITY_TYPE.UNKNOWN,
        companyId,
        companyTaxId: companyRaw,
        sourceKind
      });
    }
    if (companyRaw && !isValidTaxIdentityDigits(companyRaw)) {
      return decision({
        status: EDEFTER_IDENTITY_STATUS.IDENTITY_INVALID,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        documentTaxId: documentRaw,
        sourceKind
      });
    }
    if (documentRaw && !isValidTaxIdentityDigits(documentRaw)) {
      return decision({
        status: EDEFTER_IDENTITY_STATUS.IDENTITY_INVALID,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        documentTaxId: documentRaw,
        sourceKind
      });
    }
    if (!companyRaw && documentRaw) {
      return decision({
        status: EDEFTER_IDENTITY_STATUS.COMPANY_IDENTITY_MISSING,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        documentTaxId: documentRaw,
        sourceKind
      });
    }
    if (companyRaw && !documentRaw) {
      if (!expectsDocumentIdentity) {
        return decision({
          status: EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING_REVIEW,
          matched: false,
          verified: false,
          blocking: false,
          reviewRequired: true,
          allowAnalyze: true,
          allowPersist: false,
          allowExport: false,
          companyType,
          documentType,
          companyId,
          companyTaxId: companyRaw,
          sourceKind
        });
      }
      return decision({
        status: EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        sourceKind
      });
    }
    if (!companyRaw && !documentRaw) {
      if (!expectsDocumentIdentity) {
        return decision({
          status: EDEFTER_IDENTITY_STATUS.NOT_APPLICABLE,
          matched: false,
          verified: false,
          blocking: false,
          reviewRequired: true,
          allowAnalyze: true,
          allowPersist: false,
          allowExport: false,
          companyType,
          documentType,
          companyId,
          companyTaxId: companyRaw,
          sourceKind
        });
      }
      return decision({
        status: EDEFTER_IDENTITY_STATUS.COMPANY_IDENTITY_MISSING,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        sourceKind
      });
    }
    if (companyType !== documentType && companyType !== EDEFTER_IDENTITY_TYPE.EMPTY && documentType !== EDEFTER_IDENTITY_TYPE.EMPTY && companyType !== EDEFTER_IDENTITY_TYPE.UNKNOWN && documentType !== EDEFTER_IDENTITY_TYPE.UNKNOWN) {
      return decision({
        status: EDEFTER_IDENTITY_STATUS.IDENTITY_TYPE_CONFLICT,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        documentTaxId: documentRaw,
        sourceKind
      });
    }
    if (companyRaw !== documentRaw) {
      return decision({
        status: EDEFTER_IDENTITY_STATUS.MISMATCH,
        blocking: true,
        allowAnalyze: false,
        companyType,
        documentType,
        companyId,
        companyTaxId: companyRaw,
        documentTaxId: documentRaw,
        sourceKind
      });
    }
    return decision({
      status: EDEFTER_IDENTITY_STATUS.MATCHED,
      matched: true,
      verified: true,
      blocking: false,
      allowAnalyze: true,
      allowPersist: true,
      allowExport: true,
      companyType,
      documentType,
      companyId,
      companyTaxId: companyRaw,
      documentTaxId: documentRaw,
      sourceKind
    });
  }
  function identityStatusToErrorCode(status) {
    switch (status) {
      case EDEFTER_IDENTITY_STATUS.MISMATCH:
        return "COMPANY_MISMATCH";
      case EDEFTER_IDENTITY_STATUS.IDENTITY_AMBIGUOUS:
        return "MIXED_COMPANY_OR_PERIOD";
      case EDEFTER_IDENTITY_STATUS.COMPANY_IDENTITY_MISSING:
        return "COMPANY_IDENTITY_MISSING";
      case EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING:
      case EDEFTER_IDENTITY_STATUS.DOCUMENT_IDENTITY_MISSING_REVIEW:
        return "DOCUMENT_IDENTITY_MISSING";
      case EDEFTER_IDENTITY_STATUS.IDENTITY_INVALID:
        return "IDENTITY_INVALID";
      case EDEFTER_IDENTITY_STATUS.IDENTITY_TYPE_CONFLICT:
        return "IDENTITY_TYPE_CONFLICT";
      default:
        return status || "IDENTITY_GATE";
    }
  }
  function applyIdentityGateToSummary(summary = {}, identity = null) {
    const next = { ...summary || {} };
    if (!identity) return next;
    next.identityStatus = identity.status;
    next.identityVerified = Boolean(identity.verified || identity.identityVerified);
    next.identityUserConfirmed = Boolean(
      identity.userConfirmed || identity.identityUserConfirmed
    );
    next.identityMatched = Boolean(identity.matched);
    next.identityReviewRequired = Boolean(identity.reviewRequired);
    next.identityConfirmation = identity.confirmation || (next.identityUserConfirmed ? IDENTITY_CONFIRMATION.USER_CONFIRMED : next.identityVerified ? IDENTITY_CONFIRMATION.AUTO_MATCHED : identity.blocking ? IDENTITY_CONFIRMATION.BLOCKED : identity.reviewRequired ? IDENTITY_CONFIRMATION.UNVERIFIED : "");
    next.identityFingerprint = identity.safeFingerprint || "";
    next.identityMessage = identity.safeMessage || "";
    next.identityAllowAnalyze = Boolean(identity.allowAnalyze);
    next.identityAllowPersist = Boolean(identity.allowPersist);
    next.identityAllowExport = Boolean(identity.allowExport);
    if (identity.blocking) {
      next.edefterUygun = false;
      next.canApproveExport = false;
    }
    if (!identity.allowExport) {
      next.canApproveExport = false;
    }
    return next;
  }

  // src/utils/eDefterSecurity.js
  var EDEFTER_MAX_BYTES = 40 * 1024 * 1024;
  var EDEFTER_MAX_UNCOMPRESSED_BYTES = 120 * 1024 * 1024;
  var EDEFTER_ERROR_CODE = Object.freeze({
    COMPANY_MISMATCH: "COMPANY_MISMATCH",
    MIXED_COMPANY_OR_PERIOD: "MIXED_COMPANY_OR_PERIOD",
    JOURNAL_LEDGER_MISMATCH: "JOURNAL_LEDGER_MISMATCH",
    EXTERNAL_VERIFICATION_REQUIRED: "EXTERNAL_VERIFICATION_REQUIRED",
    COMPANY_IDENTITY_MISSING: "COMPANY_IDENTITY_MISSING",
    DOCUMENT_IDENTITY_MISSING: "DOCUMENT_IDENTITY_MISSING",
    IDENTITY_INVALID: "IDENTITY_INVALID",
    IDENTITY_AMBIGUOUS: "IDENTITY_AMBIGUOUS",
    IDENTITY_TYPE_CONFLICT: "IDENTITY_TYPE_CONFLICT",
    XML_BOZUK: "XML_BOZUK",
    XXE_REJECTED: "XXE_REJECTED",
    ZIP_BOMB: "ZIP_BOMB",
    ZIP_SLIP: "ZIP_SLIP",
    TOO_LARGE: "TOO_LARGE",
    TOO_MANY_ENTRIES: "TOO_MANY_ENTRIES",
    TIMEOUT: "TIMEOUT",
    CANCELLED: "CANCELLED",
    DUPLICATE_FILE: "DUPLICATE_FILE",
    UNSUPPORTED: "UNSUPPORTED",
    ENCRYPTED: "ENCRYPTED"
  });
  function extractPeriodFromText(text = "") {
    const t = String(text || "");
    const iso = t.match(/(20\d{2})[-./](0[1-9]|1[0-2])/);
    if (iso) return `${iso[1]}-${iso[2]}`;
    const tr = t.match(/(0[1-9]|1[0-2])[-./](20\d{2})/);
    if (tr) return `${tr[2]}-${tr[1]}`;
    const yyyyMm = t.match(/\b(20\d{2})(0[1-9]|1[0-2])\b/);
    if (yyyyMm) return `${yyyyMm[1]}-${yyyyMm[2]}`;
    return "";
  }
  function normalizePeriodKey(period = "") {
    const raw = String(period || "").trim();
    if (!raw) return "";
    const fromText = extractPeriodFromText(raw);
    if (fromText) return fromText;
    const parts = raw.replace(/\//g, "-").split("-");
    if (parts.length >= 2) {
      const y = parts[0].length === 4 ? parts[0] : parts[1];
      const m = parts[0].length === 4 ? parts[1] : parts[0];
      if (/^20\d{2}$/.test(y) && /^(0?[1-9]|1[0-2])$/.test(m)) {
        return `${y}-${String(m).padStart(2, "0")}`;
      }
    }
    return raw;
  }
  function normalizeTaxId(value = "") {
    return String(value || "").replace(/\D/g, "");
  }

  // src/utils/eDefterKontrolEngine.js
  function compactText3(value) {
    return normalizeParserText(value).replace(/\s+/g, "");
  }
  function roundMoney2(value) {
    return Number(Number(value || 0).toFixed(2));
  }
  function findHeaderIndex(rows, matchers) {
    return rows.findIndex((row) => {
      const text = row.map((cell) => normalizeParserText(cell)).join(" ");
      return matchers.every((matcher) => matcher(text));
    });
  }
  function getSheetCell(row, headers, names) {
    const list = Array.isArray(names) ? names : [names];
    for (const name of list) {
      const wanted = compactText3(name);
      const index = headers.findIndex((header) => {
        const current = compactText3(header);
        return current === wanted || current.includes(wanted);
      });
      if (index >= 0) return row[index];
    }
    return "";
  }
  function parseLedgerRow(row, headers, index, kaynak) {
    const tarih = getSheetCell(row, headers, ["TAR\u0130H", "TARIH", "F\u0130\u015E TAR\u0130H\u0130", "FIS TARIHI", "KAYIT TARIHI"]) || "";
    const fisNo = String(
      getSheetCell(row, headers, ["F\u0130\u015E NO", "FIS NO", "FISNO", "YEVMIYE FIS NO"]) || ""
    ).trim();
    const yevmiyeNo = String(
      getSheetCell(row, headers, ["YEVM\u0130YE NO", "YEVMIYE NO", "YEVMIYENO", "YEVMIYE"]) || ""
    ).trim();
    const hesapKodu = String(
      getSheetCell(row, headers, ["HESAP KODU", "HESAP KOD", "HESAP NO", "KOD"]) || ""
    ).trim();
    const hesapAdi = String(
      getSheetCell(row, headers, ["HESAP ADI", "HESAP AD", "HESAP ADI/UNVAN", "HESAP"]) || ""
    ).trim();
    const aciklama = String(
      getSheetCell(row, headers, ["A\xC7IKLAMA", "ACIKLAMA", "DETAY", "NOT"]) || ""
    ).trim();
    const belgeTuru = String(
      getSheetCell(row, headers, ["BELGE T\xDCR\xDC", "BELGE TURU", "EVRAK TURU", "EVRAK T\u0130P\u0130"]) || ""
    ).trim();
    const belgeNo = String(
      getSheetCell(row, headers, ["BELGE NO", "EVRAK NO", "FATURA NO"]) || ""
    ).trim();
    const belgeTarihi = getSheetCell(row, headers, ["BELGE TAR\u0130H\u0130", "BELGE TARIHI", "EVRAK TARIHI"]) || "";
    const borc = parseMoneyTR(getSheetCell(row, headers, ["BOR\xC7", "BORC"]));
    const alacak = parseMoneyTR(getSheetCell(row, headers, ["ALACAK"]));
    const cariUnvan = String(
      getSheetCell(row, headers, [
        "CAR\u0130 UNVAN",
        "CARI UNVAN",
        "CAR\u0130 ADI",
        "CARI ADI",
        "UNVAN",
        "M\xDC\u015ETER\u0130",
        "MUSTERI",
        "SATICI",
        "PAYEE",
        "PARTY"
      ]) || ""
    ).trim();
    const counterAccountCode = String(
      getSheetCell(row, headers, [
        "KAR\u015EI HESAP",
        "KARSI HESAP",
        "KAR\u015EI HESAP KODU",
        "KARSI HESAP KODU",
        "KARS\u0130 HESAP",
        "COUNTER ACCOUNT",
        "COUNTERACCOUNT"
      ]) || ""
    ).trim();
    if (!tarih && !fisNo && !hesapKodu && !borc && !alacak) return null;
    return {
      id: `${kaynak}-${index + 1}`,
      kaynak,
      tarih: formatDateTR(tarih),
      fisNo,
      yevmiyeNo,
      hesapKodu,
      hesapAdi,
      aciklama,
      belgeTuru,
      belgeNo,
      belgeTarihi: belgeTarihi ? formatDateTR(belgeTarihi) : "",
      borc,
      alacak,
      cariUnvan,
      counterAccountCode,
      karsiHesapKodu: counterAccountCode,
      tutar: roundMoney2(Math.max(borc, alacak)),
      kontrolDurumu: "",
      not: "",
      duzeltildiMi: false,
      disaridaBirak: false,
      manuallyEdited: false
    };
  }
  function parseLedgerSheet(sheetRows = [], kaynak = E_DEFTER_KAYNAK.MUAVIN) {
    if (!sheetRows.length) return [];
    const headerIndex = findHeaderIndex(sheetRows, [
      (text) => text.includes("HESAP") || text.includes("BORC") || text.includes("BOR\xC7"),
      (text) => text.includes("TARIH") || text.includes("TAR\u0130H") || text.includes("FIS")
    ]);
    const headers = headerIndex >= 0 ? sheetRows[headerIndex] : sheetRows[0];
    const dataRows = sheetRows.slice((headerIndex >= 0 ? headerIndex : 0) + 1);
    return dataRows.filter((row) => row && row.some((cell) => String(cell || "").trim())).map((row, index) => parseLedgerRow(row, headers, index, kaynak)).filter(Boolean);
  }
  var LUCA_ACCOUNT_SEGMENT_RE = "[\\p{L}\\p{N}]";
  var LUCA_ACCOUNT_CODE_RE = new RegExp(
    `^(\\d{3}(?:[./]${LUCA_ACCOUNT_SEGMENT_RE}{1,8})*)\\s+(.+)$`,
    "u"
  );
  function preserveFisNo(value) {
    if (value === null || value === void 0 || value === "") return "";
    if (typeof value === "string") return value.trim();
    return String(value);
  }
  function isLucaMuavinSkipLabel(rowText = "") {
    const t = compactText3(rowText);
    if (!t) return true;
    if (t.includes("NAKLIYEKUNHARIC")) return true;
    if (t.includes("GENELTOPLAM")) return true;
    return false;
  }
  function isLucaMuavinColumnHeaderRow(row = []) {
    const cells = (row || []).map((cell) => normalizeParserText(cell));
    const hasTarih = cells.some((c) => c === "TARIH" || c.includes("TARIH"));
    const hasTip = cells.some((c) => c === "TIP" || c.includes("TIP"));
    const hasFis = cells.some((c) => c.includes("FISNO") || c.includes("FIS NO"));
    const hasBorc = cells.some((c) => c.includes("BORC"));
    const hasAlacak = cells.some((c) => c.includes("ALACAK"));
    const hasHesapCol = cells.some((c) => c.includes("HESAPKODU") || c.includes("HESAP KODU"));
    return hasTarih && hasTip && hasFis && hasBorc && hasAlacak && !hasHesapCol;
  }
  function buildLucaColumnMap(headerRow = []) {
    const headers = (headerRow || []).map((cell) => compactText3(normalizeParserText(cell)));
    const findIndex = (matchers) => headers.findIndex(
      (header) => matchers.some((matcher) => {
        const wanted = compactText3(matcher);
        return header === wanted || header.includes(wanted);
      })
    );
    return {
      tarih: findIndex(["TAR\u0130H", "TARIH"]),
      tip: findIndex(["T\u0130P", "TIP"]),
      fisNo: findIndex(["F\u0130\u015E NO", "FIS NO", "FISNO"]),
      aciklama: findIndex(["A\xC7IKLAMA", "ACIKLAMA"]),
      borc: findIndex(["BOR\xC7", "BORC"]),
      alacak: findIndex(["ALACAK"]),
      bakiye: findIndex(["BAK\u0130YE", "BAKIYE"]),
      ba: findIndex(["B/A"])
    };
  }
  function lucaCell(row, index) {
    if (index < 0 || !Array.isArray(row)) return "";
    return row[index] ?? "";
  }
  function parseLucaAccountHeaderCell(cell) {
    const text = String(cell ?? "").trim();
    if (!text) return null;
    if (parseDateTR(text)) return null;
    const match = text.match(LUCA_ACCOUNT_CODE_RE);
    if (!match) return null;
    const hesapKodu = match[1].trim();
    const hesapAdi = match[2].trim();
    if (!hesapAdi) return null;
    const adiCompact = compactText3(hesapAdi);
    if (adiCompact.includes("TARIH") && adiCompact.includes("FIS")) return null;
    return { hesapKodu, hesapAdi };
  }
  function detectLucaMultiAccountMuavinLayout(sheetRows = []) {
    if (!Array.isArray(sheetRows) || sheetRows.length < 4) return false;
    let accountHeaders = 0;
    let columnHeaders = 0;
    for (const row of sheetRows) {
      if (!Array.isArray(row) || !row.some((cell) => String(cell ?? "").trim())) continue;
      if (parseLucaAccountHeaderCell(row[0])) accountHeaders += 1;
      if (isLucaMuavinColumnHeaderRow(row)) columnHeaders += 1;
    }
    return accountHeaders >= 1 && columnHeaders >= 1;
  }
  function parseLucaMultiAccountMuavinSheet(sheetRows = []) {
    const rows = [];
    let activeAccount = { hesapKodu: "", hesapAdi: "" };
    let colMap = null;
    let movementIndex = 0;
    for (const row of sheetRows) {
      if (!Array.isArray(row) || !row.some((cell) => String(cell ?? "").trim())) continue;
      const rowText = row.map((cell) => String(cell ?? "")).join(" ");
      if (isLucaMuavinSkipLabel(rowText)) continue;
      const accountHeader = parseLucaAccountHeaderCell(row[0]);
      if (accountHeader) {
        const restHasDate = row.slice(1).some((cell) => parseDateTR(cell));
        if (!restHasDate) {
          activeAccount = accountHeader;
          continue;
        }
      }
      if (isLucaMuavinColumnHeaderRow(row)) {
        colMap = buildLucaColumnMap(row);
        continue;
      }
      if (!colMap || !activeAccount.hesapKodu) continue;
      const tarihRaw = lucaCell(row, colMap.tarih);
      if (!parseDateTR(tarihRaw)) continue;
      const borc = parseMoneyTR(lucaCell(row, colMap.borc));
      const alacak = parseMoneyTR(lucaCell(row, colMap.alacak));
      if (!borc && !alacak) continue;
      movementIndex += 1;
      const aciklama = String(lucaCell(row, colMap.aciklama) ?? "").trim();
      const tip = String(lucaCell(row, colMap.tip) ?? "").trim();
      rows.push({
        id: `${E_DEFTER_KAYNAK.MUAVIN}-luca-${movementIndex}`,
        kaynak: E_DEFTER_KAYNAK.MUAVIN,
        tarih: formatDateTR(tarihRaw),
        fisNo: preserveFisNo(lucaCell(row, colMap.fisNo)),
        yevmiyeNo: "",
        hesapKodu: activeAccount.hesapKodu,
        hesapAdi: activeAccount.hesapAdi,
        aciklama: aciklama || tip,
        belgeTuru: tip,
        belgeNo: "",
        belgeTarihi: "",
        borc,
        alacak,
        cariUnvan: "",
        counterAccountCode: "",
        karsiHesapKodu: "",
        tutar: roundMoney2(Math.max(borc, alacak)),
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false
      });
    }
    return rows;
  }
  function parseMuavinSheet(sheetRows = []) {
    if (!sheetRows?.length) return [];
    if (detectLucaMultiAccountMuavinLayout(sheetRows)) {
      const lucaRows = parseLucaMultiAccountMuavinSheet(sheetRows);
      if (!lucaRows.length) {
        throw Object.assign(new Error("Luca muavin blo\u011Fu ayr\u0131\u015Ft\u0131r\u0131lamad\u0131."), {
          code: "UNSUPPORTED_MUAVIN_LAYOUT"
        });
      }
      return lucaRows;
    }
    return parseLedgerSheet(sheetRows, E_DEFTER_KAYNAK.MUAVIN);
  }
  var YEVMIYE_LAYOUT = {
    STANDARD_COLUMN: "STANDARD_COLUMN",
    REPEATED_JOURNAL_BLOCK: "REPEATED_JOURNAL_BLOCK",
    UNKNOWN: "UNKNOWN"
  };
  var YEVMIYE_BLOCK_HEADER_RE = /^(\d+)\s*-{3,}\s*(\d+)\s*-{3,}\s*(.+?)\s*-{3,}\s*(.+)$/;
  function formatLucaBlockHeaderDateTR(value) {
    const text = String(value ?? "").trim();
    const slash = text.match(/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/);
    if (slash) {
      const day = Number(slash[1]);
      const month = Number(slash[2]);
      const year = Number(slash[3]);
      if (day >= 1 && day <= 31 && month >= 1 && month <= 12 && year >= 1900) {
        return `${String(day).padStart(2, "0")}.${String(month).padStart(2, "0")}.${year}`;
      }
    }
    return formatDateTR(text);
  }
  function parseYevmiyeBlockHeaderCell(value) {
    const text = String(value ?? "").trim();
    if (!text || !text.includes("---")) return null;
    const match = text.match(YEVMIYE_BLOCK_HEADER_RE);
    if (!match) return null;
    const tarihRaw = match[4].trim();
    const tarih = formatLucaBlockHeaderDateTR(tarihRaw);
    if (!tarih || !parseDateTR(tarih)) return null;
    return {
      fisNo: preserveFisNo(match[1]),
      yevmiyeNo: preserveFisNo(match[2]),
      fisTuru: String(match[3] || "").trim(),
      tarihRaw,
      tarih
    };
  }
  function isYevmiyeBlockColumnHeaderRow(row = []) {
    const corpus = (row || []).map((cell) => compactText3(cell)).join(" ");
    return corpus.includes("HESAPKODU") && corpus.includes("HESAPADI") && (corpus.includes("BORC") || corpus.includes("ALACAK"));
  }
  function isYevmiyeBlockSkipRow(codeCell = "") {
    const text = String(codeCell || "").trim();
    if (!text) return true;
    const compact2 = compactText3(text);
    return compact2.startsWith("TOPLAM") || compact2.startsWith("FISACIKLAMA") || compact2 === "YEVMIYEDEFTERI" || compact2.startsWith("DONEM") || compact2.startsWith("TARIHARALIGI");
  }
  function detectYevmiyeLayout(sheetRows = []) {
    if (!Array.isArray(sheetRows) || sheetRows.length < 3) return YEVMIYE_LAYOUT.UNKNOWN;
    let blockHeaders = 0;
    let columnHeaders = 0;
    let standardDateHits = 0;
    let standardFisHits = 0;
    for (const row of sheetRows.slice(0, 400)) {
      if (!Array.isArray(row) || !row.some((cell) => String(cell ?? "").trim())) continue;
      if (parseYevmiyeBlockHeaderCell(row[0])) blockHeaders += 1;
      if (isYevmiyeBlockColumnHeaderRow(row)) columnHeaders += 1;
      const corpus = (row || []).map((cell) => compactText3(cell)).join(" ");
      if (corpus.includes("TARIH") && (corpus.includes("FISNO") || corpus.includes("YEVMIYENO"))) {
        standardFisHits += 1;
      }
      if (row.some((cell) => parseDateTR(cell)) && row.length >= 5) {
        standardDateHits += 1;
      }
    }
    if (blockHeaders >= 1 && columnHeaders >= 1) {
      return YEVMIYE_LAYOUT.REPEATED_JOURNAL_BLOCK;
    }
    if (standardFisHits >= 1 && standardDateHits >= 2 && blockHeaders === 0) {
      return YEVMIYE_LAYOUT.STANDARD_COLUMN;
    }
    return YEVMIYE_LAYOUT.UNKNOWN;
  }
  function parseLucaRepeatedJournalBlockYevmiyeSheet(sheetRows = []) {
    const rows = [];
    let block = null;
    let side = null;
    let movementIndex = 0;
    for (const row of sheetRows) {
      if (!Array.isArray(row) || !row.some((cell) => String(cell ?? "").trim())) continue;
      const header = parseYevmiyeBlockHeaderCell(row[0]);
      if (header) {
        block = {
          fisNo: header.fisNo,
          yevmiyeNo: header.yevmiyeNo,
          fisTuru: header.fisTuru,
          tarih: header.tarih || formatLucaBlockHeaderDateTR(header.tarihRaw)
        };
        side = null;
        continue;
      }
      if (!block) continue;
      if (isYevmiyeBlockColumnHeaderRow(row)) continue;
      const codeCell = String(row[0] ?? "");
      if (isYevmiyeBlockSkipRow(codeCell)) continue;
      const leading = (codeCell.match(/^(\s*)/) || ["", ""])[1].length;
      const hesapKodu = codeCell.trim();
      if (!hesapKodu) continue;
      const detayAmt = parseMoneyTR(row[3]);
      const borcCol = parseMoneyTR(row[4]);
      const alacakCol = parseMoneyTR(row[5]);
      if (leading === 0) {
        if (borcCol > 0 && alacakCol <= 0) side = "B";
        else if (alacakCol > 0 && borcCol <= 0) side = "A";
        continue;
      }
      let borc = 0;
      let alacak = 0;
      if (detayAmt > 0) {
        if (side === "B") borc = detayAmt;
        else if (side === "A") alacak = detayAmt;
        else continue;
      } else if (borcCol > 0 && alacakCol <= 0) {
        borc = borcCol;
      } else if (alacakCol > 0 && borcCol <= 0) {
        alacak = alacakCol;
      } else {
        continue;
      }
      movementIndex += 1;
      const hesapAdi = String(row[1] ?? "").trim();
      const aciklama = String(row[2] ?? "").trim();
      rows.push({
        id: `${E_DEFTER_KAYNAK.YEVMIYE}-luca-block-${movementIndex}`,
        kaynak: E_DEFTER_KAYNAK.YEVMIYE,
        documentClass: "YEVMIYE",
        tarih: block.tarih,
        fisNo: block.fisNo,
        yevmiyeNo: block.yevmiyeNo,
        fisTuru: block.fisTuru,
        hesapKodu,
        hesapAdi,
        aciklama,
        belgeTuru: block.fisTuru,
        belgeNo: "",
        belgeTarihi: "",
        borc: roundMoney2(borc),
        alacak: roundMoney2(alacak),
        cariUnvan: "",
        counterAccountCode: "",
        karsiHesapKodu: "",
        tutar: roundMoney2(Math.max(borc, alacak)),
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false
      });
    }
    return rows;
  }
  function parseYevmiyeSheet(sheetRows = []) {
    if (!sheetRows?.length) return [];
    const layout = detectYevmiyeLayout(sheetRows);
    if (layout === YEVMIYE_LAYOUT.REPEATED_JOURNAL_BLOCK) {
      return parseLucaRepeatedJournalBlockYevmiyeSheet(sheetRows);
    }
    return parseLedgerSheet(sheetRows, E_DEFTER_KAYNAK.YEVMIYE);
  }
  function parseMizanSheetRaw(sheetRows = []) {
    if (!sheetRows.length) return [];
    const headerIndex = findHeaderIndex(sheetRows, [
      (text) => text.includes("HESAP"),
      (text) => text.includes("BORC") || text.includes("BOR\xC7") || text.includes("BAKIYE")
    ]);
    const headers = headerIndex >= 0 ? sheetRows[headerIndex] : sheetRows[0];
    const dataRows = sheetRows.slice((headerIndex >= 0 ? headerIndex : 0) + 1);
    return dataRows.filter((row) => row && row.some((cell) => String(cell || "").trim())).map((row, index) => {
      const hesapKodu = String(
        getSheetCell(row, headers, ["HESAP KODU", "HESAP KOD", "KOD"]) || ""
      ).trim();
      const hesapAdi = String(
        getSheetCell(row, headers, ["HESAP ADI", "HESAP AD", "HESAP"]) || ""
      ).trim();
      const borc = parseMoneyTR(
        getSheetCell(row, headers, ["BOR\xC7", "BORC", "BOR\xC7 TOPLAMI", "BORC TOPLAMI"])
      );
      const alacak = parseMoneyTR(
        getSheetCell(row, headers, ["ALACAK", "ALACAK TOPLAMI"])
      );
      const bakiye = parseMoneyTR(
        getSheetCell(row, headers, ["BAK\u0130YE", "BAKIYE", "NET BAKIYE"])
      );
      if (!hesapKodu && !borc && !alacak && !bakiye) return null;
      return {
        id: `mizan-${index + 1}`,
        kaynak: E_DEFTER_KAYNAK.MIZAN,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu,
        hesapAdi,
        aciklama: hesapAdi,
        belgeTuru: "",
        belgeNo: "",
        belgeTarihi: "",
        borc,
        alacak,
        cariUnvan: "",
        tutar: roundMoney2(Math.max(borc, alacak, Math.abs(bakiye))),
        mizanBakiye: bakiye,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false
      };
    }).filter(Boolean);
  }
  function parseMizanSheetWithStructure(sheetRows = []) {
    return structureMizanParseResult(parseMizanSheetRaw(sheetRows));
  }
  function parseMizanSheet(sheetRows = []) {
    return parseMizanSheetWithStructure(sheetRows).rows;
  }
  function daysBetween(left, right) {
    const leftDate = parseDateTR(left);
    const rightDate = parseDateTR(right);
    if (!leftDate || !rightDate) return 999;
    return Math.abs(Math.round((leftDate.getTime() - rightDate.getTime()) / 864e5));
  }
  function extractNumeric(value) {
    const match = String(value || "").match(/\d+/);
    return match ? Number(match[0]) : NaN;
  }
  function accountPrefix(hesapKodu) {
    return String(hesapKodu || "").replace(/\./g, "").slice(0, 3);
  }
  function buildAccountBalanceMap(rows = []) {
    const map = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (!row.hesapKodu || row.kaynak === E_DEFTER_KAYNAK.MIZAN) continue;
      const key = compactText3(row.hesapKodu);
      const current = map.get(key) || { borc: 0, alacak: 0, hesapKodu: row.hesapKodu };
      current.borc += roundMoney2(row.borc);
      current.alacak += roundMoney2(row.alacak);
      map.set(key, current);
    }
    const result = /* @__PURE__ */ new Map();
    for (const [key, item] of map.entries()) {
      result.set(key, {
        ...item,
        net: roundMoney2(item.borc - item.alacak)
      });
    }
    return result;
  }
  function resolveVoucherCounterparts(rows = []) {
    const byFis = /* @__PURE__ */ new Map();
    const results = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (!row || row.kaynak === E_DEFTER_KAYNAK.MIZAN) continue;
      if (row.kaynak === E_DEFTER_KAYNAK.TEKNIK || row.kaynak === E_DEFTER_KAYNAK.VERGISEL || row.kaynak === E_DEFTER_KAYNAK.CAPRAZ) {
        continue;
      }
      const fisKey = compactText3(row.fisNo);
      if (!fisKey) {
        results.set(row.id, {
          status: "REVIEW",
          code: E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW,
          counterAccountCode: "",
          candidates: [],
          confidence: 0,
          reason: "fis_missing"
        });
        continue;
      }
      const periodKey = compactText3(row.period || row.donem || "");
      const groupKey = `${fisKey}|${compactText3(row.kaynak) || "ledger"}|${periodKey}`;
      const list = byFis.get(groupKey) || [];
      list.push(row);
      byFis.set(groupKey, list);
    }
    for (const [, group] of byFis.entries()) {
      if (isOpeningVoucherGroup(group)) {
        for (const row of group) {
          results.set(row.id, {
            status: "SKIP",
            code: "",
            counterAccountCode: "",
            candidates: [],
            confidence: 0,
            reason: "opening_voucher",
            classification: OPENING_VOUCHER_CLASS
          });
        }
        continue;
      }
      const sides = /* @__PURE__ */ new Map();
      let totalBorc = 0;
      let totalAlacak = 0;
      const debitCodes = /* @__PURE__ */ new Set();
      const creditCodes = /* @__PURE__ */ new Set();
      let activeCount = 0;
      for (const row of group) {
        const borc = roundMoney2(row.borc);
        const alacak = roundMoney2(row.alacak);
        const zeroLeg = Math.abs(borc) <= BORC_ALACAK_TOLERANCE && Math.abs(alacak) <= BORC_ALACAK_TOLERANCE;
        if (zeroLeg) {
          sides.set(row.id, { zero: true, isDebit: false, isCredit: false, code: "" });
          continue;
        }
        const isDebit = borc > BORC_ALACAK_TOLERANCE && alacak <= BORC_ALACAK_TOLERANCE;
        const isCredit = alacak > BORC_ALACAK_TOLERANCE && borc <= BORC_ALACAK_TOLERANCE;
        const code = String(row.hesapKodu || "").trim();
        sides.set(row.id, { zero: false, isDebit, isCredit, code });
        activeCount += 1;
        totalBorc += borc;
        totalAlacak += alacak;
        if (isDebit && code) debitCodes.add(code);
        if (isCredit && code) creditCodes.add(code);
      }
      const balanced = Math.abs(roundMoney2(totalBorc - totalAlacak)) <= BORC_ALACAK_TOLERANCE;
      for (const row of group) {
        const side = sides.get(row.id);
        if (!side || side.zero) {
          results.set(row.id, {
            status: "SKIP",
            code: "",
            counterAccountCode: "",
            candidates: [],
            confidence: 0,
            reason: "zero_amount"
          });
          continue;
        }
        const { isDebit, isCredit } = side;
        const selfCode = compactText3(row.hesapKodu);
        const explicitRaw = String(row.counterAccountCode || row.karsiHesapKodu || "").trim();
        const explicit = compactText3(explicitRaw);
        const oppositeCodes = [
          ...isDebit ? creditCodes : isCredit ? debitCodes : []
        ].filter((code) => compactText3(code) !== selfCode);
        const uniqueCodes = [...new Set(oppositeCodes)];
        if (explicit) {
          if (explicit === selfCode) {
            results.set(row.id, {
              status: "ISSUE",
              code: E_DEFTER_ISSUE_CODE.COUNTERPART_SELF,
              counterAccountCode: "",
              candidates: [explicitRaw],
              confidence: 0,
              reason: "explicit_self"
            });
            continue;
          }
          if (uniqueCodes.length === 1 && compactText3(uniqueCodes[0]) !== explicit) {
            results.set(row.id, {
              status: "ISSUE",
              code: E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT,
              counterAccountCode: "",
              candidates: [explicitRaw, uniqueCodes[0]],
              confidence: 0,
              reason: "explicit_vs_computed",
              balanced
            });
            continue;
          }
          if (uniqueCodes.length === 1 && compactText3(uniqueCodes[0]) === explicit) {
            results.set(row.id, {
              status: "RESOLVED",
              code: E_DEFTER_ISSUE_CODE.COUNTERPART_VERIFIED,
              counterAccountCode: explicitRaw,
              candidates: [explicitRaw],
              confidence: 1,
              reason: "explicit_verified",
              balanced
            });
            continue;
          }
          results.set(row.id, {
            status: "RESOLVED",
            code: "",
            counterAccountCode: explicitRaw,
            candidates: [explicitRaw],
            confidence: uniqueCodes.length ? 0.7 : 0.85,
            reason: uniqueCodes.length > 1 ? "explicit_with_multi_legs" : "explicit",
            balanced
          });
          continue;
        }
        if (!isDebit && !isCredit) {
          results.set(row.id, {
            status: "REVIEW",
            code: E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW,
            counterAccountCode: "",
            candidates: [],
            confidence: 0,
            reason: "mixed_side_row"
          });
          continue;
        }
        if (!uniqueCodes.length) {
          let hasSameSidePeer = false;
          for (const other of group) {
            if (other.id === row.id) continue;
            const o = sides.get(other.id);
            if (!o || o.zero) continue;
            if (isDebit && o.isDebit || isCredit && o.isCredit) {
              hasSameSidePeer = true;
              break;
            }
          }
          results.set(row.id, {
            status: "ISSUE",
            code: hasSameSidePeer ? E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE : E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART,
            counterAccountCode: "",
            candidates: [],
            confidence: 0,
            reason: hasSameSidePeer ? "same_side_only" : "no_opposite",
            balanced
          });
          continue;
        }
        if (uniqueCodes.length === 1) {
          results.set(row.id, {
            status: "RESOLVED",
            code: "",
            counterAccountCode: uniqueCodes[0],
            candidates: uniqueCodes,
            confidence: balanced ? 0.95 : 0.6,
            reason: activeCount === 2 ? "two_line_pair" : "single_opposite_code",
            balanced
          });
          continue;
        }
        results.set(row.id, {
          status: "MULTI",
          code: E_DEFTER_ISSUE_CODE.MULTI_COUNTERPART,
          counterAccountCode: "",
          candidates: uniqueCodes,
          confidence: 0.4,
          reason: "multi_opposite",
          balanced
        });
      }
    }
    return results;
  }
  function buildFisBalanceMap(rows = []) {
    const map = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (!row.fisNo || row.kaynak === E_DEFTER_KAYNAK.MIZAN) continue;
      const key = compactText3(row.fisNo);
      const current = map.get(key) || { borc: 0, alacak: 0, fisNo: row.fisNo, tarih: row.tarih };
      current.borc += roundMoney2(row.borc);
      current.alacak += roundMoney2(row.alacak);
      if (row.tarih) current.tarih = row.tarih;
      map.set(key, current);
    }
    const result = /* @__PURE__ */ new Map();
    for (const [key, item] of map.entries()) {
      result.set(key, {
        ...item,
        fark: roundMoney2(item.borc - item.alacak)
      });
    }
    return result;
  }
  function analyzeAccountBalanceIssues(hesapKodu, net) {
    const issues = [];
    let riskScore = 0;
    const prefix = accountPrefix(hesapKodu);
    if (prefix.startsWith("100") && Math.abs(net) > KASA_BAKIYE_ESIK) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "Kasa hesab\u0131nda ola\u011Fan d\u0131\u015F\u0131 y\xFCksek bakiye (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 10
      });
      riskScore += 10;
    }
    if (prefix.startsWith("120") && net < -BORC_ALACAK_TOLERANCE) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "120 al\u0131c\u0131 hesab\u0131nda d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 12
      });
      riskScore += 12;
    }
    if (prefix.startsWith("320") && net > BORC_ALACAK_TOLERANCE) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "320 sat\u0131c\u0131 hesab\u0131nda d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 12
      });
      riskScore += 12;
    }
    if (prefix.startsWith("191") && net < -BORC_ALACAK_TOLERANCE) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "191 indirilecek KDV hesab\u0131nda d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 12
      });
      riskScore += 12;
    }
    if (prefix.startsWith("391") && net > BORC_ALACAK_TOLERANCE) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "391 hesaplanan KDV hesab\u0131nda d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 12
      });
      riskScore += 12;
    }
    if (prefix.startsWith("360") && Math.abs(net) > KASA_BAKIYE_ESIK) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "360 \xF6denecek vergi hesab\u0131nda ola\u011Fand\u0131\u015F\u0131 bakiye (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 10
      });
      riskScore += 10;
    }
    if (prefix.startsWith("361") && Math.abs(net) > KASA_BAKIYE_ESIK) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "361 SGK/bor\xE7 hesab\u0131nda ola\u011Fand\u0131\u015F\u0131 bakiye (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 10
      });
      riskScore += 10;
    }
    if (prefix.startsWith("102") && net < -BORC_ALACAK_TOLERANCE) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "102 banka hesab\u0131nda d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 10
      });
      riskScore += 10;
    }
    if (prefix.startsWith("180") && Math.abs(net) > KASA_BAKIYE_ESIK) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "180 gelecek aylara ait giderlerde ola\u011Fand\u0131\u015F\u0131 bakiye (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 8
      });
      riskScore += 8;
    }
    if (prefix.startsWith("280") && Math.abs(net) > KASA_BAKIYE_ESIK) {
      issues.push({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: "280 gelecek y\u0131llara ait giderlerde ola\u011Fand\u0131\u015F\u0131 bakiye (inceleme bilgisi).",
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        riskScore: 8
      });
      riskScore += 8;
    }
    if (prefix.startsWith("309") || prefix.startsWith("409")) {
      if (Math.abs(net) > KASA_BAKIYE_ESIK) {
        issues.push({
          code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
          message: `${prefix.slice(0, 3)} al\u0131nan/verilen \xE7eklerde ola\u011Fand\u0131\u015F\u0131 bakiye (inceleme bilgisi).`,
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 8
        });
        riskScore += 8;
      }
    }
    if (prefix.startsWith("335") || prefix.startsWith("195") || prefix.startsWith("196")) {
      if (Math.abs(net) > BORC_ALACAK_TOLERANCE) {
        issues.push({
          code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
          message: `${prefix.slice(0, 3)} personel/avans hesab\u0131nda bakiye risk g\xF6stergesi (inceleme bilgisi).`,
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 8
        });
        riskScore += 8;
      }
    }
    if (!prefix.startsWith("120") && !prefix.startsWith("320") && !prefix.startsWith("191") && !prefix.startsWith("391") && !prefix.startsWith("100") && Math.abs(net) > 0.01) {
      const hesap = String(hesapKodu || "");
      if (hesap.startsWith("1") && net < -BORC_ALACAK_TOLERANCE) {
        issues.push({
          code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
          message: "Aktif hesapta d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 8
        });
        riskScore += 8;
      }
      if (hesap.startsWith("3") && net > BORC_ALACAK_TOLERANCE) {
        issues.push({
          code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
          message: "Pasif hesapta d\xF6nem hareketi ters bakiye g\xF6r\xFCn\xFCm\xFC (inceleme bilgisi).",
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 8
        });
        riskScore += 8;
      }
    }
    return { issues, riskScore };
  }
  function buildGlobalContext(rows = []) {
    const ledgerRows = rows.filter((row) => row.kaynak !== E_DEFTER_KAYNAK.MIZAN);
    const accountBalances = buildAccountBalanceMap(ledgerRows);
    const fisBalances = buildFisBalanceMap(ledgerRows);
    const problematicAccounts = /* @__PURE__ */ new Map();
    for (const [key, balance] of accountBalances.entries()) {
      const analysis = analyzeAccountBalanceIssues(balance.hesapKodu, balance.net);
      if (analysis.issues.length) {
        problematicAccounts.set(key, analysis);
      }
    }
    const unbalancedFis = /* @__PURE__ */ new Set();
    for (const [key, balance] of fisBalances.entries()) {
      if (Math.abs(balance.fark) > BORC_ALACAK_TOLERANCE) {
        unbalancedFis.add(key);
      }
    }
    const fisDateMap = /* @__PURE__ */ new Map();
    for (const row of ledgerRows) {
      if (!row.fisNo || !row.tarih) continue;
      const key = compactText3(row.fisNo);
      if (!fisDateMap.has(key)) {
        fisDateMap.set(key, row.tarih);
      }
    }
    const fisEntries = [...fisDateMap.entries()].map(([key, tarih]) => ({
      key,
      fisNo: key,
      numeric: extractNumeric(key),
      tarih,
      dateValue: parseDateTR(tarih)?.getTime() || 0
    })).filter((item) => !Number.isNaN(item.numeric)).sort((a, b) => a.numeric - b.numeric);
    const fisNoGaps = [];
    for (let index = 1; index < fisEntries.length; index += 1) {
      const prev = fisEntries[index - 1].numeric;
      const current = fisEntries[index].numeric;
      if (current - prev > 1) {
        fisNoGaps.push({ from: prev, to: current });
      }
    }
    const outOfOrderFis = /* @__PURE__ */ new Set();
    for (let index = 1; index < fisEntries.length; index += 1) {
      if (fisEntries[index].dateValue < fisEntries[index - 1].dateValue) {
        outOfOrderFis.add(fisEntries[index].key);
        outOfOrderFis.add(fisEntries[index - 1].key);
      }
    }
    const allText = ledgerRows.map((row) => `${row.aciklama} ${row.hesapAdi} ${row.belgeTuru}`.toLocaleLowerCase("tr-TR")).join(" ");
    const fisLineCounts = /* @__PURE__ */ new Map();
    const exactLineCounts = /* @__PURE__ */ new Map();
    const nearKeys = /* @__PURE__ */ new Map();
    for (const row of ledgerRows) {
      if (row.fisNo && row.kaynak !== E_DEFTER_KAYNAK.MIZAN) {
        const fk = compactText3(row.fisNo);
        fisLineCounts.set(fk, (fisLineCounts.get(fk) || 0) + 1);
      }
      if (row.belgeNo || row.hesapKodu || row.fisNo) {
        const ek = [
          compactText3(row.fisNo),
          compactText3(row.yevmiyeNo),
          compactText3(row.belgeNo),
          compactText3(row.hesapKodu),
          roundMoney2(row.borc),
          roundMoney2(row.alacak),
          compactText3(row.tarih),
          compactText3(row.kaynak)
        ].join("|");
        exactLineCounts.set(ek, (exactLineCounts.get(ek) || 0) + 1);
      }
      if (row.tutar && row.cariUnvan && compactText3(row.cariUnvan) !== compactText3(row.aciklama)) {
        const nk = `${compactText3(row.cariUnvan)}|${roundMoney2(row.tutar)}`;
        const list = nearKeys.get(nk) || [];
        list.push(row);
        nearKeys.set(nk, list);
      }
    }
    return {
      accountBalances,
      problematicAccounts,
      unbalancedFis,
      fisNoGaps,
      outOfOrderFis,
      fisLineCounts,
      exactLineCounts,
      nearKeys,
      counterpartByRowId: resolveVoucherCounterparts(ledgerRows),
      hasKapanisFisi: /kapan[ıi]s|7\/a|7a|gelir tablosu kapan/.test(allText),
      hasAmortisman: /amortisman/.test(allText),
      hasKurDegerleme: /kur de[ğg]erleme|kur fark[ıi]|de[ğg]erleme fark[ıi]/.test(allText)
    };
  }
  var ISSUE_SEVERITY_RANK = {
    [E_DEFTER_ISSUE_SEVERITY.BILGI]: 1,
    [E_DEFTER_ISSUE_SEVERITY.UYARI]: 2,
    [E_DEFTER_ISSUE_SEVERITY.KRITIK]: 3
  };
  var GROUP_PRIORITY = {
    [E_DEFTER_KONTROL_GRUP.KRITIK]: 100,
    [E_DEFTER_KONTROL_GRUP.CAPRAZ]: 95,
    [E_DEFTER_KONTROL_GRUP.MUKERRER]: 80,
    [E_DEFTER_KONTROL_GRUP.TERS_BAKIYE]: 70,
    [E_DEFTER_KONTROL_GRUP.EKSIK_BILGI]: 60,
    [E_DEFTER_KONTROL_GRUP.DONEM_SONU]: 50,
    [E_DEFTER_KONTROL_GRUP.KDV_KONTROL]: 45,
    [E_DEFTER_KONTROL_GRUP.VERGISEL]: 40,
    [E_DEFTER_KONTROL_GRUP.TEKNIK]: 35,
    [E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI]: 30,
    [E_DEFTER_KONTROL_GRUP.HATASIZ]: 0
  };
  function createEDefterIssue({
    code = E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
    message = "",
    severity = E_DEFTER_ISSUE_SEVERITY.UYARI,
    group = E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
    blocking = false,
    source = "engine",
    riskScore = 0
  } = {}) {
    const safeMessage = String(message || "").trim() || "Tan\u0131ms\u0131z bulgu.";
    const safeSeverity = ISSUE_SEVERITY_RANK[severity] != null ? severity : E_DEFTER_ISSUE_SEVERITY.UYARI;
    const safeBlocking = Boolean(blocking) || safeSeverity === E_DEFTER_ISSUE_SEVERITY.KRITIK;
    return {
      code: String(code || E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE),
      message: safeMessage,
      severity: safeSeverity,
      group: group || E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
      blocking: safeBlocking,
      source: String(source || "engine"),
      riskScore: Math.max(0, Number(riskScore) || 0)
    };
  }
  function normalizeEDefterIssue(raw, source = "engine") {
    if (raw && typeof raw === "object" && raw.message) {
      return createEDefterIssue({
        code: raw.code || E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message: raw.message,
        severity: raw.severity || E_DEFTER_ISSUE_SEVERITY.UYARI,
        group: raw.group || E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        blocking: raw.blocking,
        source: raw.source || source,
        riskScore: raw.riskScore
      });
    }
    const message = String(raw || "").trim();
    if (!message) return null;
    const lower = message.toLocaleLowerCase("tr-TR");
    if (lower.includes("hesap plan\u0131nda yok")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.ACCOUNT_NOT_IN_PLAN,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
        group: E_DEFTER_KONTROL_GRUP.KRITIK,
        blocking: true,
        source,
        riskScore: 35
      });
    }
    if (lower.includes("d\xF6nem d\u0131\u015F\u0131")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.DATE_OUT_OF_PERIOD,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
        group: E_DEFTER_KONTROL_GRUP.KRITIK,
        blocking: true,
        source,
        riskScore: 30
      });
    }
    if (lower.includes("negatif tutar")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.NEGATIVE_AMOUNT,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
        group: E_DEFTER_KONTROL_GRUP.KRITIK,
        blocking: true,
        source,
        riskScore: 40
      });
    }
    if (lower.includes("dengesi bozuk") || lower.includes("bor\xE7/alacak")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.DEBIT_CREDIT_MISMATCH,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
        group: E_DEFTER_KONTROL_GRUP.KRITIK,
        blocking: true,
        source,
        riskScore: 45
      });
    }
    if (lower.includes("m\xFCkerrer") || lower.includes("mukerrer") || lower.includes("tekrar")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.DUPLICATE_ENTRY,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
        group: E_DEFTER_KONTROL_GRUP.MUKERRER,
        blocking: false,
        source,
        riskScore: 25
      });
    }
    if (lower.includes("a\xE7\u0131klama bo\u015F")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.MISSING_DESCRIPTION,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
        group: E_DEFTER_KONTROL_GRUP.EKSIK_BILGI,
        blocking: false,
        source,
        riskScore: 10
      });
    }
    if (lower.includes("hesap kodu bo\u015F") || lower.includes("belge t\xFCr\xFC bo\u015F") || lower.includes("yevmiye no eksik")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.MISSING_DOCUMENT_INFO,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
        group: E_DEFTER_KONTROL_GRUP.EKSIK_BILGI,
        blocking: false,
        source,
        riskScore: 15
      });
    }
    if (lower.includes("atlama") || lower.includes("tarih s\u0131ras\u0131")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.JOURNAL_SEQUENCE_GAP,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
        group: E_DEFTER_KONTROL_GRUP.KRITIK,
        blocking: true,
        source,
        riskScore: 50
      });
    }
    if (lower.includes("inceleme bilgisi") || lower.includes("ola\u011Fand\u0131\u015F\u0131 bakiye") || lower.includes("olagan disi bakiye") || lower.includes("ola\u011Fan d\u0131\u015F\u0131 y\xFCksek bakiye")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        blocking: false,
        source,
        riskScore: 8
      });
    }
    if (lower.includes("ters bakiye")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        blocking: false,
        source,
        riskScore: 10
      });
    }
    if (lower.includes("kasa hesab")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        blocking: false,
        source,
        riskScore: 10
      });
    }
    if (lower.includes("kdv")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
        group: E_DEFTER_KONTROL_GRUP.KDV_KONTROL,
        blocking: false,
        source,
        riskScore: 20
      });
    }
    if (lower.includes("d\xF6nem sonu") || lower.includes("kapan") || lower.includes("amortisman")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
        group: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
        blocking: false,
        source,
        riskScore: 20
      });
    }
    if (lower.includes("yuvarlama")) {
      return createEDefterIssue({
        code: E_DEFTER_ISSUE_CODE.SUSPICIOUS_ROUNDING,
        message,
        severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
        group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        blocking: false,
        source,
        riskScore: 10
      });
    }
    return createEDefterIssue({
      code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
      message,
      severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
      group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
      blocking: false,
      source,
      riskScore: 20
    });
  }
  function classifyEDefterIssues(rawIssues = []) {
    const issueDetails = [];
    const seen = /* @__PURE__ */ new Set();
    for (const raw of rawIssues || []) {
      const issue = normalizeEDefterIssue(raw);
      if (!issue) continue;
      const dedupeKey = `${issue.code}|${issue.message}`;
      if (seen.has(dedupeKey)) continue;
      seen.add(dedupeKey);
      issueDetails.push(issue);
    }
    if (!issueDetails.length) {
      return {
        issueDetails: [],
        issues: [],
        primaryGroup: E_DEFTER_KONTROL_GRUP.HATASIZ,
        riskScore: 0,
        maxSeverity: null,
        hasBlocking: false,
        hasNonInfo: false
      };
    }
    let primaryGroup = E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI;
    let bestGroupRank = -1;
    let riskScore = 0;
    let maxSeverity = E_DEFTER_ISSUE_SEVERITY.BILGI;
    let hasBlocking = false;
    let hasNonInfo = false;
    for (const issue of issueDetails) {
      riskScore += issue.riskScore || 0;
      if (issue.blocking) hasBlocking = true;
      if (issue.severity !== E_DEFTER_ISSUE_SEVERITY.BILGI) hasNonInfo = true;
      if ((ISSUE_SEVERITY_RANK[issue.severity] || 0) > (ISSUE_SEVERITY_RANK[maxSeverity] || 0)) {
        maxSeverity = issue.severity;
      }
      const rank = GROUP_PRIORITY[issue.group] ?? GROUP_PRIORITY[E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI];
      if (rank > bestGroupRank) {
        bestGroupRank = rank;
        primaryGroup = issue.group || E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI;
      }
    }
    if (primaryGroup === E_DEFTER_KONTROL_GRUP.HATASIZ) {
      primaryGroup = E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI;
    }
    return {
      issueDetails,
      issues: issueDetails.map((item) => item.message),
      primaryGroup,
      riskScore: Math.max(1, Math.min(100, riskScore)),
      maxSeverity,
      hasBlocking,
      hasNonInfo
    };
  }
  function buildIssues(row, _allRows = [], context = {}) {
    const raw = [];
    const fisKey = compactText3(row.fisNo);
    const hesapKey = compactText3(row.hesapKodu);
    if (!row.hesapKodu) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.MISSING_DOCUMENT_INFO,
          message: "Hesap kodu bo\u015F.",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.EKSIK_BILGI,
          riskScore: 20
        })
      );
    }
    if (!row.aciklama) {
      const skipMizanStructuralParent = row.kaynak === E_DEFTER_KAYNAK.MIZAN && row.mizanAccountRole === "PARENT";
      if (!skipMizanStructuralParent) {
        raw.push(
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.MISSING_DESCRIPTION,
            message: "A\xE7\u0131klama bo\u015F.",
            severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
            group: E_DEFTER_KONTROL_GRUP.EKSIK_BILGI,
            riskScore: 10
          })
        );
      }
    }
    if (!row.belgeTuru && row.kaynak !== E_DEFTER_KAYNAK.MIZAN) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.MISSING_DOCUMENT_INFO,
          message: "Belge t\xFCr\xFC bo\u015F (inceleme bilgisi).",
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 5
        })
      );
    }
    const isMuavinRow = row.kaynak === E_DEFTER_KAYNAK.MUAVIN || row.documentClass === "MUAVIN" || context.documentClass === "MUAVIN";
    const yevmiyeEvidencePresent = Boolean(context.yevmiyeEvidencePresent);
    const requireYevmiyeNo = row.kaynak !== E_DEFTER_KAYNAK.MIZAN && !(isMuavinRow && !yevmiyeEvidencePresent);
    if (!row.yevmiyeNo && requireYevmiyeNo) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.MISSING_DOCUMENT_INFO,
          message: "Yevmiye no eksik.",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.EKSIK_BILGI,
          riskScore: 15
        })
      );
    }
    if (context.unbalancedFis?.has(fisKey) && row.fisNo) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.DEBIT_CREDIT_MISMATCH,
          message: "Fi\u015F bor\xE7/alacak dengesi bozuk.",
          severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
          group: E_DEFTER_KONTROL_GRUP.KRITIK,
          blocking: true,
          riskScore: 45
        })
      );
    }
    const counterpart = context.counterpartByRowId?.get(row.id);
    const isMuavinOnly = isMuavinRow;
    if (counterpart?.code === E_DEFTER_ISSUE_CODE.MULTI_COUNTERPART) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.MULTI_COUNTERPART,
          message: `Birden fazla kar\u015F\u0131t hesap aday\u0131 (${counterpart.candidates.length}) \u2014 otomatik tek kar\u015F\u0131t uydurulmad\u0131.`,
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 12
        })
      );
    } else if (counterpart?.code === E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT,
          message: "Excel kar\u015F\u0131 hesap ile fi\u015F bacaklar\u0131ndan hesaplanan kar\u015F\u0131t \xE7eli\u015Fiyor.",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 32
        })
      );
    } else if (counterpart?.code === E_DEFTER_ISSUE_CODE.COUNTERPART_VERIFIED) {
    } else if (counterpart?.code === E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART) {
      if (!isMuavinOnly) {
        raw.push(
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART,
            message: "Ayn\u0131 fi\u015Fte kar\u015F\u0131t hesap baca\u011F\u0131 bulunamad\u0131.",
            severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
            group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
            riskScore: 25
          })
        );
      }
    } else if (counterpart?.code === E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE) {
      if (!isMuavinOnly) {
        raw.push(
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE,
            message: "Ayn\u0131 fi\u015Fte yaln\u0131z ayn\u0131 y\xF6nl\xFC sat\u0131rlar var; kar\u015F\u0131t hesap ba\u011Flanamaz.",
            severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
            group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
            riskScore: 28
          })
        );
      }
    } else if (counterpart?.code === E_DEFTER_ISSUE_CODE.COUNTERPART_SELF) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.COUNTERPART_SELF,
          message: "Hesap kendisine kar\u015F\u0131t olarak i\u015Faretlenmi\u015F.",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 30
        })
      );
    } else if (counterpart?.code === E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW,
          message: "Kar\u015F\u0131t hesap g\xFCvenli \xE7\xF6z\xFClemedi (fi\u015F kimli\u011Fi eksik veya belirsiz) \u2014 inceleme gerekli.",
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 10
        })
      );
    }
    if (context.outOfOrderFis?.has(fisKey) && row.fisNo) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.JOURNAL_SEQUENCE_GAP,
          message: "Tarih s\u0131ras\u0131 bozuk fi\u015F.",
          severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
          group: E_DEFTER_KONTROL_GRUP.KRITIK,
          blocking: true,
          riskScore: 20
        })
      );
    }
    if (row.belgeTarihi && row.tarih && daysBetween(row.belgeTarihi, row.tarih) > BELGE_TARIH_FARK_GUN) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.DOCUMENT_DATE_GAP,
          message: "Belge tarihi ile fi\u015F tarihi aras\u0131nda anlaml\u0131 fark var.",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 15
        })
      );
    }
    const exactLineKey = [
      compactText3(row.fisNo),
      compactText3(row.yevmiyeNo),
      compactText3(row.belgeNo),
      compactText3(row.hesapKodu),
      roundMoney2(row.borc),
      roundMoney2(row.alacak),
      compactText3(row.tarih),
      compactText3(row.kaynak)
    ].join("|");
    if ((row.belgeNo || row.hesapKodu || row.fisNo) && (context.exactLineCounts?.get(exactLineKey) || 0) > 1) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.DUPLICATE_ENTRY,
          message: "Birebir ayn\u0131 sat\u0131r tekrar\u0131 (fi\u015F+yevmiye+belge+hesap+tutar+tarih).",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.MUKERRER,
          riskScore: 30
        })
      );
    }
    if (row.tutar && row.cariUnvan && compactText3(row.cariUnvan) !== compactText3(row.aciklama) && context.nearKeys) {
      const nk = `${compactText3(row.cariUnvan)}|${roundMoney2(row.tutar)}`;
      const peers = context.nearKeys.get(nk) || [];
      const nearSimilar = peers.some((item) => {
        if (item.id === row.id) return false;
        if (compactText3(item.fisNo) && compactText3(row.fisNo) && compactText3(item.fisNo) === compactText3(row.fisNo)) {
          return false;
        }
        if (compactText3(item.kaynak) && compactText3(row.kaynak) && compactText3(item.kaynak) !== compactText3(row.kaynak)) {
          return false;
        }
        return daysBetween(item.tarih, row.tarih) <= NEAR_DATE_DAYS;
      });
      if (nearSimilar) {
        raw.push(
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.DUPLICATE_ENTRY,
            message: "Benzer cari + tutar + yak\u0131n tarih (inceleme bilgisi).",
            severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
            group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
            riskScore: 8
          })
        );
      }
    }
    if (row.tutar > 0 && row.tutar % 1e3 === 0 && row.tutar >= 1e4) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.SUSPICIOUS_ROUNDING,
          message: "\u015E\xFCpheli yuvarlama kayd\u0131.",
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 10
        })
      );
    }
    if (Number(row.borc || 0) < 0 || Number(row.alacak || 0) < 0) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.NEGATIVE_AMOUNT,
          message: "Negatif tutar sat\u0131r\u0131.",
          severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
          group: E_DEFTER_KONTROL_GRUP.KRITIK,
          blocking: true,
          riskScore: 40
        })
      );
    }
    if (Number(row.borc || 0) === 0 && Number(row.alacak || 0) === 0 && row.kaynak !== E_DEFTER_KAYNAK.MIZAN) {
      raw.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.ZERO_AMOUNT,
          message: "S\u0131f\u0131r tutarl\u0131 sat\u0131r.",
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 15
        })
      );
    }
    if (context.accountPlanCodes instanceof Set && row.hesapKodu) {
      const skipMizanStructuralParent = row.kaynak === E_DEFTER_KAYNAK.MIZAN && row.mizanAccountRole === "PARENT";
      if (!skipMizanStructuralParent) {
        const code = String(row.hesapKodu).trim();
        const short = code.split(".")[0];
        const compactCode = compactText3(code);
        if (!context.accountPlanCodes.has(code) && !context.accountPlanCodes.has(compactCode) && !context.accountPlanCodes.has(short)) {
          raw.push(
            createEDefterIssue({
              code: E_DEFTER_ISSUE_CODE.ACCOUNT_NOT_IN_PLAN,
              message: "Hesap kodu hesap plan\u0131nda yok.",
              severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
              group: E_DEFTER_KONTROL_GRUP.KRITIK,
              blocking: true,
              riskScore: 35
            })
          );
        }
      }
    }
    if (context.expectedPeriodKey && row.tarih) {
      const d = parseDateTR(row.tarih);
      if (d) {
        const isMuavinRow2 = row.kaynak === E_DEFTER_KAYNAK.MUAVIN || row.documentClass === "MUAVIN" || context.documentClass === "MUAVIN";
        const isCumulativeYevmiyeRow = context.cumulativePeriod === true && (row.kaynak === E_DEFTER_KAYNAK.YEVMIYE || row.documentClass === "YEVMIYE");
        const [yearStr, monthStr] = String(context.expectedPeriodKey).split("-");
        const expectedYear = Number(yearStr);
        const expectedMonth = Number(monthStr);
        const rowYear = d.getFullYear();
        const rowMonth = d.getMonth() + 1;
        const useCumulativePeriod = isMuavinRow2 || isCumulativeYevmiyeRow;
        const outOfPeriod = useCumulativePeriod ? !expectedYear || !expectedMonth || rowYear !== expectedYear || rowMonth < 1 || rowMonth > expectedMonth : `${rowYear}-${String(rowMonth).padStart(2, "0")}` !== context.expectedPeriodKey;
        if (outOfPeriod) {
          raw.push(
            createEDefterIssue({
              code: E_DEFTER_ISSUE_CODE.DATE_OUT_OF_PERIOD,
              message: "D\xF6nem d\u0131\u015F\u0131 tarih.",
              severity: E_DEFTER_ISSUE_SEVERITY.KRITIK,
              group: E_DEFTER_KONTROL_GRUP.KRITIK,
              blocking: true,
              riskScore: 30
            })
          );
        }
      }
    }
    const accountIssue = context.problematicAccounts?.get(hesapKey);
    if (accountIssue?.issues?.length) {
      for (const msg of accountIssue.issues) {
        raw.push(normalizeEDefterIssue(msg, "account-balance"));
      }
    }
    return classifyEDefterIssues(raw);
  }
  function resolvePrimaryGroup(issues = [], row = {}) {
    const classified = classifyEDefterIssues(issues);
    if (classified.issueDetails.length > 0) return classified.primaryGroup;
    if (!row.hesapKodu || !row.aciklama || !row.belgeTuru && row.kaynak !== E_DEFTER_KAYNAK.MIZAN) {
      return E_DEFTER_KONTROL_GRUP.EKSIK_BILGI;
    }
    return E_DEFTER_KONTROL_GRUP.HATASIZ;
  }
  function resolveDurum(grup, row = {}) {
    if (row.kontrolDurumu) return row.kontrolDurumu;
    const map = {
      [E_DEFTER_KONTROL_GRUP.HATASIZ]: E_DEFTER_KONTROL_DURUM.HATASIZ,
      [E_DEFTER_KONTROL_GRUP.KRITIK]: E_DEFTER_KONTROL_DURUM.KRITIK,
      [E_DEFTER_KONTROL_GRUP.MUKERRER]: E_DEFTER_KONTROL_DURUM.MUKERRER,
      [E_DEFTER_KONTROL_GRUP.TERS_BAKIYE]: E_DEFTER_KONTROL_DURUM.TERS_BAKIYE,
      [E_DEFTER_KONTROL_GRUP.EKSIK_BILGI]: E_DEFTER_KONTROL_DURUM.EKSIK_BILGI,
      [E_DEFTER_KONTROL_GRUP.DONEM_SONU]: E_DEFTER_KONTROL_DURUM.DONEM_SONU,
      [E_DEFTER_KONTROL_GRUP.KDV_KONTROL]: E_DEFTER_KONTROL_DURUM.KDV_KONTROL,
      [E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI]: E_DEFTER_KONTROL_DURUM.INCELEME_GEREKLI
    };
    return map[grup] || E_DEFTER_KONTROL_DURUM.INCELEME_GEREKLI;
  }
  function buildPeriodEndWarnings(context = {}) {
    const warnings = [];
    if (!context.hasKapanisFisi) {
      warnings.push({
        id: "donem-sonu-kapanis",
        kaynak: E_DEFTER_KAYNAK.MUAVIN,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "D\xF6nem Sonu Kontrol",
        aciklama: "Kapan\u0131\u015F fi\u015Fi tespit edilemedi.",
        belgeTuru: "Bilgi",
        belgeNo: "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issueDetails: [
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
            message: "Kapan\u0131\u015F fi\u015Fi kayd\u0131 bulunamad\u0131 (inceleme bilgisi).",
            severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
            group: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
            riskScore: 8
          })
        ],
        issues: ["Kapan\u0131\u015F fi\u015Fi kayd\u0131 bulunamad\u0131 (inceleme bilgisi)."],
        riskScore: 8,
        riskBand: riskBandFromScore(8),
        grup: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
        durum: E_DEFTER_KONTROL_DURUM.DONEM_SONU,
        sonucSeviye: E_DEFTER_SONUC_SEVIYE.BILGI
      });
    }
    if (!context.hasAmortisman) {
      warnings.push({
        id: "donem-sonu-amortisman",
        kaynak: E_DEFTER_KAYNAK.MUAVIN,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "D\xF6nem Sonu Kontrol",
        aciklama: "Amortisman kayd\u0131 tespit edilemedi.",
        belgeTuru: "Bilgi",
        belgeNo: "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issueDetails: [
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
            message: "Amortisman gider kayd\u0131 bulunamad\u0131 (inceleme bilgisi).",
            severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
            group: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
            riskScore: 8
          })
        ],
        issues: ["Amortisman gider kayd\u0131 bulunamad\u0131 (inceleme bilgisi)."],
        riskScore: 8,
        riskBand: riskBandFromScore(8),
        grup: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
        durum: E_DEFTER_KONTROL_DURUM.DONEM_SONU,
        sonucSeviye: E_DEFTER_SONUC_SEVIYE.BILGI
      });
    }
    if (!context.hasKurDegerleme) {
      warnings.push({
        id: "donem-sonu-kur",
        kaynak: E_DEFTER_KAYNAK.MUAVIN,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "D\xF6nem Sonu Kontrol",
        aciklama: "Kur de\u011Ferleme kayd\u0131 tespit edilemedi.",
        belgeTuru: "Bilgi",
        belgeNo: "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issueDetails: [
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
            message: "Kur de\u011Ferleme / kur fark\u0131 kayd\u0131 bulunamad\u0131 (inceleme bilgisi).",
            severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
            group: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
            riskScore: 8
          })
        ],
        issues: ["Kur de\u011Ferleme / kur fark\u0131 kayd\u0131 bulunamad\u0131 (inceleme bilgisi)."],
        riskScore: 8,
        riskBand: riskBandFromScore(8),
        grup: E_DEFTER_KONTROL_GRUP.DONEM_SONU,
        durum: E_DEFTER_KONTROL_DURUM.DONEM_SONU,
        sonucSeviye: E_DEFTER_SONUC_SEVIYE.BILGI
      });
    }
    for (const gap of context.fisNoGaps || []) {
      warnings.push({
        id: `fis-gap-${gap.from}-${gap.to}`,
        kaynak: E_DEFTER_KAYNAK.MUAVIN,
        tarih: "",
        fisNo: `${gap.from + 1}-${gap.to - 1}`,
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "Fi\u015F No Kontrol",
        aciklama: `Fi\u015F no atlamas\u0131: ${gap.from} ile ${gap.to} aras\u0131nda eksik numara.`,
        belgeTuru: "Uyar\u0131",
        belgeNo: "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issues: [`Fi\u015F no atlamas\u0131 (${gap.from} \u2192 ${gap.to}).`],
        riskScore: 50,
        riskBand: riskBandFromScore(50),
        grup: E_DEFTER_KONTROL_GRUP.KRITIK,
        durum: E_DEFTER_KONTROL_DURUM.KRITIK
      });
    }
    return warnings;
  }
  function severityToSonucSeviye(severity) {
    if (severity === E_DEFTER_ISSUE_SEVERITY.KRITIK) return E_DEFTER_SONUC_SEVIYE.KRITIK;
    if (severity === E_DEFTER_ISSUE_SEVERITY.UYARI) return E_DEFTER_SONUC_SEVIYE.UYARI;
    if (severity === E_DEFTER_ISSUE_SEVERITY.BILGI) return E_DEFTER_SONUC_SEVIYE.BILGI;
    return E_DEFTER_SONUC_SEVIYE.UYGUN;
  }
  function analyzeEDefterRow(row, allRows = [], context = {}) {
    if (row.disaridaBirak) {
      return {
        ...row,
        issues: [],
        issueDetails: [],
        riskScore: 0,
        riskBand: riskBandFromScore(0),
        riskLevel: riskLevelFromScore(0),
        sonucSeviye: E_DEFTER_SONUC_SEVIYE.UYGUN,
        hasBlockingIssue: false,
        grup: E_DEFTER_KONTROL_GRUP.HATASIZ,
        durum: row.kontrolDurumu || "Kontrol d\u0131\u015F\u0131"
      };
    }
    if (row.grup && row.id?.startsWith("donem-sonu")) {
      return row;
    }
    if (row.grup && row.id?.startsWith("fis-gap")) {
      return row;
    }
    const counterpartResolved = context.counterpartByRowId?.get(row.id) || null;
    const analysis = buildIssues(row, allRows, context);
    const hasIssues = analysis.issueDetails.length > 0;
    const grup = hasIssues ? analysis.primaryGroup === E_DEFTER_KONTROL_GRUP.HATASIZ ? E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI : analysis.primaryGroup : resolvePrimaryGroup(analysis.issues, row);
    const durum = resolveDurum(grup, row);
    const riskScore = hasIssues ? Math.max(1, analysis.riskScore || 0) : grup === E_DEFTER_KONTROL_GRUP.HATASIZ ? 0 : analysis.riskScore;
    const sonucSeviye = hasIssues ? severityToSonucSeviye(analysis.maxSeverity) : sonucSeviyeFromScore(riskScore);
    const openingSkip = counterpartResolved?.reason === "opening_voucher";
    const resolvedCounterpart = openingSkip ? "" : counterpartResolved?.counterAccountCode || row.counterAccountCode || row.karsiHesapKodu || "";
    return {
      ...row,
      borc: roundMoney2(row.borc),
      alacak: roundMoney2(row.alacak),
      tutar: roundMoney2(row.tutar || Math.max(row.borc, row.alacak)),
      counterAccountCode: resolvedCounterpart,
      karsiHesapKodu: resolvedCounterpart,
      counterpartStatus: counterpartResolved?.status || "",
      counterpartCandidates: openingSkip ? [] : counterpartResolved?.candidates || [],
      counterpartConfidence: openingSkip ? null : counterpartResolved?.confidence ?? null,
      openingVoucherClass: openingSkip ? OPENING_VOUCHER_CLASS : "",
      issues: analysis.issues,
      issueDetails: analysis.issueDetails,
      riskScore,
      riskBand: riskBandFromScore(riskScore),
      riskLevel: riskLevelFromScore(riskScore),
      sonucSeviye,
      hasBlockingIssue: Boolean(analysis.hasBlocking),
      hataTuru: row.hataTuru || E_DEFTER_HATA_TURU.MUHASEBESEL,
      onerilenKontrol: row.onerilenKontrol || (analysis.issues[0] ? `${analysis.issues[0]} i\xE7in belge ve fi\u015F kontrol\xFC yap\u0131n.` : ""),
      cozumDurumu: row.cozumDurumu || E_DEFTER_FINDING_STATUS.YENI,
      smartExplanation: row.smartExplanation || buildSmartEDefterExplanation(row, analysis.issues),
      grup,
      durum
    };
  }
  function analyzeEDefterRows(rows = [], options = {}) {
    const hasYevmiyeRows = (rows || []).some(
      (row) => row.kaynak === E_DEFTER_KAYNAK.YEVMIYE || row.kaynak === E_DEFTER_KAYNAK.YEVMIYE_XML
    );
    const context = {
      ...buildGlobalContext(rows),
      accountPlanCodes: options.accountPlanCodes || null,
      expectedPeriodKey: options.expectedPeriod ? normalizePeriodKey(String(options.expectedPeriod).replace("/", "-")) : "",
      documentClass: options.documentClass || "",
      yevmiyeEvidencePresent: options.yevmiyeEvidencePresent === true || options.yevmiyeEvidencePresent !== false && hasYevmiyeRows,
      cumulativePeriod: options.cumulativePeriod === true
    };
    const analyzed = rows.map((row) => analyzeEDefterRow(row, rows, context));
    const warnings = buildPeriodEndWarnings(context);
    return [...analyzed, ...warnings];
  }
  function sumDebitCredit(rows = []) {
    return rows.reduce(
      (acc, row) => {
        acc.borc += roundMoney2(row.borc);
        acc.alacak += roundMoney2(row.alacak);
        return acc;
      },
      { borc: 0, alacak: 0 }
    );
  }
  function reconcileJournalLedger(yevmiyeRows = [], kebirRows = []) {
    const findings = [];
    if (!yevmiyeRows.length || !kebirRows.length) {
      return { findings, matched: false, skipped: true };
    }
    const yTotals = sumDebitCredit(yevmiyeRows);
    const kTotals = sumDebitCredit(kebirRows);
    const borcFark = roundMoney2(yTotals.borc - kTotals.borc);
    const alacakFark = roundMoney2(yTotals.alacak - kTotals.alacak);
    if (Math.abs(borcFark) > BORC_ALACAK_TOLERANCE || Math.abs(alacakFark) > BORC_ALACAK_TOLERANCE) {
      findings.push({
        code: E_DEFTER_FINDING_CODE.JOURNAL_LEDGER_MISMATCH,
        message: `Yevmiye-kebir toplam fark\u0131: bor\xE7 fark=${borcFark}, alacak fark=${alacakFark}`,
        level: E_DEFTER_SONUC_SEVIYE.KRITIK
      });
    }
    const yMap = buildAccountBalanceMap(yevmiyeRows);
    const kMap = buildAccountBalanceMap(kebirRows);
    const allKeys = /* @__PURE__ */ new Set([...yMap.keys(), ...kMap.keys()]);
    for (const key of allKeys) {
      const y = yMap.get(key) || { borc: 0, alacak: 0, net: 0, hesapKodu: key };
      const k = kMap.get(key) || { borc: 0, alacak: 0, net: 0, hesapKodu: key };
      if (Math.abs(y.borc - k.borc) > BORC_ALACAK_TOLERANCE || Math.abs(y.alacak - k.alacak) > BORC_ALACAK_TOLERANCE) {
        findings.push({
          code: E_DEFTER_FINDING_CODE.JOURNAL_LEDGER_MISMATCH,
          message: `Hesap bazl\u0131 yevmiye-kebir fark\u0131: ${y.hesapKodu || k.hesapKodu}`,
          level: E_DEFTER_SONUC_SEVIYE.KRITIK
        });
        break;
      }
    }
    return {
      findings,
      matched: findings.length === 0,
      skipped: false,
      yTotals,
      kTotals
    };
  }
  function buildCrossFindingRows(findings = [], context = {}) {
    return (findings || []).map((finding, index) => {
      const riskScore = mapLegacyLevelToSonuc(finding.level) === E_DEFTER_SONUC_SEVIYE.KRITIK ? 90 : 55;
      const row = {
        id: `capraz-${finding.code || index}`,
        kaynak: E_DEFTER_KAYNAK.CAPRAZ,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "\xC7apraz Mutabakat",
        aciklama: finding.message,
        belgeTuru: "\xC7apraz",
        belgeNo: finding.code || "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issues: [finding.message],
        riskScore,
        riskBand: riskBandFromScore(riskScore),
        riskLevel: finding.level || riskLevelFromScore(riskScore),
        sonucSeviye: mapLegacyLevelToSonuc(finding.level),
        hataTuru: E_DEFTER_HATA_TURU.MUHASEBESEL,
        onerilenKontrol: "Yevmiye ve kebir toplamlar\u0131n\u0131 hesap baz\u0131nda kar\u015F\u0131la\u015Ft\u0131r\u0131n.",
        cozumDurumu: E_DEFTER_FINDING_STATUS.YENI,
        grup: E_DEFTER_KONTROL_GRUP.CAPRAZ,
        durum: E_DEFTER_KONTROL_DURUM.KRITIK,
        companyId: context.companyId || "",
        period: context.period || ""
      };
      row.smartExplanation = buildSmartEDefterExplanation(row, row.issues);
      return row;
    });
  }
  function analyzeBeratMeta(beratMeta = null, packageMeta = {}, options = {}) {
    const findings = [];
    if (!beratMeta) {
      findings.push({
        code: "BERAT_ESLESMEDI",
        message: "Berat dosyas\u0131 eksik veya okunamad\u0131 (inceleme bilgisi).",
        level: E_DEFTER_SONUC_SEVIYE.BILGI
      });
      return findings;
    }
    if (options.companyTaxId && beratMeta.taxId) {
      if (normalizeTaxId(beratMeta.taxId) !== normalizeTaxId(options.companyTaxId)) {
        findings.push({
          code: EDEFTER_ERROR_CODE.COMPANY_MISMATCH,
          message: "Berat firma bilgisi se\xE7ili firma ile uyu\u015Fmuyor.",
          level: E_DEFTER_SONUC_SEVIYE.KRITIK
        });
      }
    }
    if (options.expectedPeriod && beratMeta.period) {
      const a = normalizePeriodKey(beratMeta.period);
      const b = normalizePeriodKey(String(options.expectedPeriod).replace("/", "-"));
      if (a && b && a !== b) {
        findings.push({
          code: "BERAT_DONEM",
          message: "Berat d\xF6nemi beklenen d\xF6nem ile uyu\u015Fmuyor.",
          level: E_DEFTER_SONUC_SEVIYE.UYARI
        });
      }
    }
    findings.push({
      code: E_DEFTER_FINDING_CODE.EXTERNAL_VERIFICATION_REQUIRED,
      message: "Berat varl\u0131k/t\xFCr bilgisi okundu; G\u0130B veya mali m\xFCh\xFCr kriptografik do\u011Frulamas\u0131 yap\u0131lmad\u0131. Harici do\u011Frulama gerekir.",
      level: E_DEFTER_SONUC_SEVIYE.BILGI
    });
    return findings;
  }
  function resolveOverallSonuc(rows = []) {
    let worst = E_DEFTER_SONUC_SEVIYE.UYGUN;
    const rank = {
      [E_DEFTER_SONUC_SEVIYE.UYGUN]: 0,
      [E_DEFTER_SONUC_SEVIYE.BILGI]: 1,
      [E_DEFTER_SONUC_SEVIYE.UYARI]: 2,
      [E_DEFTER_SONUC_SEVIYE.KRITIK]: 3
    };
    for (const row of rows.filter((r) => !r.disaridaBirak)) {
      const details = Array.isArray(row.issueDetails) ? row.issueDetails : [];
      const unresolved = details.filter(
        () => row.cozumDurumu !== E_DEFTER_FINDING_STATUS.COZULDU
      );
      const hasBlocking = unresolved.some((issue) => issue.blocking);
      const hasNonInfo = unresolved.some(
        (issue) => issue.severity !== E_DEFTER_ISSUE_SEVERITY.BILGI
      );
      const legacyIssues = Array.isArray(row.issues) ? row.issues : [];
      const hasLegacyIssues = unresolved.length === 0 && legacyIssues.length > 0 && row.cozumDurumu !== E_DEFTER_FINDING_STATUS.COZULDU;
      let seviye = row.sonucSeviye || mapLegacyLevelToSonuc(row.riskLevel) || sonucSeviyeFromScore(row.riskScore || 0);
      if (hasBlocking || row.grup === E_DEFTER_KONTROL_GRUP.KRITIK || row.grup === E_DEFTER_KONTROL_GRUP.CAPRAZ) {
        worst = E_DEFTER_SONUC_SEVIYE.KRITIK;
        break;
      }
      if (hasNonInfo) {
        const fromIssues = unresolved.reduce((acc, issue) => {
          const mapped = severityToSonucSeviye(issue.severity);
          return (rank[mapped] || 0) > (rank[acc] || 0) ? mapped : acc;
        }, E_DEFTER_SONUC_SEVIYE.UYGUN);
        if ((rank[fromIssues] || 0) > (rank[seviye] || 0)) seviye = fromIssues;
        if (seviye === E_DEFTER_SONUC_SEVIYE.UYGUN) seviye = E_DEFTER_SONUC_SEVIYE.UYARI;
      } else if (hasLegacyIssues) {
        const legacyLevel = row.sonucSeviye || mapLegacyLevelToSonuc(row.riskLevel) || mapLegacyLevelToSonuc(legacyIssues[0]) || sonucSeviyeFromScore(row.riskScore || 0);
        if ((rank[legacyLevel] || 0) > (rank[seviye] || 0)) seviye = legacyLevel;
        if (seviye === E_DEFTER_SONUC_SEVIYE.UYGUN) seviye = E_DEFTER_SONUC_SEVIYE.BILGI;
      }
      if ((rank[seviye] || 0) > (rank[worst] || 0)) worst = seviye;
      if (row.grup === E_DEFTER_KONTROL_GRUP.TEKNIK && (row.riskScore || 0) >= 70) {
        worst = E_DEFTER_SONUC_SEVIYE.KRITIK;
      }
    }
    return worst;
  }
  function resolveEdefterUygun(rows = [], overallSonuc = E_DEFTER_SONUC_SEVIYE.UYGUN) {
    if (overallSonuc === E_DEFTER_SONUC_SEVIYE.KRITIK || overallSonuc === E_DEFTER_SONUC_SEVIYE.UYARI) {
      return false;
    }
    for (const row of rows.filter((r) => !r.disaridaBirak)) {
      if (row.cozumDurumu === E_DEFTER_FINDING_STATUS.COZULDU) continue;
      const details = Array.isArray(row.issueDetails) ? row.issueDetails : [];
      if (details.some((issue) => issue.blocking || issue.severity !== E_DEFTER_ISSUE_SEVERITY.BILGI)) {
        return false;
      }
      if (!details.length && Array.isArray(row.issues) && row.issues.length > 0 || row.grup && row.grup !== E_DEFTER_KONTROL_GRUP.HATASIZ && row.grup !== E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI) {
        if (row.grup === E_DEFTER_KONTROL_GRUP.KRITIK || row.grup === E_DEFTER_KONTROL_GRUP.CAPRAZ || row.grup === E_DEFTER_KONTROL_GRUP.MUKERRER || row.grup === E_DEFTER_KONTROL_GRUP.EKSIK_BILGI || row.grup === E_DEFTER_KONTROL_GRUP.TERS_BAKIYE) {
          return false;
        }
      }
    }
    return overallSonuc === E_DEFTER_SONUC_SEVIYE.UYGUN || overallSonuc === E_DEFTER_SONUC_SEVIYE.BILGI;
  }
  function canApproveEDefterExport(overallSonuc) {
    return overallSonuc !== E_DEFTER_SONUC_SEVIYE.KRITIK;
  }
  function buildFisKontrolDeepLink({ companyId = "", fisNo = "" } = {}) {
    const params = new URLSearchParams();
    if (companyId) params.set("companyId", companyId);
    if (fisNo) params.set("fisNo", fisNo);
    params.set("from", "e-defter-kontrol");
    return `/muhasebe/fis-kontrol?${params.toString()}`;
  }
  function buildEDefterIntegrationHooks({
    rows = [],
    companyId = "",
    coreDecision = null
  } = {}) {
    const criticalFis = rows.filter(
      (row) => row.fisNo && (row.grup === E_DEFTER_KONTROL_GRUP.KRITIK || row.sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK || row.riskLevel === E_DEFTER_RISK_LEVEL.KRITIK)
    ).map((row) => row.fisNo);
    const uniqueFis = [...new Set(criticalFis)];
    return {
      writeToAccountMemory: false,
      corePriority: true,
      coreOverridesMemory: true,
      coreDecisionSource: coreDecision?.decision_source || coreDecision?.source || null,
      fisKontrolLinks: uniqueFis.map(
        (fisNo) => buildFisKontrolDeepLink({ companyId, fisNo })
      ),
      disclaimer: E_DEFTER_REPORT_DISCLAIMER
    };
  }
  function recalculateEDefterSummary(rows = []) {
    const activeRows = rows.filter((row) => !row.disaridaBirak);
    const fisSet = new Set(
      activeRows.map((row) => compactText3(row.fisNo)).filter(Boolean)
    );
    let kritikHata = 0;
    let yuksekRisk = 0;
    let mukerrerRisk = 0;
    let tersBakiye = 0;
    let eksikBilgi = 0;
    let teknikHata = 0;
    let vergiselRisk = 0;
    let uyariSayisi = 0;
    for (const row of activeRows) {
      if (row.grup === E_DEFTER_KONTROL_GRUP.KRITIK || row.riskLevel === E_DEFTER_RISK_LEVEL.KRITIK) {
        kritikHata += 1;
      }
      if (row.riskScore >= 70 || row.riskLevel === E_DEFTER_RISK_LEVEL.YUKSEK) yuksekRisk += 1;
      if (row.grup === E_DEFTER_KONTROL_GRUP.MUKERRER) mukerrerRisk += 1;
      if (row.grup === E_DEFTER_KONTROL_GRUP.TERS_BAKIYE) tersBakiye += 1;
      if (row.grup === E_DEFTER_KONTROL_GRUP.EKSIK_BILGI) eksikBilgi += 1;
      if (row.grup === E_DEFTER_KONTROL_GRUP.TEKNIK || row.hataTuru === E_DEFTER_HATA_TURU.TEKNIK) {
        teknikHata += 1;
      }
      if (row.grup === E_DEFTER_KONTROL_GRUP.VERGISEL || row.hataTuru === E_DEFTER_HATA_TURU.VERGISEL) {
        vergiselRisk += 1;
      }
      if (row.riskLevel === E_DEFTER_RISK_LEVEL.ORTA || row.riskLevel === E_DEFTER_RISK_LEVEL.DUSUK) {
        uyariSayisi += 1;
      }
    }
    return {
      toplamFis: fisSet.size,
      toplamSatir: activeRows.length,
      kritikHata,
      yuksekRisk,
      mukerrerRisk,
      tersBakiye,
      eksikBilgi,
      yuklenenDefterSayisi: 0,
      teknikHata,
      vergiselRisk,
      uyariSayisi
    };
  }
  function groupEDefterCounts(rows = []) {
    const counts = Object.fromEntries(
      Object.values(E_DEFTER_KONTROL_GRUP).map((grup) => [grup, 0])
    );
    for (const row of rows.filter((item) => !item.disaridaBirak)) {
      counts[row.grup] = (counts[row.grup] || 0) + 1;
    }
    return Object.entries(counts).map(([grup, count]) => ({ grup, count }));
  }
  function runEDefterKontrolPipeline({
    muavinRows = [],
    yevmiyeRows = [],
    mizanRows = [],
    edefterListeRows = [],
    xmlRows = [],
    technicalFindings = [],
    companyId = "",
    period = "",
    declarationRecords = [],
    accountPlanCodes = null,
    beratMeta = null,
    packageMeta = {},
    companyTaxId = "",
    coreDecision = null,
    retryToken = "",
    yevmiyeEvidencePresent,
    cumulativePeriod = false
  }) {
    const mergedRows = [
      ...muavinRows,
      ...yevmiyeRows,
      ...edefterListeRows,
      ...mizanRows,
      ...xmlRows
    ];
    const yevmiyeOnly = mergedRows.filter(
      (row) => row.kaynak === E_DEFTER_KAYNAK.YEVMIYE || row.kaynak === E_DEFTER_KAYNAK.YEVMIYE_XML
    );
    const kebirOnly = mergedRows.filter((row) => row.kaynak === E_DEFTER_KAYNAK.KEBIR_XML);
    const analyzedRows = analyzeEDefterRows(mergedRows, {
      accountPlanCodes,
      expectedPeriod: period,
      yevmiyeEvidencePresent: yevmiyeEvidencePresent === true || yevmiyeEvidencePresent !== false && yevmiyeOnly.length > 0,
      cumulativePeriod
    });
    const journalLedger = reconcileJournalLedger(
      yevmiyeOnly.length ? yevmiyeOnly : mergedRows.filter((r) => r.kaynak !== E_DEFTER_KAYNAK.KEBIR_XML && r.kaynak !== E_DEFTER_KAYNAK.MIZAN),
      kebirOnly
    );
    const totals = sumDebitCredit(
      mergedRows.filter((row) => row.kaynak !== E_DEFTER_KAYNAK.MIZAN)
    );
    const totalFindings = [];
    if (mergedRows.some((r) => r.kaynak !== E_DEFTER_KAYNAK.MIZAN) && Math.abs(totals.borc - totals.alacak) > BORC_ALACAK_TOLERANCE) {
      totalFindings.push({
        code: E_DEFTER_FINDING_CODE.TOPLAM_BORC_ALACAK,
        message: `Toplam bor\xE7/alacak e\u015Fit de\u011Fil: bor\xE7=${totals.borc}, alacak=${totals.alacak}`,
        level: E_DEFTER_SONUC_SEVIYE.KRITIK
      });
    }
    const beratFindings = analyzeBeratMeta(beratMeta, packageMeta, {
      companyTaxId,
      expectedPeriod: period
    });
    const technicalRows = buildTechnicalFindingRows(
      [...technicalFindings, ...beratFindings, ...totalFindings],
      { companyId, period }
    );
    const crossRows = buildCrossFindingRows(journalLedger.findings, { companyId, period });
    const vergiselRows = buildVergiselFindingRows({
      rows: mergedRows,
      declarationRecords: declarationRecords.length ? declarationRecords : typeof window !== "undefined" ? loadDeclarationAccrualRecords().filter((record) => !companyId || record.companyId === companyId) : [],
      companyId,
      period
    });
    const combinedRows = [...analyzedRows, ...technicalRows, ...crossRows, ...vergiselRows].map(
      (row) => ({
        ...row,
        sonucSeviye: row.sonucSeviye || mapLegacyLevelToSonuc(row.riskLevel) || sonucSeviyeFromScore(row.riskScore || 0)
      })
    );
    const summary = recalculateEDefterSummary(combinedRows);
    summary.yuklenenDefterSayisi = Number(Boolean(muavinRows.length)) + Number(Boolean(yevmiyeRows.length)) + Number(Boolean(mizanRows.length)) + Number(Boolean(xmlRows.length));
    const overallSonuc = resolveOverallSonuc(combinedRows);
    summary.overallSonuc = overallSonuc;
    summary.edefterUygun = resolveEdefterUygun(combinedRows, overallSonuc);
    summary.findingCount = combinedRows.filter(
      (row) => !row.disaridaBirak && (Array.isArray(row.issueDetails) && row.issueDetails.length > 0 || Array.isArray(row.issues) && row.issues.length > 0 || row.grup !== E_DEFTER_KONTROL_GRUP.HATASIZ)
    ).length;
    summary.canApproveExport = canApproveEDefterExport(overallSonuc);
    const groupCounts = groupEDefterCounts(combinedRows);
    const hooks = buildEDefterIntegrationHooks({
      rows: combinedRows,
      companyId,
      coreDecision
    });
    return {
      rows: combinedRows,
      summary,
      groupCounts,
      overallSonuc,
      journalLedger,
      retryToken: retryToken || `retry-${Date.now()}`,
      disclaimer: E_DEFTER_REPORT_DISCLAIMER,
      integrationMeta: {
        source: "e-defter-kontrol-v3",
        fisNolar: combinedRows.map((row) => row.fisNo).filter(Boolean),
        hesapKodlari: combinedRows.map((row) => row.hesapKodu).filter(Boolean),
        belgeNolar: combinedRows.map((row) => row.belgeNo).filter(Boolean),
        kdvMatrahKontrolReady: true,
        kurDegerlemeReady: true,
        lucaAktarimKontrolReady: true,
        muavinMutabakatReady: true,
        ...hooks
      }
    };
  }
  async function runOneClickEDefterKontrol({
    parsedUpload = null,
    muavinRows = [],
    yevmiyeRows = [],
    mizanRows = [],
    edefterListeRows = [],
    companyId = "",
    companyTaxId = "",
    period = "",
    accountPlanCodes = null,
    declarationRecords = [],
    coreDecision = null,
    fingerprintSession = null
  } = {}) {
    if (parsedUpload?.duplicate) {
      return {
        duplicate: true,
        duplicateMessage: parsedUpload.duplicateMessage,
        rows: [],
        summary: {
          overallSonuc: E_DEFTER_SONUC_SEVIYE.BILGI,
          edefterUygun: false,
          canApproveExport: false,
          kritikHata: 0,
          uyariSayisi: 0,
          toplamSatir: 0,
          toplamFis: 0,
          yuklenenDefterSayisi: 0,
          teknikHata: 0,
          vergiselRisk: 0,
          yuksekRisk: 0,
          mukerrerRisk: 0,
          tersBakiye: 0,
          eksikBilgi: 0
        },
        groupCounts: [],
        overallSonuc: E_DEFTER_SONUC_SEVIYE.BILGI,
        disclaimer: E_DEFTER_REPORT_DISCLAIMER
      };
    }
    const fileTax = parsedUpload?.packageMeta?.taxId || "";
    const filePeriod = parsedUpload?.packageMeta?.period || "";
    const hasXmlPackage = Boolean(parsedUpload && !parsedUpload.duplicate);
    const hasExcelOnly = !hasXmlPackage && Boolean(muavinRows.length || yevmiyeRows.length || mizanRows.length || edefterListeRows.length);
    const sourceKind = hasXmlPackage ? "xml" : hasExcelOnly ? "excel" : "unknown";
    const identity = evaluateEDefterCompanyIdentity({
      companyTaxId,
      documentTaxId: fileTax,
      companyId,
      sourceKind
    });
    if (identity.blocking || !identity.allowAnalyze) {
      const err = new Error(identity.safeMessage);
      err.code = identityStatusToErrorCode(identity.status) || EDEFTER_ERROR_CODE.COMPANY_MISMATCH;
      err.identity = {
        status: identity.status,
        verified: identity.verified,
        matched: identity.matched,
        safeMessage: identity.safeMessage,
        safeFingerprint: identity.safeFingerprint
      };
      throw err;
    }
    let resolvedPeriod = period;
    if (!resolvedPeriod && filePeriod) {
      resolvedPeriod = filePeriod.replace("-", "/");
    } else if (resolvedPeriod && filePeriod) {
      const a = normalizePeriodKey(resolvedPeriod.replace("/", "-"));
      const b = normalizePeriodKey(filePeriod);
      if (a && b && a !== b) {
        const err = new Error("Dosya d\xF6nemi ile se\xE7ili d\xF6nem uyu\u015Fmuyor.");
        err.code = EDEFTER_ERROR_CODE.MIXED_COMPANY_OR_PERIOD;
        throw err;
      }
    }
    if (fingerprintSession && parsedUpload?.fingerprint) {
      fingerprintSession.add(parsedUpload.fingerprint);
    }
    const result = runEDefterKontrolPipeline({
      muavinRows,
      yevmiyeRows,
      mizanRows,
      edefterListeRows,
      xmlRows: parsedUpload?.rows || [],
      technicalFindings: parsedUpload?.technicalFindings || [],
      companyId,
      period: resolvedPeriod,
      declarationRecords,
      accountPlanCodes,
      beratMeta: parsedUpload?.beratMeta || null,
      packageMeta: parsedUpload?.packageMeta || {},
      companyTaxId,
      coreDecision
    });
    result.summary = applyIdentityGateToSummary(result.summary, identity);
    result.identity = {
      status: identity.status,
      verified: identity.verified,
      identityVerified: Boolean(identity.verified),
      matched: identity.matched,
      userConfirmed: Boolean(identity.userConfirmed),
      identityUserConfirmed: Boolean(identity.userConfirmed),
      reviewRequired: identity.reviewRequired,
      allowAnalyze: identity.allowAnalyze,
      allowPersist: identity.allowPersist,
      allowExport: identity.allowExport,
      confirmation: identity.confirmation || "",
      safeMessage: identity.safeMessage,
      safeFingerprint: identity.safeFingerprint,
      companyIdentityType: identity.companyIdentityType,
      documentIdentityType: identity.documentIdentityType,
      sourceKind: identity.sourceKind
    };
    if (!identity.allowExport) {
      result.summary.canApproveExport = false;
    }
    return result;
  }
  function scoreFromLevel(level = "") {
    const mapped = mapLegacyLevelToSonuc(level);
    if (mapped === E_DEFTER_SONUC_SEVIYE.KRITIK || level === E_DEFTER_RISK_LEVEL.KRITIK) return 85;
    if (mapped === E_DEFTER_SONUC_SEVIYE.UYARI || level === E_DEFTER_RISK_LEVEL.YUKSEK) return 65;
    if (mapped === E_DEFTER_SONUC_SEVIYE.BILGI || level === E_DEFTER_RISK_LEVEL.ORTA) return 40;
    return 15;
  }
  function buildSmartEDefterExplanation(row = {}, issues = []) {
    const issueText = issues.join(" ").toLocaleLowerCase("tr-TR");
    const why = issueText.includes("kdv") || issueText.includes("191") || issueText.includes("391") ? "KDV hesaplar\u0131 ile beyanname/matrah aras\u0131nda tutars\u0131zl\u0131k olu\u015Fmu\u015F olabilir." : issueText.includes("kasa") || issueText.includes("100") ? "Nakit hareketlerinin tamam\u0131 bankaya aktar\u0131lmam\u0131\u015F veya kay\u0131t d\u0131\u015F\u0131 i\u015Flem olabilir." : issueText.includes("m\xFCkerrer") || issueText.includes("mukerrer") ? "Ayn\u0131 belge veya fi\u015F birden fazla kez i\u015Flenmi\u015F olabilir." : issueText.includes("xml") || issueText.includes("berat") ? "E-defter dosya yap\u0131s\u0131 veya berat e\u015Fle\u015Fmesinde teknik sorun olabilir." : "Kay\u0131t, belge veya d\xF6nemlendirme hatas\u0131 s\xF6z konusu olabilir.";
    const check = issueText.includes("sgk") || issueText.includes("361") ? "SGK tahakkuk fi\u015Fi, bordro ve banka \xF6deme dekontunu kontrol edin." : issueText.includes("belge") ? "\u0130lgili fatura, fi\u015F, dekont ve yevmiye kayd\u0131n\u0131 birlikte inceleyin." : "Muavin d\xF6k\xFCm\xFC, yevmiye fi\u015Fi ve destekleyici belgeleri kar\u015F\u0131la\u015Ft\u0131r\u0131n.";
    const effect = issueText.includes("kdv") || issueText.includes("vergi") ? "Vergi beyan\u0131 ve e-defter berat s\xFCrecinde red veya ek a\xE7\u0131klama istenebilir." : issueText.includes("m\xFCkerrer") || issueText.includes("mukerrer") ? "\xC7ift gider veya \xE7ift gelir beyan\u0131 riski do\u011Fabilir." : "E-defter berat \xF6ncesi d\xFCzeltme gerekmeden s\xFCre\xE7 tamamlanmayabilir.";
    return [
      `Sorun neden olu\u015Fmu\u015F olabilir? ${why}`,
      `Hangi belge kontrol edilmeli? ${check}`,
      `Olas\u0131 vergisel etkisi: ${effect}`,
      row.aciklama ? `Kay\u0131t: ${row.aciklama}` : ""
    ].filter(Boolean).join("\n");
  }
  function buildTechnicalFindingRows(findings = [], context = {}) {
    return (findings || []).map((finding, index) => {
      const sonucSeviye = mapLegacyLevelToSonuc(finding.level) || E_DEFTER_SONUC_SEVIYE.BILGI;
      const riskScore = scoreFromLevel(finding.level);
      const issue = normalizeEDefterIssue(
        {
          code: finding.code || E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
          message: finding.message,
          severity: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_ISSUE_SEVERITY.KRITIK : sonucSeviye === E_DEFTER_SONUC_SEVIYE.UYARI ? E_DEFTER_ISSUE_SEVERITY.UYARI : E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_KONTROL_GRUP.KRITIK : E_DEFTER_KONTROL_GRUP.TEKNIK,
          blocking: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK,
          riskScore
        },
        "technical"
      );
      const row = {
        id: `teknik-${finding.code || index}-${Date.now()}`,
        kaynak: E_DEFTER_KAYNAK.TEKNIK,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "Teknik Kontrol",
        aciklama: finding.message,
        belgeTuru: "Teknik",
        belgeNo: finding.code || "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issues: [finding.message],
        issueDetails: [issue],
        riskScore,
        riskBand: riskBandFromScore(riskScore),
        riskLevel: finding.level || riskLevelFromScore(riskScore),
        sonucSeviye,
        hataTuru: E_DEFTER_HATA_TURU.TEKNIK,
        onerilenKontrol: "XML/ZIP dosyas\u0131n\u0131 ve berat e\u015Fle\u015Fmesini yeniden kontrol edin.",
        cozumDurumu: E_DEFTER_FINDING_STATUS.YENI,
        grup: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_KONTROL_GRUP.KRITIK : sonucSeviye === E_DEFTER_SONUC_SEVIYE.UYARI ? E_DEFTER_KONTROL_GRUP.TEKNIK : E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        durum: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_KONTROL_DURUM.KRITIK : E_DEFTER_KONTROL_DURUM.INCELEME_GEREKLI,
        companyId: context.companyId || "",
        period: context.period || ""
      };
      row.smartExplanation = buildSmartEDefterExplanation(row, row.issues);
      return row;
    });
  }
  function sumAccountPrefix(rows = [], prefix) {
    return rows.filter((row) => String(row.hesapKodu || "").startsWith(prefix)).reduce((sum, row) => sum + roundMoney2(Math.max(row.borc, row.alacak)), 0);
  }
  function buildVergiselFindingRows({ rows = [], declarationRecords = [], companyId = "", period = "" }) {
    const findings = [];
    const taxRows = rows.some(
      (row) => row.kaynak === E_DEFTER_KAYNAK.YEVMIYE || row.kaynak === E_DEFTER_KAYNAK.YEVMIYE_XML
    ) ? rows.filter(
      (row) => row.kaynak === E_DEFTER_KAYNAK.YEVMIYE || row.kaynak === E_DEFTER_KAYNAK.YEVMIYE_XML
    ) : rows.filter((row) => row.kaynak !== E_DEFTER_KAYNAK.KEBIR_XML);
    const kdv191 = sumAccountPrefix(taxRows, "191");
    const kdv391 = sumAccountPrefix(taxRows, "391");
    const kdv360 = sumAccountPrefix(taxRows, "360");
    const sgk361 = sumAccountPrefix(taxRows, "361");
    const devreden190 = sumAccountPrefix(taxRows, "190");
    if (Math.abs(kdv191 - kdv391) > 1e3 && (kdv191 || kdv391)) {
      findings.push({
        message: `191/391 KDV d\xF6nem hareket fark\u0131 (inceleme bilgisi): 191=${kdv191.toLocaleString("tr-TR")} TL, 391=${kdv391.toLocaleString("tr-TR")} TL`,
        level: E_DEFTER_RISK_LEVEL.ORTA,
        code: "KDV_191_391",
        action: "KDV listesi ve hesap hareketlerini kar\u015F\u0131la\u015Ft\u0131r\u0131n."
      });
    }
    if (devreden190 > KASA_BAKIYE_ESIK) {
      findings.push({
        message: `Devreden KDV s\xFCreklilik analizi (inceleme bilgisi): 190 hesab\u0131 ${devreden190.toLocaleString("tr-TR")} TL`,
        level: E_DEFTER_RISK_LEVEL.ORTA,
        code: "DEVREDEN_KDV",
        action: "KDV beyannamesi ve indirilecek KDV listesini inceleyin."
      });
    }
    const declarationKdv = declarationRecords.filter((record) => record.type === "KDV").filter((record) => !period || record.period === period).reduce((sum, record) => sum + Number(record.totalPayment || 0), 0);
    if (declarationKdv && Math.abs(kdv360 - declarationKdv) > 1e3) {
      findings.push({
        message: `Beyanname ile muhasebe fark\u0131 (KDV): mizan 360=${kdv360.toLocaleString("tr-TR")} TL, tahakkuk=${declarationKdv.toLocaleString("tr-TR")} TL`,
        level: E_DEFTER_RISK_LEVEL.KRITIK,
        code: "BEYANNAME_KDV",
        action: "KDV beyannamesi ve 360 hesap hareketlerini e\u015Fle\u015Ftirin."
      });
    }
    const declarationSgk = declarationRecords.filter((record) => record.type === "SGK").reduce((sum, record) => sum + Number(record.totalPayment || 0), 0);
    if (declarationSgk && Math.abs(sgk361 - declarationSgk) > 1e3) {
      findings.push({
        message: `SGK tahakkuk uyumu: 361=${sgk361.toLocaleString("tr-TR")} TL, tahakkuk=${declarationSgk.toLocaleString("tr-TR")} TL`,
        level: E_DEFTER_RISK_LEVEL.YUKSEK,
        code: "SGK_TAHAKKUK",
        action: "SGK tahakkuk fi\u015Fi ve bordro belgelerini kontrol edin."
      });
    }
    ["Damga Vergisi", "Konaklama Vergisi", "Turizm Pay\u0131"].forEach((type) => {
      const total = declarationRecords.filter((record) => record.type === type).reduce((sum, record) => sum + Number(record.totalPayment || 0), 0);
      if (total > 0) {
        findings.push({
          message: `${type} tahakkuk kayd\u0131 mevcut (${total.toLocaleString("tr-TR")} TL); muhasebe e\u015Fle\u015Fmesi kontrol edilmeli (inceleme bilgisi).`,
          level: E_DEFTER_RISK_LEVEL.ORTA,
          code: type.replace(/\s+/g, "_").toUpperCase(),
          action: `${type} beyan\u0131 ve ilgili hesap hareketlerini do\u011Frulay\u0131n.`
        });
      }
    });
    const tevkifatRows = taxRows.filter((row) => /tevkifat|stopaj/i.test(String(row.aciklama || "")));
    if (tevkifatRows.length) {
      findings.push({
        message: `${tevkifatRows.length} tevkifat/stopaj kayd\u0131 tespit edildi; oran ve hesap e\u015Fle\u015Fmesi kontrol edilmeli (inceleme bilgisi).`,
        level: E_DEFTER_RISK_LEVEL.ORTA,
        code: "TEVKIFAT",
        action: "Tevkifat beyannamesi ve stopaj hesaplar\u0131n\u0131 kar\u015F\u0131la\u015Ft\u0131r\u0131n."
      });
    }
    const sgdpRows = taxRows.filter(
      (row) => /sgdp|sosyal g[uü]venlik destek/i.test(`${row.aciklama || ""} ${row.hesapAdi || ""}`)
    );
    if (sgdpRows.length || sgk361 > 0 && /sgdp/i.test(taxRows.map((r) => r.aciklama).join(" "))) {
      findings.push({
        message: "361/SGDP risk g\xF6stergesi: SGDP prim kayd\u0131 ile 361 hesab\u0131 birlikte kontrol edilmeli (inceleme bilgisi; kesin vergi h\xFCkm\xFC de\u011Fildir).",
        level: E_DEFTER_RISK_LEVEL.ORTA,
        code: "SGDP_361",
        action: "SGDP bordro/tahakkuk ile 361 hareketlerini kar\u015F\u0131la\u015Ft\u0131r\u0131n."
      });
    }
    const kasaBanka = sumAccountPrefix(taxRows, "100") + sumAccountPrefix(taxRows, "102");
    if (kasaBanka > KASA_BAKIYE_ESIK * 2) {
      findings.push({
        message: "100/102 nakit-banka y\xFCksek hareket risk g\xF6stergesi (inceleme bilgisi).",
        level: E_DEFTER_RISK_LEVEL.ORTA,
        code: "KASA_BANKA_100_102",
        action: "Kasa ve banka mutabakat\u0131n\u0131 kontrol edin."
      });
    }
    return findings.map((finding, index) => {
      const sonucSeviye = mapLegacyLevelToSonuc(finding.level) || E_DEFTER_SONUC_SEVIYE.BILGI;
      const riskScore = scoreFromLevel(finding.level);
      const issue = normalizeEDefterIssue(
        {
          code: finding.code || E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE,
          message: finding.message,
          severity: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_ISSUE_SEVERITY.KRITIK : sonucSeviye === E_DEFTER_SONUC_SEVIYE.UYARI ? E_DEFTER_ISSUE_SEVERITY.UYARI : E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_KONTROL_GRUP.KRITIK : E_DEFTER_KONTROL_GRUP.VERGISEL,
          blocking: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK,
          riskScore
        },
        "vergisel"
      );
      const row = {
        id: `vergisel-${finding.code || index}`,
        kaynak: E_DEFTER_KAYNAK.VERGISEL,
        tarih: "",
        fisNo: "",
        yevmiyeNo: "",
        hesapKodu: "",
        hesapAdi: "Vergisel Kontrol",
        aciklama: finding.message,
        belgeTuru: "Vergisel",
        belgeNo: finding.code || "",
        belgeTarihi: "",
        borc: 0,
        alacak: 0,
        cariUnvan: "",
        tutar: 0,
        kontrolDurumu: "",
        not: "",
        duzeltildiMi: false,
        disaridaBirak: false,
        manuallyEdited: false,
        issues: [finding.message],
        issueDetails: [issue],
        riskScore,
        riskBand: riskBandFromScore(riskScore),
        riskLevel: finding.level,
        sonucSeviye,
        hataTuru: E_DEFTER_HATA_TURU.VERGISEL,
        onerilenKontrol: finding.action,
        cozumDurumu: E_DEFTER_FINDING_STATUS.YENI,
        grup: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_KONTROL_GRUP.KRITIK : E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
        durum: sonucSeviye === E_DEFTER_SONUC_SEVIYE.KRITIK ? E_DEFTER_KONTROL_DURUM.KRITIK : E_DEFTER_KONTROL_DURUM.INCELEME_GEREKLI,
        companyId,
        period
      };
      row.smartExplanation = buildSmartEDefterExplanation(row, row.issues);
      return row;
    });
  }

  // src/utils/genelMuhasebeFindingsLabels.js
  var GENEL_MUHASEBE_FINDING_TITLE_TR = {
    [E_DEFTER_ISSUE_CODE.MULTI_COUNTERPART]: "Bile\u015Fik fi\u015F",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE]: "Ayn\u0131 y\xF6nl\xFC kay\u0131t",
    [E_DEFTER_ISSUE_CODE.SUSPICIOUS_ROUNDING]: "\u015E\xFCpheli yuvarlama kayd\u0131",
    [E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE]: "Hesap inceleme bilgisi",
    [E_DEFTER_ISSUE_CODE.ACCOUNT_NOT_IN_PLAN]: "Hesap plan\u0131nda bulunamad\u0131",
    [E_DEFTER_ISSUE_CODE.MISSING_DOCUMENT_INFO]: "Belge bilgisi eksik",
    [E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_EVIDENCE_MISSING]: "Mizan y\xFCklenmedi",
    [E_DEFTER_ISSUE_CODE.PLAN_EVIDENCE_MISSING]: "Hesap plan\u0131 kan\u0131t\u0131 bulunamad\u0131",
    [E_DEFTER_ISSUE_CODE.MUAVIN_YEVMIYE_MISMATCH]: "Muavin\u2013yevmiye fark\u0131",
    [E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_MISMATCH]: "Muavin\u2013mizan fark\u0131",
    [E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART]: "Kar\u015F\u0131t hesap bulunamad\u0131",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_SELF]: "Kendine kar\u015F\u0131t hesap",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW]: "Kar\u015F\u0131t hesap incelemesi",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT]: "Kar\u015F\u0131t hesap \xE7eli\u015Fkisi",
    [E_DEFTER_ISSUE_CODE.MISSING_DESCRIPTION]: "A\xE7\u0131klama eksik",
    [E_DEFTER_ISSUE_CODE.DATE_OUT_OF_PERIOD]: "D\xF6nem d\u0131\u015F\u0131 kay\u0131t",
    [E_DEFTER_ISSUE_CODE.NEGATIVE_AMOUNT]: "Negatif tutar",
    [E_DEFTER_ISSUE_CODE.DEBIT_CREDIT_MISMATCH]: "Bor\xE7\u2013alacak uyumsuzlu\u011Fu",
    [E_DEFTER_ISSUE_CODE.DUPLICATE_ENTRY]: "M\xFCkerrer kay\u0131t bilgisi",
    [E_DEFTER_ISSUE_CODE.ZERO_AMOUNT]: "S\u0131f\u0131r tutarl\u0131 kay\u0131t",
    [E_DEFTER_ISSUE_CODE.DOCUMENT_DATE_GAP]: "Belge tarihi fark\u0131",
    [E_DEFTER_ISSUE_CODE.JOURNAL_SEQUENCE_GAP]: "Yevmiye s\u0131ra bo\u015Flu\u011Fu"
  };
  var GENEL_MUHASEBE_FINDING_MESSAGE_TR = {
    [E_DEFTER_ISSUE_CODE.MULTI_COUNTERPART]: "Bu fi\u015Fte bir hesap sat\u0131r\u0131 kar\u015F\u0131 y\xF6ndeki birden fazla hesapla birlikte \xE7al\u0131\u015Fmaktad\u0131r. Bu durum tek ba\u015F\u0131na hata de\u011Fildir.",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE]: "Ayn\u0131 fi\u015Fte yaln\u0131z ayn\u0131 y\xF6nl\xFC kay\u0131tlar bulundu\u011Fu i\xE7in kar\u015F\u0131t hesap belirlenemedi.",
    [E_DEFTER_ISSUE_CODE.SUSPICIOUS_ROUNDING]: "Tutar yuvarlama a\xE7\u0131s\u0131ndan inceleme gerektirebilir.",
    [E_DEFTER_ISSUE_CODE.UNKNOWN_ISSUE]: "Hesap sat\u0131r\u0131 inceleme bilgisi i\xE7eriyor.",
    [E_DEFTER_ISSUE_CODE.ACCOUNT_NOT_IN_PLAN]: "Bu hesap kodu y\xFCklenen hesap plan\u0131nda bulunamad\u0131.",
    [E_DEFTER_ISSUE_CODE.MISSING_DOCUMENT_INFO]: "Belge bilgisi eksik veya tamamlanmam\u0131\u015F.",
    [E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_EVIDENCE_MISSING]: "Mizan dosyas\u0131 y\xFCklenmedi\u011Fi i\xE7in muavin\u2013mizan mutabakat\u0131 yap\u0131lamad\u0131.",
    [E_DEFTER_ISSUE_CODE.PLAN_EVIDENCE_MISSING]: "Hesap plan\u0131 kan\u0131t\u0131 bulunamad\u0131\u011F\u0131 i\xE7in plan kontrol\xFC yap\u0131lmad\u0131.",
    [E_DEFTER_ISSUE_CODE.MUAVIN_YEVMIYE_MISMATCH]: "Muavin ve yevmiye kay\u0131tlar\u0131 aras\u0131nda fark tespit edildi.",
    [E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_MISMATCH]: "Muavin ve mizan hesaplar\u0131 aras\u0131nda fark tespit edildi.",
    [E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART]: "Ayn\u0131 fi\u015Fte kar\u015F\u0131t hesap baca\u011F\u0131 bulunamad\u0131.",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_SELF]: "Kar\u015F\u0131t hesap olarak ayn\u0131 hesap kodu g\xF6r\xFCnd\xFC; inceleme gerekli.",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW]: "Kar\u015F\u0131t hesap ba\u011Flant\u0131s\u0131 inceleme gerektiriyor.",
    [E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT]: "Excel kar\u015F\u0131 hesap ile fi\u015F bacaklar\u0131ndan hesaplanan kar\u015F\u0131t \xE7eli\u015Fiyor.",
    [E_DEFTER_ISSUE_CODE.MISSING_DESCRIPTION]: "A\xE7\u0131klama alan\u0131 bo\u015F.",
    [E_DEFTER_ISSUE_CODE.DATE_OUT_OF_PERIOD]: "Kay\u0131t tarihi se\xE7ilen d\xF6nem d\u0131\u015F\u0131nda.",
    [E_DEFTER_ISSUE_CODE.NEGATIVE_AMOUNT]: "Negatif tutarl\u0131 sat\u0131r tespit edildi.",
    [E_DEFTER_ISSUE_CODE.DEBIT_CREDIT_MISMATCH]: "Bor\xE7 ve alacak tutarlar\u0131 dengeli de\u011Fil.",
    [E_DEFTER_ISSUE_CODE.DUPLICATE_ENTRY]: "Benzer cari, tutar ve yak\u0131n tarih \u2014 inceleme bilgisi.",
    [E_DEFTER_ISSUE_CODE.ZERO_AMOUNT]: "S\u0131f\u0131r tutarl\u0131 sat\u0131r tespit edildi.",
    [E_DEFTER_ISSUE_CODE.DOCUMENT_DATE_GAP]: "Belge tarihi ile fi\u015F tarihi aras\u0131nda fark var.",
    [E_DEFTER_ISSUE_CODE.JOURNAL_SEQUENCE_GAP]: "Yevmiye numaralar\u0131nda s\u0131ra bo\u015Flu\u011Fu var."
  };

  // src/utils/correctionRecords/correctionRecordTypes.js
  var CORRECTION_RECORD_ERROR = {
    UNAUTHORIZED: "CORRECTION_RECORD_UNAUTHORIZED",
    INVALID: "CORRECTION_RECORD_INVALID",
    CONFLICT: "CORRECTION_RECORD_CONFLICT",
    NOT_FOUND: "CORRECTION_RECORD_NOT_FOUND",
    EXPORT_FAILED: "CORRECTION_RECORD_EXPORT_FAILED",
    APPLY_FAILED: "CORRECTION_RECORD_APPLY_FAILED"
  };
  var CORRECTION_RECORD_USER_MESSAGE_TR = {
    [CORRECTION_RECORD_ERROR.UNAUTHORIZED]: "Bu firmaya eri\u015Fim yetkiniz yok.",
    [CORRECTION_RECORD_ERROR.INVALID]: "D\xFCzeltme kayd\u0131 do\u011Frulanamad\u0131.",
    [CORRECTION_RECORD_ERROR.CONFLICT]: "Bu d\xFCzeltme zaten kay\u0131tl\u0131.",
    [CORRECTION_RECORD_ERROR.NOT_FOUND]: "D\xFCzeltme kayd\u0131 bulunamad\u0131.",
    [CORRECTION_RECORD_ERROR.EXPORT_FAILED]: "D\xFCzeltme export kayd\u0131 olu\u015Fturulamad\u0131.",
    [CORRECTION_RECORD_ERROR.APPLY_FAILED]: "D\xFCzeltme uygulama kayd\u0131 g\xFCncellenemedi."
  };

  // src/utils/voucherResultGroups.js
  var SEVERITY_PRIORITY = {
    [E_DEFTER_ISSUE_SEVERITY.KRITIK]: 0,
    HATA: 0,
    [E_DEFTER_ISSUE_SEVERITY.UYARI]: 1,
    [E_DEFTER_ISSUE_SEVERITY.BILGI]: 2
  };

  // src/utils/genelMuhasebeFindingsView.js
  function isSyntheticSystemFindingRow(row = {}) {
    const id = String(row?.id || "");
    return id.startsWith("donem-sonu") || id.startsWith("fis-gap") || id.startsWith("teknik-") || id.startsWith("vergisel-") || id.startsWith("capraz-") || id.startsWith("cross-");
  }
  var SEVERITY_RANK = {
    [E_DEFTER_ISSUE_SEVERITY.KRITIK]: 0,
    HATA: 0,
    [E_DEFTER_ISSUE_SEVERITY.UYARI]: 1,
    [E_DEFTER_ISSUE_SEVERITY.BILGI]: 2,
    [E_DEFTER_ISSUE_SEVERITY.UYGUN]: 3
  };
  var SONUC_RANK = {
    [E_DEFTER_SONUC_SEVIYE.KRITIK]: 3,
    [E_DEFTER_SONUC_SEVIYE.UYARI]: 2,
    [E_DEFTER_SONUC_SEVIYE.BILGI]: 1,
    [E_DEFTER_SONUC_SEVIYE.UYGUN]: 0
  };
  var REVIEW_ISSUE_CODES = /* @__PURE__ */ new Set([
    E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART,
    E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE,
    E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW,
    E_DEFTER_ISSUE_CODE.COUNTERPART_SELF,
    E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT,
    E_DEFTER_ISSUE_CODE.MUAVIN_YEVMIYE_MISMATCH,
    E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_MISMATCH
  ]);
  function issueToSonuc(severity = "") {
    if (severity === E_DEFTER_ISSUE_SEVERITY.KRITIK || severity === "HATA") {
      return E_DEFTER_SONUC_SEVIYE.KRITIK;
    }
    if (severity === E_DEFTER_ISSUE_SEVERITY.UYARI) return E_DEFTER_SONUC_SEVIYE.UYARI;
    if (severity === E_DEFTER_ISSUE_SEVERITY.BILGI) return E_DEFTER_SONUC_SEVIYE.BILGI;
    return E_DEFTER_SONUC_SEVIYE.UYGUN;
  }
  function normalizeFinding(raw = {}, source = "row") {
    return {
      source,
      fisNo: String(raw.fisNo || "").trim(),
      tarih: String(raw.tarih || "").trim(),
      hesapKodu: String(raw.hesapKodu || "").trim(),
      severity: raw.severity || E_DEFTER_ISSUE_SEVERITY.BILGI,
      code: raw.code || "",
      message: String(raw.message || "").trim(),
      statusLabel: String(raw.statusLabel || "").trim(),
      kaynak: raw.kaynak || "",
      groupKey: `${String(raw.fisNo || "").trim()}|${raw.code || ""}`
    };
  }
  function buildGenelMuhasebeFindingsCatalog({
    rows = [],
    findingExtras = [],
    includeSyntheticRows = false
  } = {}) {
    const catalog = [];
    for (const issue of findingExtras) {
      catalog.push(normalizeFinding(issue, "extra"));
    }
    for (const row of rows) {
      if (!includeSyntheticRows && isSyntheticSystemFindingRow(row)) continue;
      for (const issue of row.issueDetails || []) {
        catalog.push(
          normalizeFinding(
            {
              ...issue,
              fisNo: row.fisNo,
              tarih: row.tarih,
              hesapKodu: row.hesapKodu,
              kaynak: row.kaynak
            },
            "row"
          )
        );
      }
    }
    return catalog;
  }
  function countFindingsBySeverity(catalog = []) {
    const counts = {
      [E_DEFTER_ISSUE_SEVERITY.KRITIK]: 0,
      HATA: 0,
      [E_DEFTER_ISSUE_SEVERITY.UYARI]: 0,
      [E_DEFTER_ISSUE_SEVERITY.BILGI]: 0
    };
    for (const item of catalog) {
      const key = item.severity in counts ? item.severity : E_DEFTER_ISSUE_SEVERITY.BILGI;
      counts[key] = (counts[key] || 0) + 1;
    }
    return counts;
  }
  function countFindingsByCode(catalog = []) {
    const counts = /* @__PURE__ */ new Map();
    for (const item of catalog) {
      const code = item.code || "UNKNOWN";
      counts.set(code, (counts.get(code) || 0) + 1);
    }
    return Object.fromEntries(
      [...counts.entries()].sort((left, right) => right[1] - left[1])
    );
  }
  function summarizeGenelMuhasebeFindingsCatalog(catalog = []) {
    let overallSonuc = E_DEFTER_SONUC_SEVIYE.UYGUN;
    let incelemeGerekli = 0;
    for (const item of catalog) {
      const mapped = issueToSonuc(item.severity);
      if ((SONUC_RANK[mapped] || 0) > (SONUC_RANK[overallSonuc] || 0)) {
        overallSonuc = mapped;
      }
      if (item.severity !== E_DEFTER_ISSUE_SEVERITY.BILGI) {
        incelemeGerekli += 1;
      }
    }
    return {
      overallSonuc,
      incelemeGerekli,
      severityCounts: countFindingsBySeverity(catalog),
      codeCounts: countFindingsByCode(catalog)
    };
  }

  // src/utils/genelMuhasebeKontrolEngine.js
  var GENEL_MUHASEBE_DOC_CLASS = {
    MUAVIN: "MUAVIN",
    YEVMIYE: "YEVMIYE",
    MIZAN: "MIZAN",
    E_DEFTER_XML: "E_DEFTER_XML",
    UNKNOWN: "UNKNOWN"
  };
  function compact(value) {
    return normalizeParserText(value).replace(/\s+/g, "");
  }
  function compactAccountCode(value) {
    return normalizeAccountCodeForComparison(value);
  }
  function roundMoney3(value) {
    return Number(Number(value || 0).toFixed(2));
  }
  function headerCorpus(sheetRows = []) {
    return (sheetRows.slice(0, 12) || []).map((row) => (row || []).map((cell) => String(cell || "")).join(" ")).join(" | ").toLocaleLowerCase("tr-TR");
  }
  function sampleAccountCodes(sheetRows = [], limit = 40) {
    const codes = /* @__PURE__ */ new Set();
    for (const row of sheetRows.slice(1, 1 + limit)) {
      if (!Array.isArray(row)) continue;
      for (const cell of row) {
        const text = String(cell || "").trim();
        if (/^\d{3,}([./]\d{1,4}){0,5}$/.test(text)) codes.add(compactAccountCode(text));
      }
      if (codes.size > 8) break;
    }
    return codes;
  }
  function classifyLedgerDocumentType(sheetRows = [], hints = {}) {
    if (hints.forceXml || hints.isXml) return GENEL_MUHASEBE_DOC_CLASS.E_DEFTER_XML;
    if (detectLucaMultiAccountMuavinLayout(sheetRows)) {
      return GENEL_MUHASEBE_DOC_CLASS.MUAVIN;
    }
    if (detectYevmiyeLayout(sheetRows) === YEVMIYE_LAYOUT.REPEATED_JOURNAL_BLOCK) {
      return GENEL_MUHASEBE_DOC_CLASS.YEVMIYE;
    }
    const head = headerCorpus(sheetRows);
    const has = (s) => head.includes(s);
    const hasBakiye = has("bor\xE7 bakiyesi") || has("borc bakiyesi") || has("alacak bakiyesi") || has("bor\xE7 bak.") || has("alacak bak.");
    const hasHesap = has("hesap");
    const hasBorcAlacak = (has("bor\xE7") || has("borc")) && has("alacak");
    const hasTarih = has("tarih");
    const hasFis = has("fi\u015F no") || has("fis no") || has("yevmiye");
    const hasYevmiyeHint = has("yevmiye no") || has("yevmiye fi\u015F") || has("yevmiye") && !has("muavin");
    const hasMuavinHint = has("muavin") || has("hesap hareket") || has("hareket d\xF6k\xFCm");
    const accountCodes = sampleAccountCodes(sheetRows);
    const singleAccountExtract = accountCodes.size === 1;
    if (hasBakiye && hasHesap && !hasFis) return GENEL_MUHASEBE_DOC_CLASS.MIZAN;
    if (hasYevmiyeHint && hasBorcAlacak) return GENEL_MUHASEBE_DOC_CLASS.YEVMIYE;
    if (hasMuavinHint && hasBorcAlacak) return GENEL_MUHASEBE_DOC_CLASS.MUAVIN;
    if (hasBorcAlacak && hasTarih && hasFis) {
      if (singleAccountExtract && !has("yevmiye no")) return GENEL_MUHASEBE_DOC_CLASS.MUAVIN;
      return GENEL_MUHASEBE_DOC_CLASS.YEVMIYE;
    }
    if (hasBorcAlacak && hasHesap && !hasTarih) return GENEL_MUHASEBE_DOC_CLASS.MIZAN;
    return GENEL_MUHASEBE_DOC_CLASS.UNKNOWN;
  }
  function refineDocumentClass(contentClass, preferredHint = "", sheetRows = []) {
    if (contentClass === GENEL_MUHASEBE_DOC_CLASS.YEVMIYE || contentClass === GENEL_MUHASEBE_DOC_CLASS.MIZAN || contentClass === GENEL_MUHASEBE_DOC_CLASS.E_DEFTER_XML || contentClass === GENEL_MUHASEBE_DOC_CLASS.MUAVIN) {
      return contentClass;
    }
    if (preferredHint === GENEL_MUHASEBE_DOC_CLASS.MIZAN) {
      return GENEL_MUHASEBE_DOC_CLASS.MIZAN;
    }
    if (preferredHint === GENEL_MUHASEBE_DOC_CLASS.MUAVIN) {
      const accounts = sampleAccountCodes(sheetRows);
      if (accounts.size <= 1) return GENEL_MUHASEBE_DOC_CLASS.MUAVIN;
    }
    return GENEL_MUHASEBE_DOC_CLASS.YEVMIYE;
  }
  function isValidYevmiyeMovementRow(row = {}) {
    return Boolean(String(row.hesapKodu || "").trim() && parseDateTR(row.tarih));
  }
  function parseLedgerByDocumentClass(sheetRows = [], documentClass = "") {
    const cls = documentClass || classifyLedgerDocumentType(sheetRows);
    if (cls === GENEL_MUHASEBE_DOC_CLASS.MIZAN) {
      return { documentClass: cls, rows: parseMizanSheet(sheetRows).map((r) => ({ ...r, documentClass: cls })) };
    }
    if (cls === GENEL_MUHASEBE_DOC_CLASS.MUAVIN) {
      return {
        documentClass: cls,
        rows: parseMuavinSheet(sheetRows).map((r) => ({ ...r, documentClass: cls, kaynak: E_DEFTER_KAYNAK.MUAVIN }))
      };
    }
    if (cls === GENEL_MUHASEBE_DOC_CLASS.YEVMIYE) {
      return {
        documentClass: cls,
        rows: parseYevmiyeSheet(sheetRows).map((r) => ({ ...r, documentClass: cls, kaynak: E_DEFTER_KAYNAK.YEVMIYE }))
      };
    }
    const yev = parseYevmiyeSheet(sheetRows);
    if (yev.length) {
      return {
        documentClass: GENEL_MUHASEBE_DOC_CLASS.UNKNOWN,
        rows: yev.map((r) => ({ ...r, documentClass: GENEL_MUHASEBE_DOC_CLASS.UNKNOWN, kaynak: E_DEFTER_KAYNAK.YEVMIYE }))
      };
    }
    const mizan = parseMizanSheet(sheetRows);
    return {
      documentClass: GENEL_MUHASEBE_DOC_CLASS.UNKNOWN,
      rows: mizan.map((r) => ({ ...r, documentClass: GENEL_MUHASEBE_DOC_CLASS.UNKNOWN }))
    };
  }
  function accountCodeFromPlanRow(account) {
    return String(
      account?.account_code || account?.accountCode || account?.hesapKodu || account?.code || ""
    ).trim();
  }
  function buildAccountPlanCodeSet(accounts = []) {
    const set = /* @__PURE__ */ new Set();
    for (const account of accounts || []) {
      const code = accountCodeFromPlanRow(account);
      if (!code) continue;
      set.add(code);
      set.add(compactAccountCode(code));
      const short = code.split(".")[0];
      if (short) set.add(short);
    }
    return set;
  }
  function isSyntheticSystemFindingRow2(row = {}) {
    const id = String(row?.id || "");
    return id.startsWith("donem-sonu") || id.startsWith("fis-gap") || id.startsWith("teknik-") || id.startsWith("vergisel-") || id.startsWith("capraz-") || id.startsWith("cross-");
  }
  function reconcileMizanMuavin({
    muavinRows = [],
    mizanRows = [],
    mizanTotals = null,
    tolerance = BORC_ALACAK_TOLERANCE
  } = {}) {
    const hasMuavin = (muavinRows || []).length > 0;
    const hasMizan = (mizanRows || []).length > 0;
    if (!hasMuavin || !hasMizan) {
      return {
        status: "EVIDENCE_MISSING",
        code: E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_EVIDENCE_MISSING,
        matched: false,
        message: !hasMuavin && !hasMizan ? "Muavin ve mizan y\xFCklenmedi; mutabakat yap\u0131lamad\u0131." : !hasMuavin ? "Muavin yok; mizan ile mutab\u0131k denemez." : "Mizan yok; muavin ile mutab\u0131k denemez.",
        differences: [],
        onlyMuavin: [],
        onlyMizan: [],
        comparedAccounts: 0,
        grandTotals: null,
        leafAccountCount: 0,
        parentAccountCount: 0
      };
    }
    const leafMizanRows = filterLeafMizanRows(mizanRows);
    const parentAccountCount = (mizanRows || []).filter(
      (row) => row.mizanAccountRole === MIZAN_ACCOUNT_ROLE.PARENT
    ).length;
    const sumByAccount = (rows, isMizan) => {
      const map = /* @__PURE__ */ new Map();
      for (const row of rows) {
        const key = compactAccountCode(row.hesapKodu);
        if (!key) continue;
        const cur = map.get(key) || {
          hesapKodu: row.hesapKodu,
          borc: 0,
          alacak: 0,
          bakiye: 0
        };
        cur.borc = roundMoney3(cur.borc + roundMoney3(row.borc));
        cur.alacak = roundMoney3(cur.alacak + roundMoney3(row.alacak));
        cur.bakiye = roundMoney3(cur.borc - cur.alacak);
        map.set(key, cur);
      }
      return map;
    };
    const muavinMap = sumByAccount(muavinRows, false);
    const mizanMap = sumByAccount(leafMizanRows, true);
    const allKeys = /* @__PURE__ */ new Set([...muavinMap.keys(), ...mizanMap.keys()]);
    const differences = [];
    const onlyMuavin = [];
    const onlyMizan = [];
    for (const key of allKeys) {
      const m = muavinMap.get(key);
      const z = mizanMap.get(key);
      if (m && !z) {
        onlyMuavin.push(m.hesapKodu);
        continue;
      }
      if (z && !m) {
        onlyMizan.push(z.hesapKodu);
        continue;
      }
      const borcFark = roundMoney3(m.borc - z.borc);
      const alacakFark = roundMoney3(m.alacak - z.alacak);
      if (Math.abs(borcFark) > tolerance || Math.abs(alacakFark) > tolerance) {
        const bakiyeFark = roundMoney3(m.bakiye - z.bakiye);
        differences.push({
          hesapKodu: m.hesapKodu,
          muavin: { borc: m.borc, alacak: m.alacak, bakiye: m.bakiye },
          mizan: { borc: z.borc, alacak: z.alacak, bakiye: z.bakiye },
          fark: { borc: borcFark, alacak: alacakFark, bakiye: bakiyeFark }
        });
      }
    }
    const grandTotals = verifyMizanMuavinGrandTotals({
      muavinRows,
      mizanTotals,
      leafMizanRows,
      tolerance
    });
    const leafMatched = differences.length === 0 && onlyMuavin.length === 0 && onlyMizan.length === 0;
    const matched = leafMatched && grandTotals.matched;
    return {
      status: matched ? "MATCHED" : "MISMATCH",
      code: matched ? "" : E_DEFTER_ISSUE_CODE.MIZAN_MUAVIN_MISMATCH,
      matched,
      message: matched ? "Muavin ve mizan hesap baz\u0131nda mutab\u0131k." : leafMatched && !grandTotals.matched ? "Muavin\u2194mizan genel toplam fark\u0131 var." : "Muavin\u2194mizan fark\u0131 veya yaln\u0131z bir tarafta olan hesap var.",
      differences,
      onlyMuavin,
      onlyMizan,
      comparedAccounts: allKeys.size,
      leafAccountCount: new Set(leafMizanRows.map((row) => row.hesapKodu)).size,
      parentAccountCount,
      grandTotals
    };
  }
  function moneyKey(value) {
    return roundMoney3(value).toFixed(2);
  }
  var MUAVIN_YEVMIYE_DIFF_STATUS = {
    ONLY_MUAVIN: "ONLY_MUAVIN",
    ONLY_YEVMIYE: "ONLY_YEVMIYE",
    AMOUNT_DIFF: "AMOUNT_DIFF",
    DATE_DIFF: "DATE_DIFF",
    FIS_DIFF: "FIS_DIFF"
  };
  var MUAVIN_YEVMIYE_DIFF_LABEL = {
    ONLY_MUAVIN: "Yaln\u0131z muavinde",
    ONLY_YEVMIYE: "Yaln\u0131z yevmiyede",
    AMOUNT_DIFF: "Tutar farkl\u0131",
    DATE_DIFF: "Tarih farkl\u0131",
    FIS_DIFF: "Fi\u015F numaras\u0131 farkl\u0131"
  };
  function muavinYevmiyeMatchKey(row = {}) {
    return [
      compact(row.fisNo),
      String(row.tarih || "").trim(),
      compactAccountCode(row.hesapKodu),
      moneyKey(row.borc),
      moneyKey(row.alacak)
    ].join("|");
  }
  function pushMultiset(map, key, row) {
    const list = map.get(key) || [];
    list.push(row);
    map.set(key, list);
  }
  function takeMultiset(map, key) {
    const list = map.get(key);
    if (!list?.length) return null;
    const row = list.shift();
    if (!list.length) map.delete(key);
    else map.set(key, list);
    return row;
  }
  function flattenMultiset(map) {
    const rows = [];
    for (const list of map.values()) rows.push(...list);
    return rows;
  }
  function sideSnapshot(row = {}) {
    return {
      fisNo: row.fisNo || "",
      tarih: row.tarih || "",
      hesapKodu: row.hesapKodu || "",
      borc: roundMoney3(row.borc),
      alacak: roundMoney3(row.alacak)
    };
  }
  function buildMuavinYevmiyeDiff({
    status,
    muavin = null,
    yevmiye = null
  } = {}) {
    const primary = muavin || yevmiye || {};
    return {
      status,
      statusLabel: MUAVIN_YEVMIYE_DIFF_LABEL[status] || status,
      fisNo: primary.fisNo || muavin?.fisNo || yevmiye?.fisNo || "",
      tarih: primary.tarih || muavin?.tarih || yevmiye?.tarih || "",
      hesapKodu: primary.hesapKodu || muavin?.hesapKodu || yevmiye?.hesapKodu || "",
      muavin: muavin ? sideSnapshot(muavin) : null,
      yevmiye: yevmiye ? sideSnapshot(yevmiye) : null
    };
  }
  function findSoftPeer(map, predicate) {
    for (const [key, list] of map.entries()) {
      if (!list?.length) continue;
      if (predicate(list[0], key)) return key;
    }
    return null;
  }
  function reconcileMuavinYevmiye({
    muavinRows = [],
    yevmiyeRows = [],
    tolerance = BORC_ALACAK_TOLERANCE
  } = {}) {
    const hasMuavin = (muavinRows || []).length > 0;
    const hasYevmiye = (yevmiyeRows || []).length > 0;
    const empty = {
      status: "EVIDENCE_MISSING",
      matched: false,
      message: !hasMuavin && !hasYevmiye ? "Muavin ve yevmiye y\xFCklenmedi; mutabakat yap\u0131lamad\u0131." : !hasMuavin ? "Muavin yok; yevmiye ile e\u015Fle\u015Ftirme yap\u0131lamad\u0131." : "Yevmiye yok; muavin ile e\u015Fle\u015Ftirme yap\u0131lamad\u0131.",
      matchedCount: 0,
      denominator: Math.max(muavinRows?.length || 0, yevmiyeRows?.length || 0),
      onlyMuavin: [],
      onlyYevmiye: [],
      amountMismatches: [],
      differences: [],
      counts: {
        onlyMuavin: 0,
        onlyYevmiye: 0,
        amountDiff: 0,
        dateDiff: 0,
        fisDiff: 0,
        total: 0
      },
      muavinMovements: muavinRows?.length || 0,
      yevmiyeMovements: yevmiyeRows?.length || 0,
      yevmiyeVouchers: 0
    };
    if (!hasMuavin || !hasYevmiye) return empty;
    const yevmiyeVouchers = new Set(
      (yevmiyeRows || []).map((row) => compact(row.fisNo)).filter(Boolean)
    ).size;
    const muavinMap = /* @__PURE__ */ new Map();
    for (const row of muavinRows) {
      if (!compactAccountCode(row.hesapKodu)) continue;
      pushMultiset(muavinMap, muavinYevmiyeMatchKey(row), row);
    }
    let matchedCount = 0;
    const residualYevmiye = [];
    for (const row of yevmiyeRows) {
      if (!compactAccountCode(row.hesapKodu)) continue;
      const taken = takeMultiset(muavinMap, muavinYevmiyeMatchKey(row));
      if (taken) {
        matchedCount += 1;
        continue;
      }
      residualYevmiye.push(row);
    }
    const residualMuavinMap = muavinMap;
    const residualYevmiyeMap = /* @__PURE__ */ new Map();
    for (const row of residualYevmiye) {
      pushMultiset(residualYevmiyeMap, muavinYevmiyeMatchKey(row), row);
    }
    const differences = [];
    const softPairOnce = (status, predicate) => {
      for (const yRow of flattenMultiset(residualYevmiyeMap)) {
        const yKey = muavinYevmiyeMatchKey(yRow);
        if (!residualYevmiyeMap.get(yKey)?.length) continue;
        const softKey = findSoftPeer(residualMuavinMap, (m) => predicate(m, yRow));
        if (!softKey) continue;
        const mRow = takeMultiset(residualMuavinMap, softKey);
        const takenY = takeMultiset(residualYevmiyeMap, yKey);
        if (!mRow || !takenY) {
          if (mRow) pushMultiset(residualMuavinMap, softKey, mRow);
          if (takenY) pushMultiset(residualYevmiyeMap, yKey, takenY);
          continue;
        }
        if (status === MUAVIN_YEVMIYE_DIFF_STATUS.AMOUNT_DIFF) {
          const borcGap = Math.abs(roundMoney3(mRow.borc) - roundMoney3(takenY.borc));
          const alacakGap = Math.abs(roundMoney3(mRow.alacak) - roundMoney3(takenY.alacak));
          if (borcGap <= tolerance && alacakGap <= tolerance) {
            matchedCount += 1;
            return true;
          }
        }
        differences.push(
          buildMuavinYevmiyeDiff({
            status,
            muavin: mRow,
            yevmiye: takenY
          })
        );
        return true;
      }
      return false;
    };
    while (softPairOnce(
      MUAVIN_YEVMIYE_DIFF_STATUS.AMOUNT_DIFF,
      (m, y) => compact(m.fisNo) === compact(y.fisNo) && String(m.tarih || "").trim() === String(y.tarih || "").trim() && compactAccountCode(m.hesapKodu) === compactAccountCode(y.hesapKodu)
    )) {
    }
    while (softPairOnce(
      MUAVIN_YEVMIYE_DIFF_STATUS.DATE_DIFF,
      (m, y) => compact(m.fisNo) === compact(y.fisNo) && compactAccountCode(m.hesapKodu) === compactAccountCode(y.hesapKodu) && moneyKey(m.borc) === moneyKey(y.borc) && moneyKey(m.alacak) === moneyKey(y.alacak)
    )) {
    }
    while (softPairOnce(
      MUAVIN_YEVMIYE_DIFF_STATUS.FIS_DIFF,
      (m, y) => String(m.tarih || "").trim() === String(y.tarih || "").trim() && compactAccountCode(m.hesapKodu) === compactAccountCode(y.hesapKodu) && moneyKey(m.borc) === moneyKey(y.borc) && moneyKey(m.alacak) === moneyKey(y.alacak)
    )) {
    }
    const onlyMuavin = [];
    for (const row of flattenMultiset(residualMuavinMap)) {
      const diff = buildMuavinYevmiyeDiff({
        status: MUAVIN_YEVMIYE_DIFF_STATUS.ONLY_MUAVIN,
        muavin: row
      });
      onlyMuavin.push(diff);
      differences.push(diff);
    }
    const onlyYevmiye = [];
    for (const row of flattenMultiset(residualYevmiyeMap)) {
      const diff = buildMuavinYevmiyeDiff({
        status: MUAVIN_YEVMIYE_DIFF_STATUS.ONLY_YEVMIYE,
        yevmiye: row
      });
      onlyYevmiye.push(diff);
      differences.push(diff);
    }
    const amountMismatches = differences.filter(
      (d) => d.status === MUAVIN_YEVMIYE_DIFF_STATUS.AMOUNT_DIFF
    );
    const counts = {
      onlyMuavin: onlyMuavin.length,
      onlyYevmiye: onlyYevmiye.length,
      amountDiff: amountMismatches.length,
      dateDiff: differences.filter((d) => d.status === MUAVIN_YEVMIYE_DIFF_STATUS.DATE_DIFF).length,
      fisDiff: differences.filter((d) => d.status === MUAVIN_YEVMIYE_DIFF_STATUS.FIS_DIFF).length,
      total: differences.length
    };
    const denominator = Math.max(muavinRows.length, yevmiyeRows.length);
    const matched = counts.total === 0;
    return {
      status: matched ? "MATCHED" : "MISMATCH",
      matched,
      message: matched ? `Tam e\u015Fle\u015Fti (${denominator}/${denominator})` : `${matchedCount}/${denominator} e\u015Fle\u015Fti`,
      matchedCount,
      denominator,
      onlyMuavin,
      onlyYevmiye,
      amountMismatches,
      differences,
      counts,
      muavinMovements: muavinRows.length,
      yevmiyeMovements: yevmiyeRows.length,
      yevmiyeVouchers
    };
  }
  function emptyCounters() {
    return {
      parseInvocations: 0,
      analysisInvocations: 0,
      persistInvocations: 0
    };
  }
  function runGenelMuhasebeKontrol({
    companyId = "",
    period = "",
    muavinSheetRows = null,
    yevmiyeSheetRows = null,
    mizanSheetRows = null,
    accountPlanAccounts = null,
    accountPlanStatus = "unknown",
    spies = null
  } = {}) {
    const counters = spies || emptyCounters();
    const timing = {
      parseMs: 0,
      groupingMs: 0,
      counterpartMs: 0,
      accountingMs: 0,
      reconcileMs: 0,
      totalMs: 0
    };
    const tTotal0 = performance.now();
    const parsed = { muavin: [], yevmiye: [], mizan: [], classes: {}, mizanTotals: null, mizanStructure: null };
    const ingest = (sheetRows, preferredHint) => {
      if (!sheetRows) return;
      counters.parseInvocations += 1;
      const t0 = performance.now();
      const contentClass = classifyLedgerDocumentType(sheetRows);
      const documentClass = refineDocumentClass(contentClass, preferredHint, sheetRows);
      const { rows } = parseLedgerByDocumentClass(sheetRows, documentClass);
      timing.parseMs += performance.now() - t0;
      parsed.classes[documentClass] = (parsed.classes[documentClass] || 0) + 1;
      if (documentClass === GENEL_MUHASEBE_DOC_CLASS.MIZAN) parsed.mizan.push(...rows);
      else if (documentClass === GENEL_MUHASEBE_DOC_CLASS.MUAVIN) parsed.muavin.push(...rows);
      else parsed.yevmiye.push(...rows);
    };
    if (muavinSheetRows) ingest(muavinSheetRows, GENEL_MUHASEBE_DOC_CLASS.MUAVIN);
    if (yevmiyeSheetRows) {
      ingest(yevmiyeSheetRows, GENEL_MUHASEBE_DOC_CLASS.YEVMIYE);
      parsed.yevmiye = parsed.yevmiye.filter(isValidYevmiyeMovementRow);
      if (!parsed.yevmiye.length) {
        const layout = detectYevmiyeLayout(yevmiyeSheetRows);
        if (layout === YEVMIYE_LAYOUT.UNKNOWN) {
          throw Object.assign(new Error("Desteklenmeyen yevmiye d\xFCzeni."), {
            code: "UNSUPPORTED_YEVMIYE_LAYOUT"
          });
        }
        throw Object.assign(new Error("Yevmiye dosyas\u0131ndan hareket okunamad\u0131."), {
          code: "EMPTY_YEVMIYE_PARSE"
        });
      }
    }
    if (mizanSheetRows) {
      counters.parseInvocations += 1;
      const t0 = performance.now();
      const contentClass = classifyLedgerDocumentType(mizanSheetRows);
      const documentClass = refineDocumentClass(
        contentClass,
        GENEL_MUHASEBE_DOC_CLASS.MIZAN,
        mizanSheetRows
      );
      const bundle = parseMizanSheetWithStructure(mizanSheetRows);
      timing.parseMs += performance.now() - t0;
      parsed.classes[documentClass] = (parsed.classes[documentClass] || 0) + 1;
      parsed.mizan.push(
        ...bundle.rows.map((row) => ({
          ...row,
          documentClass: GENEL_MUHASEBE_DOC_CLASS.MIZAN
        }))
      );
      parsed.mizanTotals = bundle.mizanTotals;
      parsed.mizanStructure = bundle.stats;
    }
    const stamp = (rows) => rows.map((row) => ({
      ...row,
      period: period || row.period || "",
      companyId: companyId || row.companyId || ""
    }));
    parsed.muavin = stamp(parsed.muavin);
    parsed.yevmiye = stamp(parsed.yevmiye);
    parsed.mizan = stamp(parsed.mizan);
    const planLoaded = Array.isArray(accountPlanAccounts);
    const planCodeCount = planLoaded ? accountPlanAccounts.filter((account) => accountCodeFromPlanRow(account)).length : 0;
    const planEmpty = planLoaded && planCodeCount === 0;
    const planEvidencePresent = planLoaded && !planEmpty && accountPlanStatus !== "missing";
    const accountPlanCodes = planEvidencePresent ? buildAccountPlanCodeSet(accountPlanAccounts) : null;
    counters.analysisInvocations += 1;
    const tAcc0 = performance.now();
    const yevmiyeEvidencePresent = parsed.yevmiye.length > 0;
    const pipeline = runEDefterKontrolPipeline({
      muavinRows: yevmiyeEvidencePresent ? [] : parsed.muavin,
      yevmiyeRows: yevmiyeEvidencePresent ? parsed.yevmiye : [],
      mizanRows: parsed.mizan,
      companyId,
      period,
      accountPlanCodes,
      yevmiyeEvidencePresent,
      cumulativePeriod: true
    });
    timing.accountingMs += performance.now() - tAcc0;
    const tRec0 = performance.now();
    const reconcile = reconcileMizanMuavin({
      muavinRows: parsed.muavin,
      mizanRows: parsed.mizan,
      mizanTotals: parsed.mizanTotals
    });
    const muavinYevmiye = reconcileMuavinYevmiye({
      muavinRows: parsed.muavin,
      yevmiyeRows: parsed.yevmiye
    });
    timing.reconcileMs += performance.now() - tRec0;
    const ledgerRows = pipeline.rows.filter(
      (row) => !isSyntheticSystemFindingRow2(row) && (row.kaynak === E_DEFTER_KAYNAK.MUAVIN || row.kaynak === E_DEFTER_KAYNAK.YEVMIYE || row.kaynak === E_DEFTER_KAYNAK.YEVMIYE_XML)
    );
    const systemFindingRows = pipeline.rows.filter((row) => isSyntheticSystemFindingRow2(row));
    let resolved = 0;
    let multi = 0;
    let review = 0;
    for (const row of ledgerRows) {
      if (row.openingVoucherClass) continue;
      const codes = new Set((row.issueDetails || []).map((i) => i.code));
      if (codes.has(E_DEFTER_ISSUE_CODE.MULTI_COUNTERPART)) multi += 1;
      else if (codes.has(E_DEFTER_ISSUE_CODE.MISSING_COUNTERPART) || codes.has(E_DEFTER_ISSUE_CODE.COUNTERPART_SAME_SIDE) || codes.has(E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW) || codes.has(E_DEFTER_ISSUE_CODE.COUNTERPART_SELF) || codes.has(E_DEFTER_ISSUE_CODE.COUNTERPART_CONFLICT)) {
        review += 1;
      } else if (row.counterAccountCode || row.karsiHesapKodu) {
        resolved += 1;
      }
    }
    timing.counterpartMs = 0;
    timing.groupingMs = 0;
    const extraFindings = [];
    if (!planEvidencePresent) {
      extraFindings.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.PLAN_EVIDENCE_MISSING,
          message: "Hesap plan\u0131 y\xFCklenemedi; \u2018hesap planda yok\u2019 karar\u0131 verilmedi.",
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 8
        })
      );
    }
    if (reconcile.status === "EVIDENCE_MISSING") {
      const mizanMissingOnly = parsed.muavin.length > 0 && parsed.mizan.length === 0;
      extraFindings.push(
        createEDefterIssue({
          code: reconcile.code,
          message: mizanMissingOnly ? "Mizan y\xFCklenmedi" : reconcile.message,
          severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 8
        })
      );
    } else if (reconcile.status === "MISMATCH") {
      extraFindings.push(
        createEDefterIssue({
          code: reconcile.code,
          message: `${reconcile.message} (fark=${reconcile.differences.length}, yaln\u0131zMuavin=${reconcile.onlyMuavin.length}, yaln\u0131zMizan=${reconcile.onlyMizan.length})`,
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 22
        })
      );
    }
    if (muavinYevmiye.status === "MISMATCH") {
      if (!muavinYevmiye.differences?.length) {
        throw Object.assign(
          new Error("Muavin\u2194yevmiye fark\u0131 tespit edildi fakat ayr\u0131nt\u0131 \xFCretilemedi."),
          { code: E_DEFTER_ISSUE_CODE.MUAVIN_YEVMIYE_RECONCILE_FAILED }
        );
      }
      extraFindings.push(
        createEDefterIssue({
          code: E_DEFTER_ISSUE_CODE.MUAVIN_YEVMIYE_MISMATCH,
          message: `${muavinYevmiye.message} (yaln\u0131z muavin=${muavinYevmiye.counts.onlyMuavin}, yaln\u0131z yevmiye=${muavinYevmiye.counts.onlyYevmiye}, tutar=${muavinYevmiye.counts.amountDiff}, tarih=${muavinYevmiye.counts.dateDiff}, fi\u015F=${muavinYevmiye.counts.fisDiff})`,
          severity: E_DEFTER_ISSUE_SEVERITY.UYARI,
          group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
          riskScore: 22
        })
      );
      for (const diff of muavinYevmiye.differences) {
        const m = diff.muavin;
        const y = diff.yevmiye;
        const moneyPart = [
          m ? `Muavin ${roundMoney3(m.borc)}/${roundMoney3(m.alacak)}` : null,
          y ? `Yevmiye ${roundMoney3(y.borc)}/${roundMoney3(y.alacak)}` : null
        ].filter(Boolean).join(" \xB7 ");
        extraFindings.push({
          ...createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.MUAVIN_YEVMIYE_MISMATCH,
            message: `${diff.statusLabel}${moneyPart ? ` \u2014 ${moneyPart}` : ""}`,
            severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
            group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
            riskScore: 8
          }),
          fisNo: diff.fisNo || "",
          tarih: diff.tarih || "",
          hesapKodu: diff.hesapKodu || "",
          statusLabel: diff.statusLabel,
          muavin: m,
          yevmiye: y
        });
      }
    }
    for (const row of ledgerRows) {
      const code = String(row.hesapKodu || "");
      const counter = String(row.counterAccountCode || row.karsiHesapKodu || "");
      const isVadeli = (c) => /^(102\.|111\.|112\.|121\.|3)/.test(c) && /vadeli|mevduat/i.test(`${row.hesapAdi || ""} ${row.aciklama || ""}`);
      if (counter && isVadeli(code) && isVadeli(counter)) {
        extraFindings.push(
          createEDefterIssue({
            code: E_DEFTER_ISSUE_CODE.COUNTERPART_REVIEW,
            message: "Vadeli hesaplar aras\u0131nda virman benzeri kar\u015F\u0131t \u2014 inceleme gerekli.",
            severity: E_DEFTER_ISSUE_SEVERITY.BILGI,
            group: E_DEFTER_KONTROL_GRUP.INCELEME_GEREKLI,
            riskScore: 10
          })
        );
        break;
      }
    }
    const issueFlat = pipeline.rows.flatMap((row) => row.issueDetails || []);
    const countCode = (code) => issueFlat.filter((i) => i.code === code).length;
    const totals = ledgerRows.reduce(
      (acc, row) => {
        acc.borc = roundMoney3(acc.borc + roundMoney3(row.borc));
        acc.alacak = roundMoney3(acc.alacak + roundMoney3(row.alacak));
        return acc;
      },
      { borc: 0, alacak: 0 }
    );
    const fisKeys = new Set(
      ledgerRows.map((row) => compact(row.fisNo)).filter(Boolean)
    );
    const unbalanced = /* @__PURE__ */ new Set();
    const byFis = /* @__PURE__ */ new Map();
    for (const row of ledgerRows) {
      const key = compact(row.fisNo);
      if (!key) continue;
      const cur = byFis.get(key) || { borc: 0, alacak: 0 };
      cur.borc = roundMoney3(cur.borc + roundMoney3(row.borc));
      cur.alacak = roundMoney3(cur.alacak + roundMoney3(row.alacak));
      byFis.set(key, cur);
    }
    for (const [key, bal] of byFis.entries()) {
      if (Math.abs(bal.borc - bal.alacak) > BORC_ALACAK_TOLERANCE) unbalanced.add(key);
    }
    let overallSonuc = pipeline.overallSonuc || E_DEFTER_SONUC_SEVIYE.UYGUN;
    const findingsCatalog = buildGenelMuhasebeFindingsCatalog({
      rows: pipeline.rows,
      findingExtras: extraFindings
    });
    const findingsSummary = summarizeGenelMuhasebeFindingsCatalog(findingsCatalog);
    overallSonuc = findingsSummary.overallSonuc;
    counters.persistInvocations = 0;
    timing.totalMs = performance.now() - tTotal0;
    const mizanMuavinSummary = {
      ...reconcile,
      userLabel: reconcile.status === "EVIDENCE_MISSING" && parsed.muavin.length > 0 && parsed.mizan.length === 0 ? "Mizan y\xFCklenmedi" : reconcile.status === "EVIDENCE_MISSING" ? reconcile.message : reconcile.status === "MATCHED" || reconcile.matched ? "Tam e\u015Fle\u015Fti" : reconcile.status === "MISMATCH" ? "Fark var" : reconcile.status || "\u2014",
      structure: parsed.mizanStructure,
      mizanTotals: parsed.mizanTotals
    };
    const yevmiyeFisKeys = new Set(
      parsed.yevmiye.map((row) => compact(row.fisNo)).filter(Boolean)
    );
    const muavinYevmiyeSummary = {
      ...muavinYevmiye,
      userLabel: muavinYevmiye.status === "EVIDENCE_MISSING" ? "Kar\u015F\u0131la\u015Ft\u0131r\u0131lamad\u0131" : muavinYevmiye.matched ? `Tam e\u015Fle\u015Fti (${muavinYevmiye.denominator}/${muavinYevmiye.denominator})` : `${muavinYevmiye.matchedCount}/${muavinYevmiye.denominator} e\u015Fle\u015Fti`
    };
    return {
      mode: "local-control",
      companyId,
      period,
      rows: pipeline.rows,
      findingExtras: extraFindings,
      findingsCatalog,
      findingsSummary,
      documentClasses: parsed.classes,
      parsedCounts: {
        muavin: parsed.muavin.length,
        yevmiye: parsed.yevmiye.length,
        mizan: parsed.mizan.length
      },
      timing: {
        parseMs: Math.round(timing.parseMs),
        groupingMs: Math.round(timing.groupingMs),
        counterpartMs: Math.round(timing.counterpartMs),
        accountingMs: Math.round(timing.accountingMs),
        reconcileMs: Math.round(timing.reconcileMs),
        totalMs: Math.round(timing.totalMs)
      },
      summary: {
        // Movement totals only — synthetic system findings excluded from hareket/fiş/BA.
        toplamSatir: ledgerRows.length,
        hareketSatir: ledgerRows.length,
        sistemBilgisi: systemFindingRows.length,
        toplamFis: fisKeys.size,
        dengeliFis: Math.max(0, fisKeys.size - unbalanced.size),
        dengesizFis: unbalanced.size,
        kesinKarsit: resolved,
        cokluKarsit: multi,
        // BİLGİ extras (mizan/plan) must not inflate inceleme counter.
        incelemeGerekli: findingsSummary.incelemeGerekli,
        hesapPlandaYok: countCode(E_DEFTER_ISSUE_CODE.ACCOUNT_NOT_IN_PLAN),
        donemDisi: countCode(E_DEFTER_ISSUE_CODE.DATE_OUT_OF_PERIOD),
        mukerrer: countCode(E_DEFTER_ISSUE_CODE.DUPLICATE_ENTRY),
        borcToplam: totals.borc,
        alacakToplam: totals.alacak,
        borcAlacakFark: roundMoney3(totals.borc - totals.alacak),
        mizanMuavin: mizanMuavinSummary,
        muavinYevmiye: muavinYevmiyeSummary,
        muavinHareketSatir: parsed.muavin.length,
        yevmiyeHareketSatir: parsed.yevmiye.length,
        yevmiyeFis: yevmiyeFisKeys.size,
        yevmiyeEvidence: yevmiyeEvidencePresent ? "PRESENT" : "MISSING",
        planEvidence: planEvidencePresent ? "PRESENT" : "MISSING",
        planStatus: planEvidencePresent ? "loaded" : "missing",
        overallSonuc,
        edefterUygun: overallSonuc === E_DEFTER_SONUC_SEVIYE.UYGUN || overallSonuc === E_DEFTER_SONUC_SEVIYE.BILGI,
        localOnly: true
      },
      counters: { ...counters },
      pipelineSummary: pipeline.summary
    };
  }

  // src/utils/eDefterAnalyzeContract.js
  var EDEFTER_ANALYZE_PROTOCOL = 1;
  var EDEFTER_ANALYZE_JOB_KIND = {
    E_DEFTER_CONTROL: "E_DEFTER_CONTROL",
    GENERAL_LEDGER_CONTROL: "GENERAL_LEDGER_CONTROL"
  };
  var SENSITIVE_KEY_RE = /^(xml|zip|raw|content|filePath|absolutePath|token|secret|password|authorization|env)$/i;
  function resolveAnalyzeJobKind(value) {
    const raw = String(value || "").trim();
    if (raw === EDEFTER_ANALYZE_JOB_KIND.GENERAL_LEDGER_CONTROL) {
      return EDEFTER_ANALYZE_JOB_KIND.GENERAL_LEDGER_CONTROL;
    }
    return EDEFTER_ANALYZE_JOB_KIND.E_DEFTER_CONTROL;
  }
  function cloneJson(value, depth = 0) {
    if (depth > 12) return null;
    if (value == null) return value;
    if (typeof value === "string") return value.slice(0, 4e3);
    if (typeof value === "number") {
      if (!Number.isFinite(value)) return 0;
      return value;
    }
    if (typeof value === "boolean") return value;
    if (typeof ArrayBuffer !== "undefined" && value instanceof ArrayBuffer) {
      return null;
    }
    if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView?.(value)) {
      return null;
    }
    if (Array.isArray(value)) {
      return value.slice(0, 2e5).map((item) => cloneJson(item, depth + 1));
    }
    if (typeof value !== "object") return null;
    if (value instanceof Date) return value.toISOString();
    if (value instanceof Map || value instanceof Set) return null;
    const out = {};
    for (const [key, raw] of Object.entries(value)) {
      if (SENSITIVE_KEY_RE.test(key)) continue;
      if (typeof raw === "function" || typeof raw === "symbol") continue;
      out[key] = cloneJson(raw, depth + 1);
    }
    return out;
  }
  function sanitizeRow(row = {}) {
    return cloneJson({
      id: row.id,
      kaynak: row.kaynak,
      documentClass: row.documentClass,
      fisNo: row.fisNo,
      yevmiyeNo: row.yevmiyeNo,
      tarih: row.tarih,
      belgeNo: row.belgeNo,
      belgeTarihi: row.belgeTarihi,
      fisTarihi: row.fisTarihi,
      evrakNo: row.evrakNo,
      fisTuru: row.fisTuru,
      cariUnvan: row.cariUnvan ? String(row.cariUnvan).slice(0, 120) : "",
      hesapKodu: row.hesapKodu,
      hesapAdi: row.hesapAdi ? String(row.hesapAdi).slice(0, 120) : "",
      aciklama: row.aciklama ? String(row.aciklama).slice(0, 160) : "",
      borc: row.borc,
      alacak: row.alacak,
      tutar: row.tutar,
      paraBirimi: row.paraBirimi,
      companyId: row.companyId,
      period: row.period,
      counterAccountCode: row.counterAccountCode || row.karsiHesapKodu || "",
      karsiHesapKodu: row.karsiHesapKodu || row.counterAccountCode || "",
      issueDetails: row.issueDetails,
      issues: row.issues,
      grup: row.grup,
      riskScore: row.riskScore,
      sonucSeviye: row.sonucSeviye,
      disaridaBirak: row.disaridaBirak
    });
  }
  function collectIssueCodes(rows = []) {
    return rows.flatMap((row) => {
      if (Array.isArray(row.issueDetails)) {
        return row.issueDetails.map((d) => d.code || d.message || "");
      }
      if (Array.isArray(row.issues)) return row.issues.map((x) => String(x));
      return [];
    }).filter(Boolean).sort();
  }
  function buildResultFingerprint(result = {}) {
    const summary = result.summary || {};
    const rows = Array.isArray(result.rows) ? result.rows : [];
    const issueCodes = collectIssueCodes(rows);
    if (result.mode === "local-control" || summary.localOnly === true || result.jobKind === EDEFTER_ANALYZE_JOB_KIND.GENERAL_LEDGER_CONTROL) {
      const mizan = summary.mizanMuavin || {};
      return [
        "GL",
        rows.length,
        summary.toplamSatir ?? "",
        summary.toplamFis ?? "",
        summary.kesinKarsit ?? "",
        summary.cokluKarsit ?? "",
        summary.incelemeGerekli ?? "",
        summary.hesapPlandaYok ?? "",
        summary.borcToplam ?? "",
        summary.alacakToplam ?? "",
        summary.borcAlacakFark ?? "",
        summary.planEvidence ?? "",
        mizan.status ?? "",
        mizan.matched === true ? "1" : "0",
        summary.overallSonuc || result.overallSonuc || "",
        summary.edefterUygun === true ? "1" : "0",
        JSON.stringify(result.documentClasses || {}),
        issueCodes.join("|")
      ].join("::");
    }
    return [
      rows.length,
      summary.overallSonuc || result.overallSonuc || "",
      summary.edefterUygun === true ? "1" : "0",
      summary.findingCount ?? summary.bulguSayisi ?? "",
      summary.kritikHata ?? "",
      issueCodes.join("|")
    ].join("::");
  }
  function sanitizeAnalyzeResult(result = {}, diagnostics = {}) {
    const rows = Array.isArray(result.rows) ? result.rows.map(sanitizeRow) : [];
    const summary = cloneJson(result.summary || {});
    const jobKind = diagnostics.jobKind || result.jobKind || (result.mode === "local-control" ? EDEFTER_ANALYZE_JOB_KIND.GENERAL_LEDGER_CONTROL : EDEFTER_ANALYZE_JOB_KIND.E_DEFTER_CONTROL);
    return {
      ok: true,
      jobKind,
      mode: result.mode || "",
      duplicate: Boolean(result.duplicate),
      duplicateMessage: result.duplicateMessage ? String(result.duplicateMessage).slice(0, 240) : "",
      rows,
      summary,
      groupCounts: cloneJson(result.groupCounts || []),
      overallSonuc: result.overallSonuc || summary?.overallSonuc || "",
      disclaimer: result.disclaimer ? String(result.disclaimer).slice(0, 500) : "",
      journalLedger: cloneJson(result.journalLedger || null),
      findingExtras: cloneJson(result.findingExtras || []),
      findingsCatalog: cloneJson(result.findingsCatalog || []),
      findingsSummary: cloneJson(result.findingsSummary || {}),
      documentClasses: cloneJson(result.documentClasses || {}),
      parsedCounts: cloneJson(result.parsedCounts || {}),
      timing: cloneJson(result.timing || {}),
      counters: cloneJson(result.counters || {}),
      resultFingerprint: buildResultFingerprint({
        rows,
        summary,
        overallSonuc: result.overallSonuc,
        mode: result.mode,
        jobKind,
        documentClasses: result.documentClasses
      }),
      diagnostics: cloneJson({
        ...diagnostics,
        jobKind,
        rowCount: rows.length,
        protocolVersion: EDEFTER_ANALYZE_PROTOCOL
      })
    };
  }
  async function executeEDefterAnalyzePayload(payload = {}) {
    const jobKind = resolveAnalyzeJobKind(payload.jobKind || payload.jobType);
    if (jobKind === EDEFTER_ANALYZE_JOB_KIND.GENERAL_LEDGER_CONTROL) {
      return runGenelMuhasebeKontrol({
        companyId: payload.companyId || "",
        period: payload.period || "",
        muavinSheetRows: payload.muavinSheetRows,
        yevmiyeSheetRows: payload.yevmiyeSheetRows,
        mizanSheetRows: payload.mizanSheetRows,
        accountPlanAccounts: payload.accountPlanAccounts,
        accountPlanStatus: payload.accountPlanStatus || "unknown"
      });
    }
    return runOneClickEDefterKontrol({
      parsedUpload: payload.parsedUpload || null,
      muavinRows: payload.muavinRows || [],
      yevmiyeRows: payload.yevmiyeRows || [],
      mizanRows: payload.mizanRows || [],
      edefterListeRows: payload.edefterListeRows || [],
      companyId: payload.companyId || "",
      companyTaxId: payload.companyTaxId || "",
      period: payload.period || "",
      accountPlanCodes: payload.accountPlanCodes || null,
      declarationRecords: payload.declarationRecords || [],
      coreDecision: payload.coreDecision || null,
      fingerprintSession: null
    });
  }

  // src/workers/workerUtils.js
  var WORKER_PARSE_STAGES = {
    READING: "Dosya okunuyor",
    PARSING: "Veri ayr\u0131\u015Ft\u0131r\u0131l\u0131yor",
    NORMALIZING: "Sat\u0131rlar normalize ediliyor",
    ANALYZING: "Analiz \xE7al\u0131\u015F\u0131yor",
    BATCHING: "Sonu\xE7lar haz\u0131rlan\u0131yor",
    DONE: "Tamamland\u0131"
  };
  function yieldToWorker() {
    return new Promise((resolve) => {
      setTimeout(resolve, 0);
    });
  }
  function postProgress(stage, detail = "", percent = null) {
    self.postMessage({
      type: "progress",
      stage,
      detail,
      percent
    });
  }

  // src/workers/eDefterAnalyze.worker.js
  self.onmessage = async (event) => {
    const data = event.data || {};
    const requestId = data.requestId;
    const protocolVersion = Number(data.protocolVersion || 0);
    const payload = data.payload && typeof data.payload === "object" && !Array.isArray(data.payload) ? data.payload : data;
    try {
      if (!requestId) {
        throw Object.assign(new Error("Analyze requestId zorunlu."), {
          code: "ANALYZE_REQUEST_ID_MISSING"
        });
      }
      if (protocolVersion && protocolVersion !== EDEFTER_ANALYZE_PROTOCOL) {
        throw Object.assign(new Error("Analyze worker protokol s\xFCr\xFCm\xFC uyu\u015Fmuyor."), {
          code: "ANALYZE_PROTOCOL_MISMATCH"
        });
      }
      const jobKind = resolveAnalyzeJobKind(payload?.jobKind || payload?.jobType);
      const analyzingLabel = jobKind === EDEFTER_ANALYZE_JOB_KIND.GENERAL_LEDGER_CONTROL ? "Genel muhasebe kontrol kurallar\u0131 \xE7al\u0131\u015F\u0131yor" : "e-Defter kontrol kurallar\u0131 \xE7al\u0131\u015F\u0131yor";
      postProgress(WORKER_PARSE_STAGES.ANALYZING, analyzingLabel, 20);
      await yieldToWorker();
      const startedAt = Date.now();
      const raw = await executeEDefterAnalyzePayload(payload);
      const elapsedMs = Date.now() - startedAt;
      postProgress(
        WORKER_PARSE_STAGES.DONE,
        `${Array.isArray(raw?.rows) ? raw.rows.length : 0} kay\u0131t kontrol edildi`,
        100
      );
      const result = sanitizeAnalyzeResult(raw, {
        execution: "worker",
        engineInvocations: 1,
        elapsedMs,
        jobKind,
        mainThreadAnalyze: 0
      });
      self.postMessage({
        type: "success",
        requestId,
        result
      });
    } catch (error) {
      self.postMessage({
        type: "error",
        requestId,
        error: error?.message || "Analiz ba\u015Far\u0131s\u0131z.",
        code: error?.code || "ANALYZE_WORKER_FAILED"
      });
    }
  };
})();
